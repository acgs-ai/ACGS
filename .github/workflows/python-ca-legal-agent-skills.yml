# Path-filtered CI for packages/ca-legal-agent-skills/ — CaLegal Canadian legal AI agent skills runtime.
# Triggers only when the submodule path, .gitmodules, or this workflow change.

name: python-ca-legal-agent-skills

on:
  pull_request:
    branches: [master]
    paths:
      - 'packages/ca-legal-agent-skills/**'
      - '.gitmodules'
      - 'pyproject.toml'
      - '.github/workflows/python-ca-legal-agent-skills.yml'
  push:
    branches: [master]
    paths:
      - 'packages/ca-legal-agent-skills/**'
      - '.gitmodules'
      - 'pyproject.toml'
      - '.github/workflows/python-ca-legal-agent-skills.yml'

concurrency:
  group: python-ca-legal-agent-skills-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    working-directory: packages/ca-legal-agent-skills

jobs:
  test:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 15
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      # Two-step checkout: see constitutional-hash.yml for rationale.
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Init submodules
        id: ca-legal-agent-skills
        working-directory: ${{ github.workspace }}
        env:
          SUBMODULE_TOKEN: ${{ secrets.SUBMODULE_TOKEN }}
        run: |
          if [ -n "${SUBMODULE_TOKEN}" ]; then
            git config --global url."https://x-access-token:${SUBMODULE_TOKEN}@github.com/".insteadOf "https://github.com/"
          fi
          git submodule sync -- packages/ca-legal-agent-skills
          if git submodule update --init --recursive -- packages/ca-legal-agent-skills; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::packages/ca-legal-agent-skills could not be initialized. Rotate SUBMODULE_TOKEN to include Contents: Read on dislovelhl/ca-legal-agent-skills, then rerun this workflow for full verification."
          fi

      - uses: actions/setup-python@v5
        if: steps.ca-legal-agent-skills.outputs.available == 'true'
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@v3
        if: steps.ca-legal-agent-skills.outputs.available == 'true'
        with:
          enable-cache: true
          cache-dependency-glob: 'packages/ca-legal-agent-skills/pyproject.toml'

      - name: Install (dev extras)
        if: steps.ca-legal-agent-skills.outputs.available == 'true'
        run: uv pip install --system --no-config --no-sources --editable '.[dev]'

      - name: Lint
        if: steps.ca-legal-agent-skills.outputs.available == 'true'
        run: python -m ruff check .

      - name: Test
        if: steps.ca-legal-agent-skills.outputs.available == 'true'
        run: python -m pytest --import-mode=importlib -q
