# Validation

This hub is docs-first. Validation checks that the expected files exist, solution pages are decision-useful, links exist in the source register, and forbidden overclaim/placeholders are absent.

Run:

```bash
make -C packages/ai-governance-research validate
```

Use `validation/docs-quality-checklist.md` for human review.
