# Quickstart (moved)

> **Core invariant: No valid Decision Receipt, no side effect.**

The canonical 5-minute quickstart now lives in **[`QUICKSTART.md`](QUICKSTART.md)**
(same directory, uppercase). It is the single source of truth for the install
block and the proof block.

This pointer remains so existing links keep resolving. Please update references
to point at `docs/QUICKSTART.md`.
