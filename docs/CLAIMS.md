# Claim ledger

Purpose: prevent marketing drift. Every public claim should be traceable to code, tests, demos, receipt evidence, replay verification, or roadmap.

| Claim | Status | Evidence source | Test or demo | Limitation | Safe public wording |
|---|---|---|---|---|---|
| ACGS is receipt-gated governance for AI-agent side effects. | implemented | `packages/gove-zone/src/gove_zone/*` | `gove-zone smoke`, receipt-gated demo | Local kernel, not a managed production service. | "Local receipt-gated governance layer for AI-agent side effects." |
| No valid Decision Receipt, no side effect. | tested | `executor.py`, `receipt.py` | `test_executor_guard.py`, `examples/tamper_demo`, `proofpack` | Only true for paths wired through the governed executor. | "The governed executor fails closed without a valid receipt." |
| Policy is evaluated before execution. | tested | `kernel.py` | `test_fail_closed.py`, smoke demo | Direct raw-tool calls can bypass if integrator exposes them. | "Kernel dispatch evaluates policy before the registered tool runs." |
| Denied action leaves evidence and does not run. | tested | `kernel.py`, `audit.py` | smoke demo, `test_fail_closed.py` | Local JSONL only. | "Denied local actions are audited and blocked before side effects." |
| Missing receipt is blocked. | tested | `executor.py` | `test_executor_refuses_no_receipt`, `proofpack` | Requires executor gate integration. | "Gated execution rejects missing receipts." |
| Tampered receipt is blocked. | tested | `receipt.py` | `test_decision_receipt.py`, `examples/tamper_demo` | Unsigned hashes are recomputable under host compromise. | "Receipt field tampering is hash-detected; signed mode closes recomputed-forgery when engaged." |
| Actor/action/argument binding exists. | tested | `receipt.py`, `executor.py` | `test_argument_binding.py`, `test_executor_guard.py` | Actor identity comes from integrator runtime context. | "Receipts bind the actor, action, and exact arguments checked by the executor." |
| Tenant and policy binding exist. | tested | `tenant.py`, `policy.py`, `receipt.py` | `test_tenant_safety.py`, `test_policy_bundle_io.py` | No active/stale/revoked policy-bundle lifecycle registry yet. | "Receipts can be checked against expected tenant and policy bundle/hash." |
| Expiry and bounded temporal liveness are supported. | tested | `receipt.py`, `executor.py`, `contracts.py`, `managed_mutations.py` | `test_receipt_expiry.py`, `test_future_issued_receipt_beyond_default_skew_rejects_before_side_effect`, `test_clock_skew_override_can_tighten_but_not_weaken_verification`, `test_receipt_expires_before_issued_rejects_as_liveness_failure` | Expiry is optional for legacy receipts unless a strict profile requires it; receipt-v2 requires expiry. The default and maximum signed/v2 future-issuance skew are both 300 seconds; overrides may only tighten to an integer from 0 through 300, while bool, non-integer, negative, or greater-than-300 values fail before verification, ledger burn, or execution. The check is host-clock-bound unless a caller supplies `now_iso`; tenant bootstrap pins the default 300-second bound in its canonical managed-mutation UoW. There is no built-in trusted time source or global receipt/nonce revocation service. | "Receipts may carry hash-bound expiries; signed and receipt-v2 liveness rejects expired, expiry-before-issuance, too-far-future-issued receipts, or attempts to widen skew before side effects." |
| Audit evidence is tamper-evident. | tested | `audit.py` | `test_audit_chain.py`, `test_audit_chain_corruption.py`, tamper demo | Local JSONL is not WORM storage. | "Local audit events are hash-chained and tamper-evident." |
| Replay verification exists. | partial/tested | `replay.py`, `replay_store.py` | `test_replay.py` | Audit-only replay is policy-version-only without raw args; strong replay needs side-store. | "Replay helpers can re-derive decisions when raw call context is retained." |
| Ed25519 signing mode and static signing-key-ID revocation exist. | tested | `signing.py`, `receipt.py`, `revocation.py` | `test_receipt_signing.py`, `test_revocation.py`, package demo | Issuance signing and the operator-supplied `RevocationList` are opt-in; no PKI, custody, automatic distribution/rotation, per-receipt revocation, or global nonce service. | "Configured Ed25519 receipt signing and operator-supplied static signing-key-ID revocation are implemented for local trusted-key verification." |
| The secure profile (`require_signature=True`) is the default. | tested | `executor.py`, `contracts.py` (`require_signature` defaults `True`) | `test_profile.py`, `test_signing_default_doc_matches_code.py` | The default does not auto-sign: low-level issuance needs an explicit signer, and a governed gate with no configured trusted verifier fails closed (raises `ProductionProfileError`) before execution. Unsigned "dev mode" is an explicit gate opt-in (`require_signature=False`). | "The default gate profile requires a signature and trusted verifier; receipt issuance still needs an explicit signer. Without a verifier the governed path raises before execution, while unsigned 'dev mode' requires `require_signature=False`." |
| Receipt-v2 scoped trust purpose exists. | tested | `receipt.py`, `executor.py`, `contracts.py`, `packages/acgs-control-plane/src/acgs_control_plane/tenant_bootstrap.py` | `test_receipt_v2_trust_purpose_defaults_and_custom_scope_passthrough`, `test_receipt_v2_trust_purpose_threads_through_wrappers`, tenant-bootstrap integration tests | Purpose scoping selects trusted keys; it is not PKI, automatic key distribution, or a deployment certification. | "Receipt-v2 trusted-key resolution includes a purpose dimension: the default is `decision-receipt`, and tenant bootstrap uses `acgs.platform-bootstrap.receipt.v1`; wrong-purpose or unavailable trust material fails closed." |
| `DENY` and `ESCALATE` receipts are fully verified before non-executable refusal. | tested | `receipt.py`, `executor.py` | `test_signed_non_allow_receipts_report_late_binding_before_decision_reason`, `test_signed_non_allow_receipts_report_wrong_args_before_decision_reason`, `test_fully_bound_signed_non_allow_receipts_reject_before_side_effect` | This preserves provenance and diagnostics for signed non-ALLOW receipts; it does not make DENY or ESCALATE executable. Bindings are only as complete as the `expected_*` values supplied by the gate. | "DENY and ESCALATE receipts remain non-executable, but signed artifacts are checked for integrity, scoped trust, liveness, and supplied actor/action/argument/policy bindings before the final refusal." |
| Single-use receipts (opt-in one-time-use). | partial/tested | `consumption.py`, `executor.py` | `test_receipt_consumption.py`, `test_consumption_tamper.py` | Opt-in (off by default): `verify` itself stays stateless, so without a ledger a valid `ALLOW` receipt is reusable until `expires_at`. The ledger is hash-chained (tamper-evident), but tail truncation still needs an external high-water-mark and there is no global receipt/nonce revocation service. | "Single-use enforcement is available opt-in via a hash-chained consumption ledger that burns a receipt's audit anchor before execution; it is off by default and there is no global nonce service (see SECURITY_MODEL.md)." |
| MCP/function-call adapter shapes exist. | tested | `integration.py` | `test_integration_hook.py`, `test_integration_gaps.py`, `examples/mcp_tool_gate` | Shape parsing is not the same as a certified production adapter. | "The adapter normalizes common hook, MCP, and function-call payload shapes." |
| Proof pack generation exists. | tested | `cli.py` | `test_cli.py`, `gove-zone proofpack` | Local conformance evidence only. | "The CLI can generate a local proof pack with receipts, audit, verification, and limitations." |
| Gate position is framework-neutral (sits below the runtime). | implemented | `integration.py` (`_tool_name_and_input_from_payload` is documented runtime-neutral; hook/MCP/OpenAI/generic shapes), `executor.py` | `test_integration_hook.py`, `test_executor_guard.py` | Neutral at the *parse + executor boundary*; per-runtime named coverage varies by tier (see `INTEGRATION_MATRIX.md`). | "The gate evaluates side effects at the executor boundary, below whatever framework issued the call." |
| Decision Receipt fields map to EU AI Act Article 12 record-keeping duties (evidence toward, not conformance). | partial | `receipt.py`, `docs/EU_AI_ACT_MAPPING.md` | `tests/docs/test_eu_ai_act_mapping.py` | Candidate-stage field-level traceability only, with explicit GAPs (lifetime retention, biometric reference DB, end-of-use timestamp, natural-person authentication). Not a conformity assessment. | "Decision Receipt fields provide evidence toward EU AI Act Article 12 record-keeping; it is not a compliance certification." |
| Decision Receipt is a vendor-neutral evidence format. | implemented | `receipt.py` (`DecisionReceipt` fields carry no vendor-specific shape) | `test_decision_receipt.py`, `DECISION_RECEIPT_SPEC.md` | It is this project's own schema, not an adopted cross-vendor standard. | "The Decision Receipt is a vendor-neutral evidence format defined by this project." |
| Receipts are portable across multiple agent runtimes/hosts. | roadmap | none (planned) | none | ROADMAP: "standard receipt schema for agent runtimes / reference validators in multiple hosts." | "Cross-host portability validators are planned (roadmap)." |
| Per-runtime integration support is even-handed. | partial | `integration.py`, `a2a.py`, `examples/*` | `test_integration_hook.py`, `test_a2a_delegation.py`, `test_langgraph_node.py` plus shipped examples | Tiers differ: shipped (Python/hooks/MCP/OpenAI-shape/CI) vs shipped-qualified (`make_langgraph_tool_node` contract mirror; A2A adapter-mediated, contract-level) vs pattern (OpenAI-Agents/HTTP) vs roadmap (AutoGen/CrewAI/Anthropic-branded SDK). No installed-`langgraph` conformance claim. | "Runtimes are supported at documented tiers; see the integration matrix." |
| The integration matrix's "Shipped + tested" examples are statically proven to route through the gate (ADV9 wiring). | tested | `docs/INTEGRATION_MATRIX.md` (claim surface), `examples/*`, `packages/gove-zone/examples/runtime_hook_demo.py` | `test_gate_wiring_matrix.py` | AST-only check over the examples the matrix labels "Shipped + tested": each must import+call a gate entrypoint (`execute_with_receipt`/`GovernedExecutor`/`emit_receipt_for_hook`) and exercise a fail-closed `except` branch. It guards the named examples against out-of-gate drift (ADV9), not every possible integrator wiring. | "Each integration example we label 'shipped + tested' is CI-checked to route its side effect through the gate and to demonstrate a fail-closed denial." |
| Multi-step workflow ordering is enforced: a step whose approved predecessors have not executed does not run. | tested | `workflow.py` (`WorkflowDAG`, `WorkflowExecutor.execute_step` step 7 — predecessor satisfaction and predecessor-receipt-hash match, both before the atomic inner gate-and-execute at step 8) | `test_workflow_receipt_chain.py` (`test_reorder_predecessor_not_run_rejected_tool_not_called`, `test_predecessor_substitution_rejected_tool_not_called`, `test_happy_path_multistep_executes_in_order`), `packages/gove-zone/examples/workflow-receipt-chain` | Live enforcement reads `WorkflowExecutor.ledger`, which is trusted in-process runtime state, not durable evidence — a fresh executor starts empty. Steps invoked outside the workflow executor are not ordered at all. | "Within a governed workflow run, the executor rejects a step whose approved predecessors have not executed, before the side effect runs." |
| The approved step graph is content-addressed, and that hash is bound into the signed workflow objects. | tested | `workflow.py` (`WorkflowDAG.dag_hash` hashes each step's action plus its sorted predecessor set; `dag_hash` and `predecessor_receipt_hashes` are inside `WorkflowStepReceipt._hash_payload`), `plan.py` (`dag_hash` inside `WorkflowAuthorization._hash_payload`) | `test_dag_altered_rejected_tool_not_called`, `test_step_hash_binds_inner_receipt`, `test_dag_hash_is_order_independent_in_predecessors`, `test_authorization_hash_changes_with_dag` | Hash binding alone is recomputable under host compromise. Workflow signing is opt-in and its default differs from the single-action gate: `WorkflowExecutor.require_signature` defaults to `False`. Workflow ids are integrator-supplied opaque strings the kernel does not allocate or register. | "The approved step graph is content-addressed, and that hash is bound into the workflow authorization and into every step receipt, so an altered plan invalidates the evidence." |
| A workflow chain can be re-verified offline, including its ordering. | partial/tested | `workflow.py` (`verify_workflow_replay` — topological drain re-checking each step's recorded predecessor hashes, plus the cross-level separation sets) | `test_replay_passes_on_good_chain`, `test_replay_fails_on_predecessor_hash_mismatch`, `test_replay_rejects_cross_level_collusion`, `test_replay_signed_chain_verifies` | `verify_workflow_replay` passes `require_signature=verifier is not None`, so an unsigned chain verifies when no verifier is supplied. The runner anchor (`governed.expected_actor`) is executor configuration and is not carried in the chain, so replay cannot re-check the runner's own role membership. Replay is a separate call an operator must actually run; it is not part of the proof pack. | "A workflow chain can be re-verified offline — ordering, predecessor hashes, and cross-level role separation — without re-running the workflow; signature checking requires supplying a verifier." |
| Plan-level governance: proposer/validator separation is enforced across the plan and step levels. | tested | `plan.py` (`WorkflowAuthorization`, required — there is no ungoverned workflow path), `workflow.py` (`_verify_authorization` check E; the `proposers` / `validators` sets, committed only on step success) | `test_plan_level_governance.py` (`test_cross_level_collusion_plan_validator_is_step_proposer`, `test_cross_level_collusion_step_validator_is_plan_proposer`, `test_rejected_step_does_not_pollute_cross_level_state`, `test_runner_is_plan_validator_rejected_tool_not_called`, `test_missing_authorization_cannot_construct_executor`), `packages/gove-zone/examples/plan-level-governance` | Separation is over opaque identity strings, as elsewhere in this ledger: two identities controlled by one person are two principals to the kernel. No built-in IAM. | "A governed workflow requires a plan authorization whose proposer and validator are distinct, and the executor rejects any step that would place one principal on both sides across the plan and step levels." |
| ACGS is production-certified. | not claimed | none | none | No certification evidence. | "Not production-certified." |
| ACGS is compliance-certified. | not claimed | none | none | No compliance audit/certificate. | "Not compliance-certified." |
| ACGS is regulator-approved. | not claimed | none | none | No regulator approval. | "Not regulator-approved." |
| ACGS replaces content moderation. | not claimed | none | none | It governs side effects, not all content harms. | "Complements content moderation; does not replace it." |
| ACGS replaces sandboxing. | not claimed | none | none | It authorizes execution; sandboxing contains execution. | "Complements sandboxing; does not replace it." |
| Enterprise identity integration (OAuth / SSO / service accounts / workload identity / RBAC mapping) is supported at the adapter-contract level. | tested | `identity.py` (`IdentityProviderAdapter`, `RBACMapper`, `govern_identity_action`) | `test_identity.py` (incl. full chain through `execute_with_receipt`) | Bundled providers are in-memory mocks (`mock-azure-ad` / `mock-okta` / `mock-google-workspace`): no real OAuth/OIDC/SAML protocol exchange, no token cryptography, no JWKS/SCIM. Production adapters are integrator-built against the same contract. | "Ships a fail-closed identity-adapter contract, RBAC-to-authority mapping, and mock IdPs for integration development; production identity providers remain integrator-owned." |
| ACGS is complete IAM/RBAC/PKI. | not claimed | none | none | Identity/key lifecycle is integrator/operator-owned. | "Requires external identity and key-management systems for production." |
| ACGS is full formal verification. | not claimed | none | none | Tests and deterministic checks exist; no formal proof suite. | "Tested local invariant evidence, not full formal verification." |
| Control-plane agents and policy bundles attach to project/environment scope. | partial/tested | `packages/acgs-control-plane/src/acgs_control_plane/migrations/versions/0010_scope_attachment.py`, `scope_defaults.py`, `models.py` | `packages/acgs-control-plane/tests/test_scope_attachment.py`, full control-plane package evidence from PR #369 commit `e4898d7fe250c519f623647cbf7951f7c1f076d4` | Current-local migration and constraint evidence only. It backfills and enforces default project/environment attachment for existing agents and policy bundles, but does not prove complete tenant isolation, RLS/search_path hardening, provider wiring, or production deployment. | "The current control plane has local project/environment attachment evidence for agents and policy bundles; production tenant isolation remains roadmap." |
| Control-plane agent creation has one canonical native receipt path with durable terminal idempotency replay. | partial/tested | `packages/acgs-control-plane/src/acgs_control_plane/app.py`, `idempotency.py`, `native_receipts.py`, `exports.py`, `schemas.py`, `migrations/versions/0011_managed_idempotency_results.py` | `packages/acgs-control-plane/tests/test_native_agent_transaction_route.py`, `test_agent_create_idempotency.py`, `test_native_receipt_ledger.py`, `test_exports.py`, `test_receipts_dashboard.py`, `test_v1_api_contract.py`; PR #370 commit `feaabd96ccb68a076f39cc46fe5a7d906e0a9a5f`; PR #371 commit `e0f514f2963987f72827d33ada891abc08677f03` | Only `POST /orgs/{org_id}/agents` and its `/v1` alias use the signed native DecisionReceipt transaction plus durable idempotency path. The route requires exactly one bounded `Idempotency-Key`, stores only key/request/response digests plus signed terminal result evidence, and replays `ALLOW`, `DENY`, or `ESCALATE` outcomes from authoritative rows. PostgreSQL serializes through a tenant row lock; SQLite coverage is same-process only. Twelve legacy unsigned route aliases remain; production startup still blocks them. Provider injection is explicit, but operator environment/provider wiring is not complete. SQL rollback covers DB mutation, native receipt/event/head/outbox, consumption state, and terminal idempotency row; it is not external exactly-once delivery. Native project/environment scope is bound through the governance event path, not direct receipt columns. Export verification requires trusted public keys supplied out of band. No other mutation coverage, async export/recovery/rolling-upgrade claim, or production-readiness claim is made. | "The agent-create route has local native DecisionReceipt and durable terminal idempotency evidence for signed issuance, SQL-transaction consumption, digest-only replay storage, and export/verify surfaces; it is not a full control-plane cutover, external exactly-once guarantee, or production service." |

## Public wording rule

If a claim is not in this table, add it here before using it in public docs. If evidence is partial, use partial wording. If evidence is roadmap-only, say planned.
