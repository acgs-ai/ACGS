# Claim ledger

Purpose: prevent marketing drift. Every public claim should be traceable to code, tests, demos, receipt evidence, replay verification, or roadmap.

| Claim | Status | Evidence source | Test or demo | Limitation | Safe public wording |
|---|---|---|---|---|---|
| ACGS / gove-zone is receipt-gated governance for AI-agent side effects. | implemented | `packages/gove-zone/src/gove_zone/*` | `gove-zone smoke`, receipt-gated demo | Local kernel, not a managed production service. | "Local receipt-gated governance layer for AI-agent side effects." |
| No valid Decision Receipt, no side effect. | tested | `executor.py`, `receipt.py` | `test_executor_guard.py`, `examples/tamper_demo`, `proofpack` | Only true for paths wired through the governed executor. | "The governed executor fails closed without a valid receipt." |
| Policy is evaluated before execution. | tested | `kernel.py` | `test_fail_closed.py`, smoke demo | Direct raw-tool calls can bypass if integrator exposes them. | "Kernel dispatch evaluates policy before the registered tool runs." |
| Denied action leaves evidence and does not run. | tested | `kernel.py`, `audit.py` | smoke demo, `test_fail_closed.py` | Local JSONL only. | "Denied local actions are audited and blocked before side effects." |
| Missing receipt is blocked. | tested | `executor.py` | `test_executor_refuses_no_receipt`, `proofpack` | Requires executor gate integration. | "Gated execution rejects missing receipts." |
| Tampered receipt is blocked. | tested | `receipt.py` | `test_decision_receipt.py`, `examples/tamper_demo` | Unsigned hashes are recomputable under host compromise. | "Receipt field tampering is hash-detected; signed mode closes recomputed-forgery when engaged." |
| Actor/action/argument binding exists. | tested | `receipt.py`, `executor.py` | `test_argument_binding.py`, `test_executor_guard.py` | Actor identity comes from integrator runtime context. | "Receipts bind the actor, action, and exact arguments checked by the executor." |
| Tenant and policy binding exist. | tested | `tenant.py`, `receipt.py`, `authorization.py` (`PolicyArtifactAttestation`), `policy.py` (`PolicyArtifactSnapshot`) | `test_tenant_safety.py`, `test_policy_bundle_io.py`, `test_authorization_contracts.py`, `test_policy_artifact_snapshot.py` | Identity authentication and policy distribution remain operator-owned. | "Strict receipts bind tenant and an attested policy artifact digest." |
| Expiry and receipt revocation are supported. | tested | `receipt.py`, `consumption.py` | `test_receipt_expiry.py`, consumption-store tests | Receipt revocation is enforced by the configured strict consumption store, not a hosted global service. | "Strict managed execution rejects expired or persistently revoked receipts." |
| Audit evidence is tamper-evident. | tested | `audit.py` | `test_audit_chain.py`, `test_audit_chain_corruption.py`, tamper demo | Local JSONL is not WORM storage. | "Local audit events are hash-chained and tamper-evident." |
| Replay verification exists. | partial/tested | `replay.py`, `replay_store.py` | `test_replay.py`, `test_replay_bundle_equivalence.py` | Audit-only replay is policy-version-only without raw args; strong replay needs side-store. | "Replay helpers can re-derive decisions when raw call context is retained." |
| Strict replay requires an externally checkpointed chain and verified lifecycle attestations. | tested | `replay.py` (`replay_bundle`, `replay_checkpointed_bundle`), `audit.py` (`AuditCheckpoint`), `signing.py` (`LifecycleVerifierRegistry`) | `test_replay.py`, `test_replay_bundle_equivalence.py`, `test_lifecycle_verifier_registry.py` | A bare JSONL chain detects in-chain edits, reorders, and malformed tails, but a trusted full rewrite or truncation is only detected against an external signed checkpoint. Lifecycle records without a verifiable attestation fail closed; historical records predating `record_kind` are treated as policy decisions and remain policy-compatible only. | "Strict replay verifies against a signed external audit checkpoint and a lifecycle verifier registry; bare-chain replay detects in-chain integrity but not trusted rewrite or truncation." |
| Ed25519 signing mode exists. | tested | `signing.py`, `receipt.py` | `test_receipt_signing.py`, package demo | Opt-in; no managed PKI, key custody, rotation, or signing-key revocation service. Receipt revocation is a separate strict-consumption-store control. | "Opt-in Ed25519 receipt signing is implemented for local trusted-key verification." |
| Strict managed verification requires signed receipts. | tested | `authorization.py`, `executor.py`, `managed_execution.py` | signing and managed-execution tests | Low-level legacy/dev APIs can still be configured for unsigned local proofs. | "The strict managed side-effect path verifies a trusted signature; unsigned legacy mode is not a production claim." |
| Strict standalone and strict managed execution prevent receipt replay and bind idempotency. | tested | `executor.py` (`execute_with_receipt`, `GovernedExecutor`), `side_effect_kernel.py`, `consumption.py`, `managed_execution.py` | consumption, idempotency, restart, and concurrency tests | Only when *configured* with all of: a trusted signature verifier (`require_signature=True`), an externally checkpointed audit store, an anchored schema-v4 consumption store, and an explicit lifecycle signer. Legacy/evaluate-only APIs and raw `DecisionReceipt.verify` / `ReceiptVerifier` verification supply **none** of this. Durability is bounded by the configured store. | "When configured with signed receipts, checkpointed audit, an anchored consumption store, and a lifecycle signer, the strict standalone and managed paths bind persistent single-use consumption and idempotency; receipt-only verification does not." |
| Consumption-store schema v4 persists receipt-revocation, idempotency, and terminal execution state. | tested | `consumption.py` (`_SCHEMA_VERSION = 4`, `ConsumptionState`) | consumption migration, restart, and idempotency tests | This is the consumption-store schema, **not** a new Decision Receipt schema, not an audit `record_kind` field, and not a signing-key revocation service. Nonces and idempotency keys are stored only as tenant-scoped HMAC-SHA-256 digests; raw nonces, raw keys, receipt bodies, and arguments are never stored. | "Consumption-store schema v4 persists tenant-scoped HMAC digests for replay, receipt-revocation, idempotency, and terminal (`SUCCEEDED`/`UNKNOWN`) state." |
| Audit records authenticate their kind (three kinds), and execution-lifecycle records carry an Ed25519 lifecycle attestation. | tested | `decision.py` (`RecordKind` = `POLICY_DECISION`/`EXECUTION_LIFECYCLE`/`EXECUTION_REFUSAL`, `DecisionRecord.lifecycle`), `signing.py` (`LifecycleAttestation`, `LifecycleVerifierRegistry`), `authorization.py` (`ExecutionRefusalEvidence`), `executor.py` (`_append_execution_audit`) | `test_lifecycle_verifier_registry.py`, replay-bundle and executor tests | `record_kind` is an authenticated audit-record field, not a Decision Receipt or consumption-schema field. `EXECUTION_REFUSAL` proves a bound attempt was refused *before any adapter ran* (`adapter_invoked=False`) and must never represent `OUTCOME_UNKNOWN`. The lifecycle signing key and authority id must be code-separated from the audit-checkpoint key and authority; unsigned lifecycle attestations are refused. Key custody and PKI remain operator-owned. | "Audit records distinguish policy decisions, execution-lifecycle records, and pre-adapter execution refusals by an authenticated `record_kind`; lifecycle records require an Ed25519 attestation from an authority separate from the audit-checkpoint authority." |
| Strict execution makes a durable atomic claim and at most one adapter attempt. | tested | `executor.py` (`reserve` → claim → `run_if_committed`), `side_effect_kernel.py` (`AdapterOutcomeStatus`), `consumption.py` (`ConsumptionState`) | executor, side-effect-kernel, and consumption tests | On an ambiguous or exception outcome the executor attempts to persist and confirm terminal `UNKNOWN`. When that state is confirmed, the receipt is **not** retryable and later reuse is denied. If persistence, readback, or evidence append fails, the path fail-stops and no retry is authorized, but the terminal state and its lifecycle evidence may be unavailable. This is **not** exactly-once effect: the downstream adapter may already have acted before the outcome became ambiguous. Operators must reconcile `UNKNOWN` out of band. | "Strict execution reserves a durable claim and makes at most one adapter attempt; on an ambiguous outcome the executor attempts to persist and confirm terminal `UNKNOWN`, which blocks receipt retry once confirmed, and otherwise fail-stops without authorizing a retry. Exactly-once *effect* is not claimed — the downstream side effect may already have occurred." |
| Fixed stdio, remote Streamable HTTP, and container-isolated MCP reference topologies exist. | partial/tested | `mcp_stdio_transport.py`, `mcp_http_transport.py`, `mcp_security.py`, `mcp_reference.py` | `test_mcp_http_transport.py`, `test_mcp_reference_topology.py` | Fixture/reference only — not production deployment or production-topology evidence. Downstream targets are fixed/pinned with no direct fallback path and no agent-supplied token passthrough. | "Fixture-only MCP reference topologies pin a fixed stdio target, a no-redirect Streamable HTTP origin, and a container-isolated gateway, with no direct fallback and no credential passthrough." |
| The P1 remote listener (`gove-zone mcp serve-http --remote`) terminates TLS directly, validates an exact Host/Origin, disables proxy-header trust, and enforces bounded concurrency/request/body/header/graceful-shutdown budgets before any MCP dispatch. | tested | `mcp_runtime.py` (`RemoteMCPConfig`, `_RemoteGuard`, `RemoteMCPBudgets`, `build_remote_uvicorn_config`) | `test_mcp_runtime_e2e.py` remote-guard and startup-rejection cases (part of the file's 111 passed; command: `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_mcp_runtime_e2e.py --import-mode=importlib -q`) | Reference/local listener only. TLS terminates in this process, not behind an operator load balancer or service mesh; an absent Origin or a non-loopback bind is refused unless the asymmetric JWS verifier is configured. | "The P1 remote listener terminates TLS in-process, rejects a request with an unmatched Host or Origin, trusts no `Forwarded`/`X-Forwarded-*` header, and fails closed under its configured resource budgets." |
| Remote-mode workload identity is authenticated by a pinned Ed25519/EdDSA compact-JWS verifier binding an exact issuer, audience, resource, authority, tenant, client, user, session, scope, and time window; there is no `none`/HMAC path and no header-carried or remotely fetched key. | tested | `mcp_identity.py` (`EdDSAJWSVerifier`, `Ed25519TrustSnapshot`, `MCPTokenClaims`) | `test_mcp_identity_jws.py` (algorithm-confusion, header-key-material, and claim-schema cases; canonical base64url/padding subset: 4 passed — command: `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_mcp_identity_jws.py -k "padded_header or padded_payload or padded_signature or standard_base64" --import-mode=importlib -q`) | This is a local/reference trust profile: a fixed, in-process `kid -> public key` snapshot, not a managed PKI, key-rotation service, or full enterprise IAM. | "Remote-mode identity is a pinned Ed25519 compact-JWS verifier with exact claim binding and no algorithm negotiation — a reference trust profile, not a managed IAM/PKI system." |
| The readiness/health MCP identity is signed for `mcp.tools.list` (catalog-only) authority and cannot execute `tools/call`; a normal actor identity carries `mcp.tools.call` authority, and the call-authority check runs before policy, kernel, or adapter dispatch. | tested | `mcp_reference.py` (`_health_claims`, `HEALTH_EXPECTED_TOOLS`, `FIXTURE_CATALOG_SCOPE`), `mcp_gateway.py` (`MCP_TOOLS_LIST_AUTHORITY`, `MCP_TOOLS_CALL_AUTHORITY`) | `test_mcp_gateway_adversarial.py` list-authority-vs-call-authority subset: 9 passed (command: `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_mcp_gateway_adversarial.py -k "authority and not credential_authority_drift" --import-mode=importlib -q`) | The health identity may still see `fixture.read`'s catalog metadata under the `fixture:catalog` scope; that is list-only visibility, not call authorization. Enforcement is this gateway's own; a raw tool exposed outside the gateway remains a bypass. | "A readiness credential can list the catalog but never call a tool; a caller identity's `mcp.tools.call` authority is checked before policy, kernel, or adapter dispatch." |
| The actual `gove-zone mcp serve-http --remote` CLI entrypoint is exercised end-to-end, over a real TLS socket, by an official Streamable HTTP MCP client, against the same fixed stdio fixture downstream used by the local stdio/loopback-HTTP paths. | tested | `cli.py` (`_mcp_serve_http_remote`), `mcp_reference.py` (`create_reference_runtime`) | `test_mcp_runtime_e2e.py::test_actual_cli_remote_governs_the_streamable_http_tool_and_exits_cleanly`: 1 passed (command: `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_mcp_runtime_e2e.py -k test_actual_cli_remote_governs_the_streamable_http_tool_and_exits_cleanly --import-mode=importlib -q`) | Exercises `initialize`, `tools/list`, one allowed `tools/call` with a receipt and a single ledger effect, one denied call, and a clean SIGTERM shutdown. It is the fixed-stdio-downstream path, not the container-isolated Docker topology below. | "The actual remote-mode CLI subprocess is driven end-to-end over real TLS by an official MCP client, including an allowed call, a denied call, and a clean shutdown." |
| A container-isolated Docker reference topology (`gateway_remote.py`, `compose.remote.yaml`) runs the remote TLS gateway against a fixed HTTP downstream on an internal Docker network, and its demo script asserts token isolation, a health-credential attack, and a gateway-down case with no direct fallback. | partial/tested | `packages/gove-zone/examples/mcp-tool-gateway/reference-topology/gateway_remote.py`, `compose.remote.yaml`, `probe_remote.py`, `run-remote-demo.sh` | `test_mcp_reference_topology.py::test_live_container_remote_topology` (skips when no Docker daemon is reachable); `run-remote-demo.sh` asserts every result itself (`FAILURES` counter) and its pytest wrapper requires at least 22 `ok:` lines and zero `FAIL:` lines. Most recently captured local run: Docker topology 1 passed, remote demo 31 `ok:` / 0 `FAIL:`; not re-executed in this documentation pass because it builds and runs Docker containers. | Docker-dependent and fixture-only: ephemeral demo CA, JWS authority, and downstream credential; not a production network topology. Gateway unavailability does not fall back — it is refused, not degraded. | "A container-isolated reference topology proves token isolation, health-credential list-only behavior, and no direct-downstream fallback, when a Docker daemon is available; it is a local reference fixture, not a production deployment." |
| Policy artifacts are attested and revalidated before execution. | tested | `authorization.py` (`PolicyArtifactAttestation`), `policy.py` (`PolicyArtifactSnapshot`), `managed_execution.py` | `test_authorization_contracts.py`, `test_policy_artifact_snapshot.py`, `test_side_effect_kernel.py` | Artifact distribution and signer PKI remain operator-owned. | "Strict authorization binds a `PolicyArtifactAttestation` and revalidates it at the final adapter boundary." |
| Release Gate, MCP Gateway, and Spend Guard share one managed authorization kernel. | tested | `release_gate.py`, `mcp_gateway.py`, `spend_guard.py`, `managed_execution.py` | product, strict-kernel, and disaster-PoC suites | These are local reference products and fixtures, not hosted production services. | "P0, P1, and P2 reuse the same strict managed receipt-gated execution path." |
| A Python-only E2B sandbox adapter exists. | partial/tested | `sandbox.py` (`E2BSandbox`) | `test_sandbox.py` | The repository does not provide the E2B SDK, API key, remote service, or production execution proof. Node and worktree execution modes are not `SandboxProvider` implementations. | "A Python-only E2B adapter shape is tested with fixtures; live E2B operation is not claimed." |
| The local sandbox exposes a bwrap option that currently fails closed. | partial/tested | `sandbox.py` (`LocalProcessSandbox`) | `test_local_process_sandbox_bwrap_fd_path_fails_closed` | Anonymous response-FD transport is incompatible with the current bwrap option; no working bwrap profile is shipped. | "The current bwrap option fails closed rather than claiming working containment." |
| MCP/function-call adapter shapes exist. | tested | `integration.py` | `test_integration_hook.py`, `test_integration_gaps.py`, `examples/mcp_tool_gate` | Shape parsing is not the same as a certified production adapter. | "The adapter normalizes common hook, MCP, and function-call payload shapes." |
| Proof pack generation exists. | tested | `cli.py` | `test_cli.py`, `gove-zone proofpack` | Local conformance evidence only. | "The CLI can generate a local proof pack with receipts, audit, verification, and limitations." |
| The generic proof-pack codec is structural only; product proof packs add semantic verification. | tested | `proof_pack.py` (generic codec), `release_proof.py`, `mcp_proof.py`, `spend_proof.py` | `test_proof_pack.py`, `test_release_proof_pack.py`, `test_mcp_proof_pack.py`, `test_spend_proof_pack.py` | `proof_pack.py` deliberately knows nothing about release, MCP, receipt, policy, audit, replay, or consumption semantics — it establishes canonical bytes, exact membership, content hashes, and stable path identity. Semantic verify/replay is the caller's, performed by the Release/MCP/Spend product layers. Structural sealing alone is **not** evidence that a decision was governed. Product semantic verification is itself **relative to** a caller-supplied expected digest and to external receipt, checkpoint, lifecycle, and consumption trust inputs — it does not bootstrap trust from the pack, and pack-embedded or self-asserted trust material is not independent proof. | "The shared proof-pack codec provides structural sealing and integrity only; Release, MCP, and Spend product packs perform the domain verify/replay on top of it, relative to a caller-supplied expected digest and external trust inputs." |
| Three deterministic disaster PoCs publish re-verifiable evidence. | tested | `disaster_pocs.py`, product proof exporters | `test_disaster_pocs.py`, `test_cli_disaster_pocs.py` | Local fixtures only; no deployment, remote MCP call, or payment. Verification is **relative to** a caller-supplied expected digest and to external receipt, checkpoint, lifecycle, and consumption trust inputs; pack-embedded or self-asserted trust material is not independent proof. Avoid unqualified "independently verifiable". | "Each local disaster fixture publishes relative proof/envelope paths plus native verify and replay commands, re-verifiable against a caller-supplied expected digest and external trust inputs." |
| Gate position is framework-neutral (sits below the runtime). | implemented | `integration.py` (`_tool_name_and_input_from_payload` is documented runtime-neutral; hook/MCP/OpenAI/generic shapes), `executor.py` | `test_integration_hook.py`, `test_executor_guard.py` | Neutral at the *parse + executor boundary*; per-runtime named coverage varies by tier (see `INTEGRATION_MATRIX.md`). | "The gate evaluates side effects at the executor boundary, below whatever framework issued the call." |
| Decision Receipt is a vendor-neutral evidence format. | implemented | `receipt.py` (`DecisionReceipt` fields carry no vendor-specific shape) | `test_decision_receipt.py`, `DECISION_RECEIPT_SPEC.md` | It is this project's own schema, not an adopted cross-vendor standard. | "The Decision Receipt is a vendor-neutral evidence format defined by this project." |
| Receipt-bound action tiering (explore/commit) exists. | tested | `decision.py` (`ActionTier`), `tier.py` (`ToolTierRegistry`), `policy.py` (rule `tiers` criterion), `receipt.py` (hash-bound field) | `test_action_tiering.py` | Registry is manual/declarative in v1 — NOT semantic detection of side effects; declared tier is untrusted and defaults to the strict `commit` tier. | "Receipt-bound explore/commit action tiering routes policy strictness; the declared tier is untrusted and a tool registry is authoritative, defaulting to the strict commit tier." |
| Receipts are portable across multiple agent runtimes/hosts. | roadmap | none (planned) | none | ROADMAP: "standard receipt schema for agent runtimes / reference validators in multiple hosts." | "Cross-host portability validators are planned (roadmap)." |
| Per-runtime integration support is even-handed. | partial | `integration.py`, `examples/*` | `test_integration_hook.py` plus shipped examples | Tiers differ: shipped (Python/hooks/MCP/OpenAI-shape/CI) vs pattern (LangGraph/OpenAI-Agents/HTTP) vs roadmap (AutoGen/CrewAI/Anthropic-branded SDK). | "Runtimes are supported at documented tiers; see the integration matrix." |
| ACGS is production-certified. | not claimed | none | none | No certification evidence. | "Not production-certified." |
| ACGS is compliance-certified. | not claimed | none | none | No compliance audit/certificate. | "Not compliance-certified." |
| ACGS is regulator-approved. | not claimed | none | none | No regulator approval. | "Not regulator-approved." |
| ACGS replaces content moderation. | not claimed | none | none | It governs side effects, not all content harms. | "Complements content moderation; does not replace it." |
| ACGS replaces sandboxing. | not claimed | none | none | It authorizes execution; sandboxing contains execution. | "Complements sandboxing; does not replace it." |
| ACGS is complete IAM/RBAC/PKI. | not claimed | none | none | Identity/key lifecycle is integrator/operator-owned. | "Requires external identity and key-management systems for production." |
| ACGS is full formal verification. | not claimed | none | none | Tests and deterministic checks exist; no formal proof suite. | "Tested local invariant evidence, not full formal verification." |
| ACGS delivers exactly-once side effects. | not claimed | `executor.py`, `side_effect_kernel.py`, `consumption.py` (terminal `UNKNOWN`) | executor and side-effect-kernel tests | The kernel bounds *authorization* to at most one adapter attempt and denies later reuse once terminal `UNKNOWN` is confirmed; where that state cannot be confirmed it fail-stops rather than authorizing a retry. It cannot know whether an ambiguous downstream call took effect. | "At-most-once authorized attempt with fail-closed `UNKNOWN`, not exactly-once effect; operators must reconcile." |

## Public wording rule

If a claim is not in this table, add it here before using it in public docs. If evidence is partial, use partial wording. If evidence is roadmap-only, say planned.
