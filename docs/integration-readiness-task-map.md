# Integration readiness task map — 2026-05-24

Scope: current checkout at `/home/martin/finished work/govern-zone`, branch
`feat/acgs-conductor-adapter-spike`.

This file records the discovered gap map for the current completion pass. It
separates local integration/deployment readiness (can be verified in this
checkout) from external-production work that requires credentials, live domains,
legal review, or third-party services.

## Completed in this pass

| Area | Gap found | Resolution | Evidence |
|---|---|---|---|
| Root onboarding | No root `README.md` for new developer/agent setup | Added root README with prerequisites, install/verify/build commands, package surfaces, runtime smoke path, deployment caveat, and known limitations | `README.md`; `make verify`; `make build` |
| Governance stack index | Roadmap called for a single policy/evidence contract and gate map across packages | Added `docs/governance-stack-index.md` plus `scripts/check_governance_stack_index.py` to lock required package rows, fail-closed/hash-chain/source-tagged/claim-safe concepts, and overclaim guardrails; wired the check into root lint fan-out as `make lint-docs` | `docs/governance-stack-index.md`; `python3 scripts/check_governance_stack_index.py`; `make lint-docs` |
| Platform readiness proof surface | The broad platform/deploy goal had many package gates but no single root audit separating local evidence from external production blockers | Added `scripts/platform_readiness_report.py`, `tests/test_platform_readiness_report.py`, and `make platform-readiness` to summarize local evidence spine, bus contract regeneration, deploy contracts, claim safety, buyer proof journey, external blockers, and the pending Storybook buyer-evidence artifact | `make platform-readiness`; `uv run python -m pytest tests/test_platform_readiness_report.py --import-mode=importlib -q` |
| Release evidence bundle | Local readiness reports existed separately from buyer artifacts, verification commands, and external blocker handoff state | Added `scripts/build_release_evidence.py`, `tests/test_release_evidence_bundle.py`, and `make release-evidence` to write a local release-readiness evidence bundle with conservative claim boundaries | `make release-evidence`; `uv run python -m pytest tests/test_release_evidence_bundle.py --import-mode=importlib -q` |
| Legal/adjacent checkout registry truth | The governance index and registry needed to avoid treating ignored legal-domain checkouts as root-tracked packages | Corrected the LegalGuard row to the current ignored adjacent `ACGS/packages/legalguard/` checkout, documented that adjacent legal-domain gates are package-local rather than root CI proof, and replaced the enterprise admin adjunct discovery-only gate with its build command | `MONOREPO.md`; `docs/governance-stack-index.md`; `make lint-docs`; `pnpm -F acgs-enterprise-manager-frontend build` |
| Runtime hook adapter | `gove-zone` had CLI replay but no canonical runtime hook adapter/setup/doctor/gate path documented as current | Added `gove_zone.integration`, `gove_zone.setup`, CLI `setup`, `doctor`, `gate`, and `enable` commands | `packages/gove-zone/tests/test_integration_hook.py`; `packages/gove-zone/tests/test_setup.py`; runtime smoke command |
| Runtime framework bridge | The hook adapter documented Claude/Codex/MCP-style hosts but only locked the Claude-style `{tool_name, tool_input}` shape in tests | Added public `tool_call_from_hook_payload` normalization for Claude/Codex-style, MCP-style `tools/call`, function-call-style, OpenAI Chat `tool_calls`, LangChain-style `tool_calls`, and generic bridge payloads without adding third-party dependencies | `packages/gove-zone/src/gove_zone/integration.py`; `packages/gove-zone/tests/test_integration_hook.py`; `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_integration_hook.py --import-mode=importlib -q` |
| Runtime policy gate | Agent-framework hooks could emit receipts but could not load a reviewed `RuleSetPolicy` bundle and block denied/escalated bridge payloads from the `gove-zone gate` CLI | Added `gove-zone gate --policy-bundle` with fail-closed bundle loading, receipt emission, `blocked` JSON output, and non-zero exit for deny/escalate decisions before side effects | `packages/gove-zone/src/gove_zone/cli.py`; `packages/gove-zone/tests/test_setup.py`; `uv run --package gove-zone python -m pytest packages/gove-zone/tests/test_setup.py --import-mode=importlib -q` |
| Hook wiring proof | Handler code can exist without being registered | Wired `.claude/settings.json` to `.claude/hooks/acgs-emit-receipt.py` for `Edit|Write|MultiEdit` and selected `Bash` workflows; added wiring regression test | `packages/gove-zone/tests/test_integration_gaps.py` |
| Fail-closed control | Gate mode needed a project-level, agent-followable switch | Added `.gove-zone/gate.mode` resolution and `gove-zone enable --enforce/--observe` | `test_gate_mode_file_fallback`; `test_cli_enable_writes_gate_mode_file` |
| Agent setup ergonomics | No bounded setup/verify/dev/cleanup hooks for Vibe worktrees | Added `scripts/vibe-kanban-*.sh`, safe profile examples, and runbook | `docs/vibe-kanban-govern-zone.md`; `bash scripts/vibe-kanban-verify.sh` |
| Type strictness in touched runtime | `gove-zone` strict mypy failed when checked from package cwd due tests/examples annotations | Added missing annotations | `(cd packages/gove-zone && uv run mypy .)` |
| Docs truth | `packages/gove-zone/README.md` still claimed Phase 1 only | Updated README to current MVP surface and limits | `packages/gove-zone/README.md` |
| Policy bundle fixture evaluation | `gove-zone` roadmap calls for AgentDojo/InjecAgent-style policy-bundle evaluation before market-facing safety claims | Added `gove_zone.evaluation`, `gove-zone eval`, local scenario fixture metrics, and tests for expected decision mismatches | `packages/gove-zone/tests/test_evaluation_adapter.py`; `gove-zone eval --bundle ... --scenarios ...` |
| Benchmark fixture/result adapters | Local evaluation still required hand-shaped generic JSON before AgentDojo/InjecAgent-inspired scenarios could become repeatable evidence | Added `gove_zone.benchmark_adapters`, `gove-zone eval --benchmark-format agentdojo|injecagent`, and eval-MVP `governance.benchmarks.agentdojo_adapter` / `injecagent_adapter` result mappers with source-aware claim-evidence risk tags | `packages/gove-zone/tests/test_benchmark_adapters.py`; `acgs_governance_eval_mvp/tests/test_benchmark_adapters.py` |
| Source-aware evaluation evidence cards | Console cards typed all benchmark evidence as `gove-zone`, so AgentDojo/InjecAgent adapter output could arrive without operator-visible source framing | Added `EvaluationEvidenceSource`, source-label copy for local AgentDojo/InjecAgent/ToolEmu-style adapters, claim-safe caveat text, source-aware fixtures, and a static MVP gate for this contract | `acgi-ai/src/routes/console/Audit.tsx`; `acgi-ai/src/api/types.ts`; `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `pnpm -F acgi-ai run test:mvp` |
| ToolEmu fixture/result adapters | ToolEmu was console-framed as a future source but runtime/eval adapters only covered AgentDojo and InjecAgent-style fixtures | Added `gove-zone eval --benchmark-format toolemu`, high-stakes `test_cases[].tool_invocations` replay, eval-MVP `governance.benchmarks.toolemu_adapter`, source-preserving risk tags, and docs that keep upstream-suite execution separate from local evidence | `packages/gove-zone/tests/test_benchmark_adapters.py`; `acgs_governance_eval_mvp/tests/test_benchmark_adapters.py` |
| ToolEmu console evidence card | Runtime/eval ToolEmu evidence could be generated locally, but the console fixture path still only demonstrated AgentDojo/InjecAgent cards | Added a `toolemu-high-stakes-mini` evaluation evidence row, high-stakes ToolEmu source copy, and an MVP static invariant requiring the source/type/fixture contract | `acgi-ai/src/mocks/data/audit.ts`; `acgi-ai/src/routes/console/Audit.tsx`; `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `pnpm -F acgi-ai run test:mvp` |
| Evaluation report evidence ingestion | Evaluation metrics were generated locally but not hash-addressed in the eval MVP audit/evidence surface | Added `governance.evaluation`, `python -m governance.cli.ingest_evaluation_report`, and `/evidence/evaluation-report` to normalize `gove-zone eval` JSON, compute a report hash, append chain-hashed evidence, and mark failed reports as not claim-safe | `acgs_governance_eval_mvp/tests/test_evaluation_report_ingest.py`; `acgs_governance_eval_mvp/tests/test_api_auth.py` |
| Evaluation report evidence query | Console-visible evaluation evidence was fixture-backed after ingestion; operators still needed a live eval-MVP read surface for normalized report hashes and claim-safe status | Added `list_gove_zone_evaluation_evidence` and `/evidence/evaluation-reports` to read tenant-scoped, status-filterable, event-hash-addressed evaluation evidence from the audit chain | `acgs_governance_eval_mvp/tests/test_evaluation_report_ingest.py`; `acgs_governance_eval_mvp/tests/test_api_auth.py`; `GET /evidence/evaluation-reports?status=passed` |
| Console live evaluation evidence gateway | The audit route surfaced fixture-shaped eval evidence but did not query the live eval-MVP read surface through the console API boundary | Added snake_case-to-camelCase `EvaluationEvidenceApi` normalization, same-origin `/api/v1/evidence/evaluation-reports` client access, `useEvaluationEvidence('passed')`, and Audit route live-query cards with dev-only fixture fallback | `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `pnpm -F acgi-ai run test:mvp`; `pnpm -F acgi-ai run build` |
| Console receipt proof journey | Buyers needed one receipt-level proof path instead of separate action/audit fragments | Added guarded `/console/audit/:receiptId`, a single receipt proof packet API/hook/mock handler, action-to-proof navigation, hash-chain status, policy path, before/after state, and signed evidence packet export copy | `acgi-ai/src/routes/console/AuditProof.tsx`; `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `acgi-ai/scripts/check-router-contract.mjs`; `pnpm -F acgi-ai run test:mvp`; `pnpm -F acgi-ai run test:router` |
| Live bus receipt proof lookup | The receipt proof journey still needed a bus-owned proof source instead of only `/api/v1` fixtures | Added `GET /api/bus/receipts/{receipt_id}` in `agent-bus-analyzer`, `ReceiptProof` OpenAPI/codegen, console normalization to `ReceiptProofPacket`, MSW bus proof fixtures, and downloadable signed evidence packet control | `packages/agent-bus-analyzer/tests/test_api_traces.py`; `packages/agent-bus-analyzer/tests/test_schema_export.py`; `acgi-ai/contracts/bus.openapi.json`; `acgi-ai/src/api/bus.generated.ts`; `pnpm -F acgi-ai run test:bus-schema`; `pnpm -F acgi-ai run test:mvp` |
| gove-zone audit-tail receipt import | The bus receipt endpoint still had to prove it could ingest live `gove-zone` audit JSONL records, not only analyzer-native or fixture-shaped events | Added canonical `DecisionRecord` projection for `tool`/`argument_hash`/`timestamp_iso`/`event_hash`, a one-shot `agent-bus-analyzer import-audit` backfill command, and an evidence packet export signature digest carrying the source audit hash | `packages/agent-bus-analyzer/tests/test_gove_zone_audit_tail.py`; `packages/agent-bus-analyzer/README.md`; `agent-bus-analyzer import-audit --audit-file ... --store-dir ... --constitutional-hash ...` |
| Deployment-managed evidence signing | Receipt exports had a canonical digest but no operator-controlled signing-material contract or fail-closed downgrade protection | Added stdlib HMAC-SHA256 canonical JSON signing via `ACGS_EVIDENCE_SIGNING_KEY_ID` + `ACGS_EVIDENCE_SIGNING_SECRET`, explicit unsigned-local digest metadata for dev, and `ACGS_EVIDENCE_SIGNING_REQUIRED=true` fail-closed behavior for deploys | `packages/agent-bus-analyzer/src/agent_bus_analyzer/signing.py`; `packages/agent-bus-analyzer/tests/test_evidence_signing.py`; `packages/agent-bus-analyzer/tests/test_gove_zone_audit_tail.py`; `packages/agent-bus-analyzer/README.md` |
| Console proof signature visibility | Operators could download the evidence packet but still had to inspect raw JSON to see whether it was deployment-signed or only locally digested | Added normalized `EvidenceSignatureSummary` metadata, parsed `export_signature` from bus proof packets, seeded signer-key fixtures, and surfaced signature status, algorithm, key id, digest, and notes in `/console/audit/:receiptId` | `acgi-ai/src/api/types.ts`; `acgi-ai/src/api/hooks.ts`; `acgi-ai/src/routes/console/AuditProof.tsx`; `acgi-ai/src/mocks/data/actions.ts`; `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `pnpm -F acgi-ai run test:mvp`; `pnpm -F acgi-ai run build:console` |
| Analyzer API managed-secret deploy contract | The analyzer `serve` command could boot without a store and there was no package-local service template proving Secret Manager-backed signing material | Added `--store-dir`/`AGENT_BUS_ANALYZER_STORE_DIR` support to `agent-bus-analyzer serve`, a non-root deploy image, a Cloud Run template with pinned `secretKeyRef` signing secret wiring and fail-closed signing required, plus static deploy contract tests | `packages/agent-bus-analyzer/src/agent_bus_analyzer/cli.py`; `packages/agent-bus-analyzer/deploy/Dockerfile`; `packages/agent-bus-analyzer/deploy/cloudrun/service.yaml`; `packages/agent-bus-analyzer/tests/test_cli.py`; `packages/agent-bus-analyzer/tests/test_deploy_contract.py`; `cd packages/agent-bus-analyzer && PYTHONPATH=src python -m pytest tests/test_cli.py tests/test_deploy_contract.py -q` |
| Analyzer persistent trace-store deploy contract | Store-backed `serve` previously used a single-container filesystem and the Cloud Run template lacked a durable trace mount or single-writer guard for the file-backed store | Added a Cloud Storage FUSE trace bucket mount at `/var/lib/agent-bus-analyzer`, stable non-root UID/GID `10001`, `maxScale=1` for the current SQLite+JSONL `TraceStore`, and static deploy contract tests | `packages/agent-bus-analyzer/deploy/cloudrun/service.yaml`; `packages/agent-bus-analyzer/deploy/Dockerfile`; `packages/agent-bus-analyzer/tests/test_deploy_contract.py`; `cd packages/agent-bus-analyzer && PYTHONPATH=src python -m pytest tests/test_deploy_contract.py -q` |
| Analyzer mounted-store import and smoke contract | The mounted trace-store service still lacked an operator-owned way to backfill one canonical audit receipt into the bucket and verify that the deployed API reads a signed proof | Added a single-task Cloud Run `import-audit` Job template with shared GCS FUSE mount and no automatic retries, plus `agent-bus-analyzer postdeploy-smoke` to verify health, authenticated receipt lookup, hash-chain status, and deployment-signed evidence packet metadata | `packages/agent-bus-analyzer/deploy/cloudrun/import-audit-job.yaml`; `packages/agent-bus-analyzer/src/agent_bus_analyzer/cli.py`; `packages/agent-bus-analyzer/tests/test_cli.py`; `packages/agent-bus-analyzer/tests/test_deploy_contract.py`; `cd packages/agent-bus-analyzer && PYTHONPATH=src python -m pytest tests/test_cli.py tests/test_deploy_contract.py -q` |
| Analyzer Phoenix trace cross-links | Signed receipt proof could verify ACGS evidence but could not drill through to Phoenix/OpenTelemetry spans from the same run | Added `phoenix_trace_id`, `phoenix_span_id`, and `phoenix_parent_span_id` extraction from `traceparent`/explicit carrier fields, propagated them through `Event`, `TraceListItem`, `ReceiptProof`, the signed evidence packet, vendored OpenAPI/codegen, and `/console/audit/:receiptId` proof cards | `packages/agent-bus-analyzer/tests/test_schema_export.py`; `packages/agent-bus-analyzer/tests/test_api_traces.py`; `packages/agent-bus-analyzer/tests/test_gove_zone_audit_tail.py`; `acgi-ai/contracts/bus.openapi.json`; `acgi-ai/src/api/hooks.ts`; `acgi-ai/src/routes/console/AuditProof.tsx`; `pnpm -F acgi-ai run test:bus-schema`; `pnpm -F acgi-ai run test:mvp`; `pnpm -F acgi-ai run build:console` |
| Generated scratch noise | Analysis/tooling directories appeared as untracked repo noise | Ignored local `.antigravitycli/`, `.codegraph/`, `.plugin-eval/`, `.understand-anything/`, and `.gove-zone/` audit chain | `.gitignore`; `git status --short` |
| Console deploy identity | `/healthz` contract requires `{ ok, served_hash, build_id }`, but the console deploy path only proved `ok`/`served_hash` | Added env-backed `build_id` in `infra/Caddyfile`, rendered it from `${GITHUB_SHA::12}` into Cloud Run, and locked it with security invariants | `acgi-ai/scripts/check-security-invariants.mjs`; `.github/workflows/console.yml`; `acgi-ai/infra/cloudrun/service.yaml` |
| Post-deploy verification | `acgi-ai/PLAN.md` calls for `scripts/postdeploy-verify.sh`, but the script and workflow wiring were absent | Added executable `postdeploy-verify.sh`, `pnpm run verify:postdeploy`, artifact handoff from build to deploy, and workflow execution against the deployed Cloud Run revision | `acgi-ai/scripts/postdeploy-verify.sh`; `acgi-ai/package.json`; `.github/workflows/console.yml`; `pnpm -F acgi-ai run test:security` |
| Post-deploy live asset auth sentinels | Live deploy proof could trust the local `dist/` scan while the served JS asset still contained a demo-auth path | `postdeploy-verify.sh` now downloads the entry document's live `/assets/*` resources and rejects inline styles, unexpected third-party URL literals, `sessionStorage`, and the demo session key; a synthetic pass/fail test locks the behavior | `acgi-ai/scripts/postdeploy-verify.sh`; `acgi-ai/scripts/check-postdeploy-live-assets.mjs`; `pnpm -F acgi-ai run test:postdeploy-live-assets` |
| Engineering claim matrix | `acgi-ai/PLAN.md` A21 requires every public compliance/security claim to map to evidence and legal review before deploy | Added an engineering-draft `claim-matrix.json`, removed `production-ready` / `auditor-ready` copy, softened subprocessor/auth/retention wording, and added a static overclaim gate; legal signoff remains external | `acgi-ai/claim-matrix.json`; `acgi-ai/scripts/check-claim-matrix.mjs`; `pnpm -F acgi-ai run test:claim-matrix` |
| Marketing/console bundle split | `acgi-ai/PLAN.md` marks a combined marketing artifact that embeds console code as a critical two-origin privilege-boundary failure | Added mode-specific Vite surface aliases, explicit `build:marketing` / `build:console` profiles, Vercel/Cloud Run workflow wiring, and a bundle-split verifier that builds both artifacts and scans the marketing artifact for console-only sentinels | `acgi-ai/src/surfaces/*/App.tsx`; `acgi-ai/vite.config.ts`; `acgi-ai/scripts/check-surface-bundles.mjs`; `pnpm -F acgi-ai run test:surfaces` |
| Console `/api/*` bus proxy readiness | `acgi-ai/PLAN.md` Phase 5 requires the console origin to proxy `/api/*` to the governed bus and fail closed when `BUS_UPSTREAM` is absent | Replaced the static 503 placeholder with a Caddy reverse proxy to runtime `BUS_UPSTREAM`, added `X-ACGS-Schema-Version` handshake headers, rendered `CONSOLE_BUS_UPSTREAM` into Cloud Run, and added static + Docker-backed proxy verification | `acgi-ai/infra/Caddyfile`; `acgi-ai/infra/cloudrun/service.yaml`; `.github/workflows/console.yml`; `acgi-ai/scripts/check-bus-proxy-contract.mjs`; `acgi-ai/scripts/smoke-bus-proxy-contract.mjs` |
| Console auth forward gate | `/console*` deep links could still fall through to the SPA fallback while the external OIDC/server-cookie provider remained unprovisioned, and the SPA guard had no provider-agnostic production session-status check after edge auth accepted a request | Added a Caddy `@console_routes` `forward_auth` gate backed by `AUTH_UPSTREAM` with a closed-port fallback, added the same-origin `/auth/status` forward-auth status bridge for the production SPA route guard, rendered `AUTH_UPSTREAM` through every Cloud Run template, and made console deploy fail before Cloud Run when `CONSOLE_AUTH_UPSTREAM` is missing | `acgi-ai/infra/Caddyfile`; `acgi-ai/src/lib/session.ts`; `acgi-ai/src/surfaces/console/App.tsx`; `acgi-ai/infra/cloudrun/service.*.yaml`; `.github/workflows/console.yml`; `acgi-ai/scripts/check-auth-boundary.mjs`; `pnpm -F acgi-ai run test:auth-boundary` |
| Fixture fallback fail-closed boundary | `acgi-ai/PLAN.md` A1/T1 requires fixture fallback to be impossible in production and narrow enough not to mask API contract drift in mock mode | Tightened `withFixtureFallback` so production returns no fallback, `ApiError`/4xx/5xx and non-network errors rethrow, and fixture data is used only for explicit network-unavailable `TypeError` cases in non-production mock mode; static security/MVP gates lock the boundary | `acgi-ai/src/api/hooks.ts`; `acgi-ai/scripts/check-security-invariants.mjs`; `acgi-ai/scripts/check-gove-zone-mvp.mjs`; `pnpm -F acgi-ai run test:security`; `pnpm -F acgi-ai run test:mvp` |
| Bus schema ownership and codegen drift | `acgi-ai/PLAN.md` UC7 requires bus-owned schema vendoring, generated types, unknown/missing-field cases, schema-version skew, and error-envelope coverage | Added `contracts/bus.openapi.json` as the vendored source of truth, kept `src/api/openapi.json` as a compatibility mirror, added strict positive/negative fixtures, moved `gen:api` to the contract path, and wired `test:bus-schema` into contract/all/security/CI gates | `acgi-ai/contracts/bus.openapi.json`; `acgi-ai/contracts/fixtures/bus/*`; `acgi-ai/scripts/check-bus-schema-contract.mjs`; `pnpm -F acgi-ai run test:bus-schema` |
| Root OpenAPI regeneration target | The root `make openapi` command still regenerated only the legacy `src/api/openapi.json` mirror and could leave the vendored contract or generated TypeScript stale | Repointed `make openapi` to export `agent-bus-analyzer` OpenAPI into `acgi-ai/contracts/bus.openapi.json`, format it, mirror it to `src/api/openapi.json`, and run `acgi-ai` `gen:api`; extended `test:bus-schema` to guard this root command wiring | `Makefile`; `acgi-ai/scripts/check-bus-schema-contract.mjs`; `make openapi`; `pnpm -F acgi-ai run test:bus-schema` |
| Cloud Run service templates | `acgi-ai/PLAN.md` A14 requires reviewable per-environment `service.yaml` templates instead of ad-hoc production overrides | Added preview/staging/production Cloud Run manifests, made `console.yml` render `service.yaml` from `service.${DEPLOY_ENV}.yaml`, documented scale/cost/SLO assumptions, and locked the contract with `test:cloudrun-templates` | `acgi-ai/infra/cloudrun/service.*.yaml`; `.github/workflows/console.yml`; `acgi-ai/scripts/check-cloudrun-templates.mjs`; `pnpm -F acgi-ai run test:cloudrun-templates` |
| Shared Cloud Run renderer | Deploy-time substitution for image/build/auth/bus values lived inline in the workflow, making local dry-run proof and drift detection weak | Added `render-cloudrun-service.mjs`, `render:cloudrun`, `test:cloudrun-renderer`, workflow reuse of the shared fail-closed renderer, and runtime checks for missing secrets, invalid upstreams, unsupported environments, placeholder counts, and leftover `REPLACE_*` markers | `acgi-ai/scripts/render-cloudrun-service.mjs`; `acgi-ai/scripts/check-cloudrun-renderer.mjs`; `.github/workflows/console.yml`; `pnpm -F acgi-ai run test:cloudrun-renderer` |
| Container image and toolchain pins | `acgi-ai/PLAN.md` A12 requires Node 24 and a non-floating Caddy runtime image before production console deploy | Pinned the console runtime image to `caddy:2.10.2-alpine`, kept Node at `node:24-alpine`, aligned Docker smoke parity, and added a static image/toolchain contract gate | `acgi-ai/infra/Dockerfile.console`; `acgi-ai/scripts/smoke-bus-proxy-contract.mjs`; `acgi-ai/scripts/check-container-pins.mjs`; `pnpm -F acgi-ai run test:container-pins` |
| Local Node 24 verification gate | Local shells could pass frontend gates with Node engine warnings while CI/deploy require Node 24 | Added `scripts/run_acgi_node24_gate.sh` and `make verify-js-node24` to activate the existing `fnm` Node 24 from `acgi-ai/.node-version`, verify pnpm 9.15.4, and run the full acgi-ai readiness suite without shell-default Node drift | `scripts/run_acgi_node24_gate.sh`; `Makefile`; `tests/test_node24_gate.py`; `make verify-js-node24` |
| Production demo-auth exclusion | `acgi-ai/PLAN.md` UC2 forbids sessionStorage as the production auth path while real OIDC/server cookies remain the target | Centralized the demo-session gate, made `hasSession()` false in production, blocked `sessionStorage` access before touching browser storage, and added a production bundle sentinel scan | `acgi-ai/src/lib/session.ts`; `acgi-ai/scripts/check-auth-boundary.mjs`; `pnpm -F acgi-ai run test:auth-boundary` |
| Font hash manifest | `acgi-ai/PLAN.md` A12 requires the self-hosted WOFF2 set to have a committed hash manifest verified at build | Added `fonts.sha256`, a contract check that hashes every WOFF2 and reconciles it with `src/fonts.css`, plus package/build/test wiring | `acgi-ai/fonts.sha256`; `acgi-ai/scripts/check-font-manifest.mjs`; `pnpm -F acgi-ai run test:font-manifest` |
| Marketing CSP report-only | `acgi-ai/PLAN.md` A11 requires marketing to ship CSP in report-only mode before cutover | Added a same-origin `Content-Security-Policy-Report-Only` Vercel header with report-uri sink and a static contract check that prevents accidental enforcement before cutover | `acgi-ai/vercel.json`; `acgi-ai/scripts/check-marketing-csp.mjs`; `pnpm -F acgi-ai run test:marketing-csp` |
| Vercel marketing edge routing | `acgi-ai/DEPLOY.md` requires marketing `/console` paths to cross to the privileged console origin while internal agent/design docs stay denied before SPA fallback | Added a static route-order gate for internal-doc 404s, exact/deep `/console` 308 redirects, product/marketing SPA fallback, and package/security wiring | `acgi-ai/vercel.json`; `acgi-ai/scripts/check-vercel-routes.mjs`; `pnpm -F acgi-ai run test:vercel-routes` |
| Deploy workflow readiness gate | The deploy workflows could lint/build while bypassing the broader local readiness suite already encoded in `pnpm test:all` | Added a static CI gate verifier and wired both console/marketing workflows to run `pnpm test:all` before credentialed deploy steps; console path filters now include readiness scripts/docs | `.github/workflows/console.yml`; `.github/workflows/marketing.yml`; `acgi-ai/scripts/check-ci-readiness-gates.mjs`; `pnpm -F acgi-ai run test:ci-gates` |
| Production deploy fail-closed gate | Marketing workflow could previously warn/skip when Vercel secrets were missing on push | Made missing production Vercel secrets an error, added `test:production-deploy-contract`, and locked console/marketing deploy proof strings while keeping local readiness distinct from live production deployment proof | `.github/workflows/marketing.yml`; `acgi-ai/scripts/check-production-deploy-contract.mjs`; `pnpm -F acgi-ai run test:production-deploy-contract` |
| Production launch handoff | External production launch still required humans with credentials, but the exact secrets, preflight commands, live proof artifacts, and rollback triggers were not machine-checked in one operator handoff | Added `PRODUCTION-LAUNCH.md`, `test:production-launch-handoff`, console workflow path coverage for the handoff, and release/readiness evidence wiring while preserving the boundary that local checks are not production deployment proof | `acgi-ai/PRODUCTION-LAUNCH.md`; `acgi-ai/scripts/check-production-launch-handoff.mjs`; `pnpm -F acgi-ai run test:production-launch-handoff` |
| Production authority packet | Local readiness could still be misread as permission to deploy, mutate DNS, enable Storybook Pages, or publish stronger claims | Added `production-authority.example.json` and `test:production-authority-packet` to keep deploy-owner, DNS-owner, auth-owner, claim-owner, and rollback approvals as `pending-external:deploy-owner-approval` style refs until signed authority evidence is attached; this is not production deployment proof | `acgi-ai/production-authority.example.json`; `acgi-ai/scripts/check-production-authority-packet.mjs`; `pnpm -F acgi-ai run test:production-authority-packet`; `make platform-readiness`; `make release-evidence` |
| Production evidence template | Operators needed a machine-readable intake shape for live deploy proof without confusing a local template for production evidence | Added `production-evidence.example.json`, `test:production-evidence-template`, console workflow path coverage, release/readiness wiring, and `pending-external` assurance placeholders with an explicit not live production proof boundary | `acgi-ai/production-evidence.example.json`; `acgi-ai/scripts/check-production-evidence-template.mjs`; `pnpm -F acgi-ai run test:production-evidence-template` |
| Production blocker report | Failing `verify:production-live` JSON identified blockers, but operators still needed a local handoff artifact that made blocker ids copy-safe without implying production proof | Added `build:production-blocker-report`, `test:production-blocker-report`, and a `production-blocker-report` artifact with `productionLiveStatus`, `productionLiveBlockers`, `blockedUntil`, and `copyIntoProductionEvidence` fields while preserving the not live production proof boundary | `acgi-ai/scripts/build-production-blocker-report.mjs`; `acgi-ai/scripts/check-production-blocker-report.mjs`; `pnpm -F acgi-ai run test:production-blocker-report` |
| Production evidence validator | Operators needed a machine-verifiable way to validate a completed manifest and attached live-verifier JSON without treating local checks as deployment proof | Added the production evidence validator, `validate:production-evidence`, `test:production-evidence-validator`, `productionLiveBlockers`, `productionEvidenceValidationCommand`, `productionEvidenceValidationOutputRef`, and `validatedProductionEvidence` slots so a completed manifest can be checked against `verify:production-live` output and copied blocker ids while preserving `pending-external` assurance boundaries | `acgi-ai/scripts/validate-production-evidence.mjs`; `acgi-ai/scripts/check-production-evidence-validator.mjs`; `pnpm -F acgi-ai run test:production-evidence-validator` |
| Root Python typecheck fan-out clarity | Root `make verify` printed non-fatal mypy errors from unconfigured or broader-than-configured legacy package paths, making local readiness evidence noisy | Changed `make typecheck-py` to run mypy only for packages declaring `[tool.mypy]`, without overriding each package's pyproject `files` scope; all registered Python packages now declare a strict mypy gate, with `packages/Acgs-Swarm` starting from a strict core-primitives source scope while broad research/optional-runtime scope widening remains separate hardening work | `Makefile`; `tests/test_root_typecheck_gate.py`; `acgs-cft-governance-pack/pyproject.toml`; `acgs_governance_eval_mvp/pyproject.toml`; `packages/Acgs-Swarm/pyproject.toml`; `make typecheck-py`; `make verify` |
| Console CSP/Tailwind harness | `acgi-ai/PLAN.md` makes Tailwind v4 + strict-CSP coexistence a Phase 0 hard gate before production console deploy | Added a production console CSP harness that builds the console artifact, scans source for inline styles / literal arbitrary Tailwind classes, scans built assets for runtime style injection and unexpected URL literals, and verifies MSW/public dev artifacts are stripped from production output | `acgi-ai/scripts/check-console-csp-harness.mjs`; `acgi-ai/vite.config.ts`; `acgi-ai/scripts/postdeploy-verify.sh`; `pnpm -F acgi-ai run test:csp` |
| Surface stylesheet bundle integrity | `acgi-ai/PLAN.md` DOD requires buyer-visible marketing and console styling to ship in production artifacts | Added a production CSS selector gate so the build fails if `src/App.css` is dropped from the Vite bundle | `acgi-ai/src/index.css`; `acgi-ai/src/App.css`; `pnpm -F acgi-ai run test:style-bundle` |
| Bundle performance budget | `acgi-ai/PLAN.md` Phase 5 requires marketing <= 200 KB and console <= 350 KB gzipped bundle budgets before production deploy | Added a no-dependency performance budget verifier that builds both surfaces into `.performance-check/`, sums gzipped JS+CSS bytes, and wires the gate into `test:all`, security invariants, and CI readiness | `acgi-ai/scripts/check-performance-budget.mjs`; `acgi-ai/package.json`; `pnpm -F acgi-ai run test:performance` |
| Trust and security pages | `acgi-ai/PLAN.md` A22 requires `/trust`, `/security`, DPA/SOC2 roadmap language, security.txt, and a subprocessor change RSS before production launch | Added conservative engineering-draft trust/security routes, `/.well-known/security.txt`, `/subprocessors.xml`, claim-matrix links, and a static publication gate that forbids stronger SOC2/WCAG/pentest claims without evidence | `acgi-ai/src/routes/Trust.tsx`; `acgi-ai/src/routes/Security.tsx`; `acgi-ai/public/.well-known/security.txt`; `acgi-ai/public/subprocessors.xml`; `pnpm -F acgi-ai run test:trust-surface` |
| Accessibility foundation gate | `acgi-ai/PLAN.md` calls for `test:a11y`, skip links, landmarks, visible focus, and reduced-motion evidence, while full manual WCAG evidence remains external | Added static skip links/main targets across the route shells, documented the bounded A11Y contract, and wired a no-dependency static verifier into `test:all` | `acgi-ai/A11Y.md`; `acgi-ai/scripts/check-a11y-foundation.mjs`; `pnpm -F acgi-ai run test:a11y` |
| DX scaffolding docs | `acgi-ai/PLAN.md` A16 requires early `ARCHITECTURE.md` and `INTEGRATING.md` scaffolds so frontend and bus authors share the current contract | Added current-truth architecture, bus integration, and getting-started docs plus `hello`, dev mode, contract, audit, and docs-scaffold script wiring | `acgi-ai/ARCHITECTURE.md`; `acgi-ai/INTEGRATING.md`; `acgi-ai/GETTING_STARTED.md`; `acgi-ai/scripts/check-dx-scaffold.mjs`; `pnpm -F acgi-ai run test:docs-scaffold` |
| Runtime flags and AppError primitives | `acgi-ai/PLAN.md` Phase 0/1 requires typed `VITE_*` runtime flags and an `AppError` taxonomy before state/error coverage work expands | Added typed runtime flag parsing, deterministic eval-mode trace IDs, exhaustive `AppError` details, route throw hygiene scanning, and package/security wiring | `acgi-ai/src/lib/flags.ts`; `acgi-ai/src/lib/errors.ts`; `acgi-ai/src/vite-env.d.ts`; `acgi-ai/scripts/check-runtime-primitives.mjs`; `pnpm -F acgi-ai run test:runtime-primitives` |
| TanStack Router migration | `acgi-ai/PLAN.md` marks the custom pathname router as a critical Phase 0 blocker for `?next=`, `/products/<slug>`, and route-level console guards | Replaced marketing and console pathname state routers with TanStack Router route trees, product path params, login search validation, console session guards, `/console/overview` canonical redirect, and a router contract gate | `acgi-ai/src/surfaces/marketing/App.tsx`; `acgi-ai/src/surfaces/console/App.tsx`; `acgi-ai/scripts/check-router-contract.mjs`; `pnpm -F acgi-ai run test:router` |
| Console state coverage foundation | `acgi-ai/PLAN.md` Phase 1 requires explicit console handling for loading, empty, error, partial-bus, stale-while-revalidating, retry-in-flight, conflicted-mutation, permission-denied, rate-limited, optimistic-pending, and expired-session states | Added reusable state primitives, an `emptyMeans` taxonomy, shared Overview/MACI loading/error chrome, non-production environment indicator evidence, and a static contract gate wired into `test:all`, security invariants, and CI readiness | `acgi-ai/src/routes/console/shared.tsx`; `acgi-ai/scripts/check-console-state-coverage.mjs`; `pnpm -F acgi-ai run test:state-coverage` |
| Polling hygiene foundation | `acgi-ai/PLAN.md` Phase 1 requires jittered live/slow polling windows, background interval suppression, Visibility API gating, and bus-health adaptive backoff | Added a shared `useBusHealth()` polling contract, jittered 5-10s/30-60s intervals, QueryClient background interval suppression, and a static gate wired into `test:all`, security invariants, and CI readiness | `acgi-ai/src/api/hooks.ts`; `acgi-ai/scripts/check-polling-hygiene.mjs`; `pnpm -F acgi-ai run test:polling-hygiene` |
| Cross-tab session sync foundation | `acgi-ai/PLAN.md` Phase 1 requires session changes to broadcast via `localStorage` and `hasSession()` to re-check on query retry while production auth remains external | Added gated demo-session sync messages, a console `subscribeToSessionSync()` listener, router invalidation after storage events, retry-time `hasSession()` checks, and a static gate wired into `test:all`, security invariants, and CI readiness | `acgi-ai/src/lib/session.ts`; `acgi-ai/src/surfaces/console/App.tsx`; `acgi-ai/scripts/check-session-sync.mjs`; `pnpm -F acgi-ai run test:session-sync` |
| AppError boundary foundation | `acgi-ai/PLAN.md` Phase 1 requires console page faults to fall through a console-styled AppError boundary instead of blanking the privileged shell | Added a path-resetting console `ErrorBoundary`, normalized page/root faults through `toAppError()`, surfaced cause/fix/trace IDs in `ConsoleError`, and added route throw hygiene plus package/security/CI wiring | `acgi-ai/src/routes/Console.tsx`; `acgi-ai/src/routes/console/shared.tsx`; `acgi-ai/src/main.tsx`; `acgi-ai/scripts/check-app-error-contract.mjs`; `pnpm -F acgi-ai run test:app-errors` |
| Login interstitial foundation | `acgi-ai/PLAN.md` Phase 1 A6 requires `/login` to make the privilege handoff visible before console entry | Added an 800ms parchment interstitial on SSO provider selection that labels operator `custodian-01`, entered matter, and constitutional hash while preserving the no-client-side-session auth boundary | `acgi-ai/src/routes/Login.tsx`; `acgi-ai/src/App.css`; `acgi-ai/scripts/check-login-interstitial.mjs`; `pnpm -F acgi-ai run test:login-interstitial` |
| Privilege banner foundation | `acgi-ai/PLAN.md` Phase 1 A8 requires no fixed/sticky route chrome, toast, modal, or FAB to occlude the privilege banner, and Phase 1 requires right-rail live regions | Added a structural banner region, semantic z-index tokens that keep mobile drawer/backdrop layers below it, a polite right-rail receipt region, and a static contract gate for route-local overlay hygiene | `acgi-ai/src/routes/Console.tsx`; `acgi-ai/src/index.css`; `acgi-ai/src/App.css`; `acgi-ai/scripts/check-privilege-banner-contract.mjs`; `pnpm -F acgi-ai run test:privilege-banner` |
| Wire decisions foundation | `acgi-ai/PLAN.md` Phase 1 A7 requires per-route wire-level UI decisions for headers, actions, density, filters, pagination, right rail, receipt lifetime, and destructive confirmation | Added a typed console wire-decision registry, consumed it in the console shell for route metadata and the right-rail route contract card, documented the route-by-route appendix in `DESIGN.md`, and wired a static contract gate | `acgi-ai/src/routes/console/wire-decisions.ts`; `acgi-ai/src/routes/Console.tsx`; `acgi-ai/DESIGN.md`; `acgi-ai/scripts/check-wire-decisions.mjs`; `pnpm -F acgi-ai run test:wire-decisions` |
| Test surface script foundation | `acgi-ai/PLAN.md` Phase 2/A15 names `test:e2e` and `test:visual`, but the current package lacked bounded local script surfaces for those future browser gates | Added manifest-only E2E and visual baseline gates plus a parent test-surface verifier wired into package scripts, security invariants, CI readiness checks, and docs without claiming Playwright/browser execution | `acgi-ai/scripts/check-test-surface-foundation.mjs`; `acgi-ai/scripts/check-e2e-smoke-foundation.mjs`; `acgi-ai/scripts/check-visual-baseline-foundation.mjs`; `pnpm -F acgi-ai run test:test-surface`; `pnpm -F acgi-ai run test:e2e`; `pnpm -F acgi-ai run test:visual` |
| E2E HTTP shell smoke | `acgi-ai/PLAN.md` Phase 2 requires route-level smoke coverage, but Playwright remains future work and the local package needed a runtime route-shell gate that avoids new dependencies | Added a mock-dev-server smoke that fetches the marketing landing, product slugs, login handoff URL, and every in-scope console sidebar route to prove each returns the Vite root shell without claiming browser navigation or screenshots | `acgi-ai/scripts/smoke-e2e-http-shells.mjs`; `acgi-ai/scripts/check-e2e-http-foundation.mjs`; `pnpm -F acgi-ai run test:e2e-http` |
| TTHW foundation | `acgi-ai/PLAN.md` A4 requires a clean-runner time-to-hello-world gate, but the current package lacked the executable runner, scheduled clean-runner workflow, and local static verifier | Added `hello-world.sh`, `hello:world`, `hello:world:local`, `test:tthw`, and scheduled `tthw.yml` wiring; local proof is an HTTP shell foundation only and browser first-render evidence remains external | `acgi-ai/scripts/hello-world.sh`; `.github/workflows/tthw.yml`; `acgi-ai/scripts/check-tthw-foundation.mjs`; `pnpm -F acgi-ai run test:tthw`; `pnpm -F acgi-ai run hello:world:local` |
| MSW node-mode foundation | `acgi-ai/PLAN.md` Phase 2 requires hook tests to exercise the same MSW handlers as the dev server with strict unhandled-request behavior in eval/test mode | Added a shared node-mode MSW server, eval-mode unhandled-request policy, package/security/CI wiring, and a static verifier; actual hook tests remain Phase 2 work until a test runner is added | `acgi-ai/src/mocks/server.ts`; `acgi-ai/src/mocks/policy.ts`; `acgi-ai/scripts/check-msw-node-foundation.mjs`; `pnpm -F acgi-ai run test:msw-node` |
| Buyer evidence gallery foundation | `acgi-ai/PLAN.md` asks for Storybook as a buyer-evidence artifact, but adding a new Storybook dependency and hosted deploy is not locally authorized in this pass | Added a dependency-free local buyer-evidence gallery builder plus verifier covering the receipt proof journey, bus-owned proof source, claim-safe trust surface, and deploy-readiness boundary; `storybook:build` aliases the local artifact while hosted Storybook remains pending | `acgi-ai/scripts/build-buyer-evidence.mjs`; `acgi-ai/scripts/check-buyer-evidence-artifact.mjs`; `pnpm -F acgi-ai run evidence:build`; `pnpm -F acgi-ai run test:buyer-evidence` |
| Buyer evidence CI artifact | Local buyer-evidence proof was buildable but not retained by deploy/readiness CI for reviewer handoff | Added a console workflow step that builds the buyer evidence gallery and uploads it as the `buyer-evidence-gallery` artifact before any credentialed GCP/auth/deploy step | `.github/workflows/console.yml`; `acgi-ai/scripts/check-ci-readiness-gates.mjs`; `pnpm -F acgi-ai run test:ci-gates`; `make platform-readiness` |
| Official Storybook runtime dependency plan | Hosted buyer-evidence still needs an official Storybook runtime, but adding new dependencies was not authorized in this pass | Added `storybook-runtime.plan.json` plus `test:storybook-runtime-plan` to keep `@storybook/react-vite`, `npx storybook@latest init`, lockfile updates, and shim replacement behind `pending-external:dependency-owner-approval`; the plan is not official Storybook runtime proof, not hosted Storybook proof, and not production deployment proof | `acgi-ai/storybook-runtime.plan.json`; `acgi-ai/scripts/check-storybook-runtime-plan.mjs`; `pnpm -F acgi-ai run test:storybook-runtime-plan`; `make platform-readiness`; `make release-evidence` |
| Buyer evidence Storybook publication scaffold | Hosted buyer-evidence target lacked a workflow-scoped publication path | Added `.github/workflows/storybook.yml`, CNAME-capable buyer-evidence build output, `test:storybook-publication`, artifact upload, and a gated GitHub Pages deploy to `storybook.acgs.ai` through `STORYBOOK_PAGES_ENABLED` | `.github/workflows/storybook.yml`; `acgi-ai/scripts/check-storybook-publication.mjs`; `pnpm -F acgi-ai run test:storybook-publication`; `make platform-readiness` |
| Hosted Storybook handoff | Pages workflow and live verifier existed, but operators still needed one local copy-safe handoff for `hosted-storybook-buyer-evidence` without claiming hosted proof | Added `build:hosted-storybook-handoff`, `test:hosted-storybook-handoff`, and `hosted-storybook-handoff.json` output that combines a Pages-ready buyer-evidence manifest with saved live verifier Storybook blockers, `storybook-manifest-live`, `pending-external:storybook-pages-proof`, and `copyIntoProductionEvidence.hostedStorybook` while preserving the not live production proof boundary | `acgi-ai/scripts/build-hosted-storybook-handoff.mjs`; `acgi-ai/scripts/check-hosted-storybook-handoff.mjs`; `pnpm -F acgi-ai run test:hosted-storybook-handoff`; `make platform-readiness` |
| Production cutover plan | Live verifier blockers were packaged, but operators still needed one machine-readable DNS/deploy cutover plan that combines saved live output and blocker report evidence | Added `build:production-cutover-plan`, `test:production-cutover-plan`, and `production-cutover-plan` output with required GitHub secrets, DNS cutover records, `productionLiveBlockers`, and `copyIntoProductionEvidence` while preserving the not live production proof boundary | `acgi-ai/scripts/build-production-cutover-plan.mjs`; `acgi-ai/scripts/check-production-cutover-plan.mjs`; `pnpm -F acgi-ai run test:production-cutover-plan`; `make platform-readiness` |
| Production evidence draft | Blocked live-verifier output still required manual manifest copying before validator proof could run | Added `build:production-evidence-draft`, `test:production-evidence-draft`, and a validator-ready `production-evidence.deployment-blocked.json` draft that combines saved live verifier, `production-blocker-report`, and `production-cutover-plan` artifacts with `pending-external:` refs for missing deploy/run proof while preserving the not live production proof boundary | `acgi-ai/scripts/build-production-evidence-draft.mjs`; `acgi-ai/scripts/check-production-evidence-draft.mjs`; `pnpm -F acgi-ai run test:production-evidence-draft`; `make platform-readiness` |
| Production evidence chain | Saved live-verifier and handoff artifacts could drift after regeneration, making copied `productionLiveBlockers` hard to trust before external deploy proof exists | Added a `productionEvidenceChain` snapshot in the release bundle that compares saved live verifier, blocker report, cutover plan, deployment-blocked draft, `production-evidence-validation.deployment-blocked.json`, and hosted Storybook handoff blocker sets while preserving the not live production proof boundary | `scripts/build_release_evidence.py`; `tests/test_release_evidence_bundle.py`; `make release-evidence`; `uv run python -m pytest tests/test_release_evidence_bundle.py --import-mode=importlib -q` |
| Production launch preflight | Local release evidence could still be handed to an operator without one explicit machine-readable ready/blocked launch decision | Added `production_launch_preflight.py`, `make production-launch-preflight`, and tests that convert `dist-release-evidence/manifest.json` into a conservative `production-launch-preflight` status with `requiredActions`, repository clean-commit freshness, live verifier state, evidence-chain state, and `externalBlockerIds` while preserving the not production deployment proof boundary | `scripts/production_launch_preflight.py`; `tests/test_production_launch_preflight.py`; `make production-launch-preflight`; `uv run python -m pytest tests/test_production_launch_preflight.py --import-mode=importlib -q` |

## Verified gates

| Gate | Result |
|---|---|
| `pnpm -F acgi-ai lint` | Pass: Biome checked 69 files after source-aware evidence-card update |
| `pnpm -F acgi-ai build` | Historical pass: font manifest, console build, and marketing build completed, but the Node 22 warning is not readiness evidence; use `make verify-js-node24` for current frontend readiness claims |
| `pnpm -F acgi-ai run test:mvp` | Pass: static Gove Zone MVP gate verifies source-aware evaluation evidence contracts and fixtures |
| `cd packages/gove-zone && PYTHONPATH=src python -m pytest tests -q` | Pass: gove-zone package tests completed successfully with benchmark adapter coverage |
| `cd packages/gove-zone && PYTHONPATH=src python -m ruff check src tests/test_benchmark_adapters.py tests/test_evaluation_adapter.py && PYTHONPATH=src python -m mypy src` | Pass: ruff clean and strict mypy clean across 16 source files |
| `cd acgs_governance_eval_mvp && python -m pytest tests -q` | Pass: 126 passed, including benchmark-result adapter and evaluation-ingest coverage |
| `cd acgs_governance_eval_mvp && python -m ruff check governance/benchmarks governance/evaluation.py tests/test_benchmark_adapters.py tests/test_evaluation_report_ingest.py` | Pass: ruff clean for touched eval evidence surfaces |
| `git diff --check` | Pass: no whitespace errors |
| `bash scripts/vibe-kanban-verify.sh` | Pass: 189 constitutional markers verified, shell syntax OK, profile JSON parsed |
| `(cd packages/gove-zone && uv run mypy .)` | Pass: no issues in 32 source files |
| `uv run --package gove-zone python -m pytest packages/gove-zone/tests --import-mode=importlib -q` | Pass: 72 passed |
| `uv run ruff check packages/gove-zone .claude/hooks/acgs-emit-receipt.py` | Pass |
| `uv run ruff format --check packages/gove-zone .claude/hooks/acgs-emit-receipt.py` | Pass |
| `uv build --package gove-zone` | Pass: sdist + wheel built |
| `uv run --package gove-zone python packages/gove-zone/examples/runtime_hook_demo.py` | Pass: observe receipt, chain verify, enforce fail-closed, enforce writable pass |
| `pnpm -F acgi-ai lint` | Historical local pass, but the Node 22 warning is not readiness evidence; project requires Node 24 |
| `pnpm -F acgi-ai build` | Historical local pass, but the Node 22 warning is not readiness evidence; builds console into `dist/` and marketing into `dist-marketing/`; project requires Node 24 |
| `pnpm -F acgi-ai run test:security && pnpm -F acgi-ai run test:mvp` | Pass |
| `pnpm -F acgi-ai run test:surfaces` | Pass: builds marketing and console artifacts separately; marketing artifact excludes console-only sentinels |
| `pnpm -F acgi-ai run test:performance` | Pass: builds both surfaces and verifies marketing <= 200 KB and console <= 350 KB gzipped JS+CSS budgets |
| `EXPECTED_BUILD_ID=test-build pnpm -F acgi-ai run verify:postdeploy -- http://127.0.0.1:18184` | Pass against local synthetic server with production headers, `/healthz`, live `/assets/*` fetching, and current `dist/` bundle scan |
| `pnpm -F acgi-ai run test:bus-schema` | Pass: verifies `contracts/bus.openapi.json`, generated bus type drift, strict positive/negative fixtures, schema-version skew, error envelopes, docs, package wiring, security invariants, and CI path filters |
| `pnpm -F acgi-ai run test:bus-proxy` | Pass: static contract verifies `BUS_UPSTREAM`, Cloud Run/workflow rendering, route ordering, schema headers, and Docker smoke support |
| `pnpm -F acgi-ai run test:cloudrun-templates` | Pass: static contract verifies preview/staging/production service templates, render-time placeholders, workflow selection, docs, and SLO/cost notes |
| `pnpm -F acgi-ai run test:cloudrun-renderer` | Pass: renders the production Cloud Run manifest with fake deploy inputs, rejects missing `AUTH_UPSTREAM`, and verifies workflow/docs/readiness use the shared renderer |
| `pnpm -F acgi-ai run test:production-deploy-contract` | Pass: verifies marketing production deploy fails closed on missing Vercel secrets, console deploy remains WIF/renderer/postdeploy gated, and docs/readiness/release evidence include the production deploy fail-closed contract |
| `pnpm -F acgi-ai run test:production-launch-handoff` | Pass: verifies the production launch handoff lists required Vercel/GCP/console secrets, preflight commands, live postdeploy command, release artifacts, rollback triggers, and conservative claim boundary |
| `pnpm -F acgi-ai run test:production-authority-packet` | Pass: verifies `production-authority.example.json` keeps deploy-owner/DNS/auth/claim/rollback approvals as `pending-external:deploy-owner-approval` style refs and is not production deployment proof |
| `pnpm -F acgi-ai run test:production-evidence-template` | Pass: verifies `production-evidence.example.json` remains a template-only production evidence template with live deploy proof placeholders, `pending-external` assurance fields, release artifact links, and a not live production proof claim boundary |
| `pnpm -F acgi-ai run test:production-live-verifier` | Pass: verifies the `verify:production-live` command exists, documents `--json`, checks DNS/HTTPS/healthz/security headers/Storybook, records output plus `blockedUntil`/`blockers` in the evidence template, and stays out of `test:all` as a live network proof command |
| `pnpm -F acgi-ai run test:production-blocker-report` | Pass: verifies `build:production-blocker-report` converts saved `verify:production-live` JSON into a local `production-blocker-report` with `copyIntoProductionEvidence`, `productionLiveStatus`, `productionLiveBlockers`, and a not live production proof claim boundary |
| `pnpm -F acgi-ai run test:production-evidence-validator` | Pass: verifies `validate:production-evidence` accepts completed `live-verified` and `deployment-blocked` manifests with matching live JSON, rejects `production-evidence.example.json` placeholders, guards `productionLiveStatus`, `productionLiveBlockers`, `productionEvidenceValidationCommand`, `productionEvidenceValidationOutputRef`, and `validatedProductionEvidence`, and stays out of `test:all` as an operator-specific validation command |
| `pnpm -F acgi-ai run test:production-cutover-plan` | Pass: verifies `build:production-cutover-plan` converts saved live verifier and production-blocker-report JSON into a local `production-cutover-plan` with required GitHub secrets, DNS cutover records, `productionLiveBlockers`, and `copyIntoProductionEvidence`; this is not live production proof and does not deploy or mutate DNS |
| `pnpm -F acgi-ai run test:production-evidence-draft` | Pass: verifies `build:production-evidence-draft` converts saved live verifier, `production-blocker-report`, and `production-cutover-plan` JSON into a validator-ready `production-evidence.deployment-blocked.json` draft with `productionLiveBlockers`, `productionEvidenceValidationCommand`, `productionBlockerReport`, `productionCutoverPlan`, and explicit `pending-external:` refs; this is not live production proof and does not deploy, mutate DNS, or fetch live origins |
| `uv run python -m pytest tests/test_release_evidence_bundle.py --import-mode=importlib -q` | Pass: verifies the release bundle exposes `productionEvidenceChain`, snapshots `production-evidence-validation.deployment-blocked.json`, and compares saved live-verifier, blocker-report, cutover-plan, deployment-blocked draft, validator, and `hostedStorybookHandoff` artifacts without claiming live production proof |
| `uv run python -m pytest tests/test_production_launch_preflight.py --import-mode=importlib -q` | Pass: verifies `production_launch_preflight.py` reports `blocked` for the current manifest until pending readiness items, live verifier blockers, evidence validation, and external blocker ids are replaced by proof; `--require-ready` is available for operator/CI use after evidence is attached |
| `pnpm -F acgi-ai run test:storybook-runtime-plan` | Pass: verifies `storybook-runtime.plan.json` keeps official Storybook runtime dependency work behind `pending-external:dependency-owner-approval`, documents `@storybook/react-vite` and `npx storybook@latest init`, preserves the current dependency-free `storybook:build` shim, and remains not official Storybook runtime proof, not hosted Storybook proof, and not production deployment proof |
| `pnpm -F acgi-ai run test:hosted-storybook-handoff` | Pass: verifies `build:hosted-storybook-handoff` converts a Pages-ready buyer-evidence manifest and saved live verifier JSON into `hosted-storybook-handoff.json` with `storybook-manifest-live`, `pending-external:storybook-pages-proof`, and `copyIntoProductionEvidence.hostedStorybook`; this is not live production proof and does not deploy, mutate DNS, fetch live origins, or install the official Storybook runtime |
| `pnpm -F acgi-ai run test:container-pins` | Pass: static contract verifies `.node-version`, package engine/packageManager, `node:24-alpine`, pinned `caddy:2.10.2-alpine`, Docker smoke parity, docs, and package/security wiring |
| `make verify-js-node24` | Pass: activates fnm Node 24 from `acgi-ai/.node-version`, verifies pnpm 9.15.4, and runs `pnpm -F acgi-ai run test:all` without Node engine warnings |
| `pnpm -F acgi-ai run test:auth-boundary` | Pass: static + bundle contract verifies client-side demo sessions are non-production-only, the production route guard awaits `/auth/status`, production bundles omit demo auth sentinels, `/console*` and `/auth/status` are Caddy `forward_auth` gated by `AUTH_UPSTREAM`, and deploy fails without `CONSOLE_AUTH_UPSTREAM` |
| `pnpm -F acgi-ai run test:font-manifest` | Pass: verifies every self-hosted WOFF2 has a matching SHA-256 manifest entry and `src/fonts.css` reference |
| `pnpm -F acgi-ai run test:postdeploy-live-assets` | Pass: synthetic post-deploy server verifies clean live assets pass and demo-auth-contaminated live JS is rejected |
| `pnpm -F acgi-ai run test:claim-matrix` | Pass: verifies engineering-draft claim matrix, conservative source copy, required claim IDs, evidence pointers, and package wiring |
| `pnpm -F acgi-ai run test:marketing-csp` | Pass: verifies Vercel marketing CSP is report-only, same-origin by default, and reports to the configured marketing sink |
| `pnpm -F acgi-ai run test:vercel-routes` | Pass: verifies Vercel internal-doc 404 ordering, `/console` and `/console/*` 308 redirects to `console.acgs.ai`, marketing/product SPA fallback, and package/security wiring |
| `pnpm -F acgi-ai run test:buyer-evidence` | Pass: verifies local buyer-evidence gallery generation, manifest/story coverage, conservative claim boundary, package wiring, and docs |
| `pnpm -F acgi-ai run test:storybook-publication` | Pass: verifies the gated `.github/workflows/storybook.yml` buyer-evidence publication scaffold, `storybook.acgs.ai` CNAME build output, Pages artifact/deploy wiring, and conservative claim boundary |
| `pnpm -F acgi-ai run test:ci-gates` | Pass: verifies console/marketing workflows run `pnpm test:all` before credentialed deploy steps, the console workflow uploads the buyer evidence gallery artifact, and console path filters include readiness scripts/docs |
| `make release-evidence` | Pass: writes a local release-readiness evidence bundle with readiness JSON, buyer-evidence artifact metadata, verification commands, and explicit external blockers; this is not live production proof |
| `pnpm -F acgi-ai run test:csp` | Pass: builds the production console artifact, verifies strict Caddy CSP/Tailwind harness wiring, strips production MSW assets, and scans source/output for inline-style and unexpected URL hazards |
| `pnpm -F acgi-ai run test:trust-surface` | Pass: verifies `/trust`, `/security`, `/.well-known/security.txt`, `/subprocessors.xml`, claim-matrix citations, package wiring, and conservative trust/security wording |
| `pnpm -F acgi-ai run test:a11y` | Pass: verifies static skip links, main landmarks, focus/reduced-motion foundations, bounded A11Y.md wording, docs evidence, and package wiring |
| `pnpm -F acgi-ai run test:docs-scaffold` | Pass: verifies `ARCHITECTURE.md`, `INTEGRATING.md`, `GETTING_STARTED.md`, `hello`, dev-mode aliases, contract/audit scripts, and docs avoid unsupported production/compliance claims |
| `pnpm -F acgi-ai run test:runtime-primitives` | Pass: verifies typed runtime flags, `AppError` taxonomy completeness, deterministic eval-mode trace IDs, route throw hygiene, and package/security wiring |
| `pnpm -F acgi-ai run test:router` | Pass: verifies TanStack Router route trees, `/products/$slug`, `/login?next`, console auth guards, `/console/overview` redirect, and docs/security/package wiring |
| `pnpm -F acgi-ai run test:state-coverage` | Pass: verifies the console 11-state primitive set, `emptyMeans` taxonomy, non-production environment indicator, shared Overview/MACI state chrome, docs, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:polling-hygiene` | Pass: verifies jittered live/slow polling windows, document-visibility gating, background interval suppression, shared `useBusHealth()` consumers, bus-health failure backoff, docs, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:session-sync` | Pass: verifies gated localStorage session-change broadcasts, storage-event listener wiring, console router invalidation, retry-time `hasSession()` checks, docs, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:app-errors` | Pass: verifies AppError taxonomy completeness, console shell `ErrorBoundary` wiring, typed `ConsoleError` details, root fallback details, route throw hygiene, docs, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:login-interstitial` | Pass: verifies the 800ms login parchment handoff, operator/matter/hash copy, Enter dismissal, package/security/CI wiring, and no client-side console session grant |
| `pnpm -F acgi-ai run test:privilege-banner` | Pass: verifies structural privilege-banner region, protected z-index tokens, polite right-rail live region, route overlay hygiene, docs, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:wire-decisions` | Pass: verifies typed per-route console wire decisions, shell route-contract evidence, DESIGN appendix, package wiring, security invariants, and CI readiness |
| `pnpm -F acgi-ai run test:test-surface` | Pass: verifies bounded E2E/visual manifest gates, package wiring, docs, security invariants, and CI readiness references without claiming browser execution |
| `pnpm -F acgi-ai run test:e2e` | Pass: verifies the Phase 2 E2E smoke route/viewport manifest, product slugs, console sidebar route coverage, docs, and package wiring |
| `pnpm -F acgi-ai run test:e2e-http` | Pass: starts the local mock Vite dev server and fetches marketing, product, login, and console sidebar route shells; browser Playwright execution remains external |
| `pnpm -F acgi-ai run test:visual` | Pass: verifies the Phase 2 visual baseline viewport/target manifest, 0.1% threshold note, docs, and package wiring |
| `pnpm -F acgi-ai run test:tthw` | Pass: verifies `hello-world.sh`, `hello:world`, `hello:world:local`, scheduled `tthw.yml`, docs, security invariants, and CI readiness references |
| `pnpm -F acgi-ai run hello:world:local` | Pass: local HTTP shell smoke for `/` and `/console`; skips install, allows local Node drift, and does not provide headless browser or production deployment proof |
| `pnpm -F acgi-ai run test:msw-node` | Pass: verifies MSW node-mode setup, shared handler reuse, strict `onUnhandledRequest: 'error'` for node/eval mode, package wiring, security invariants, and CI readiness references |
| `pnpm -F acgi-ai run smoke:bus-proxy` | Pass: Docker `caddy:2.10.2-alpine` served `/healthz` and proxied `/api/v1/console-summary` to a local stub bus with `X-ACGS-Schema-Version: v1` |
| YAML parse for console workflow + Cloud Run manifest | Pass: `.github/workflows/console.yml` and `acgi-ai/infra/cloudrun/service.yaml` parsed with PyYAML |
| `make typecheck-py` | Pass: all registered Python packages declare a `[tool.mypy]` gate and root fan-out uses each package's pyproject scope, including the strict `packages/Acgs-Swarm` core-primitives source scope instead of printing non-fatal legacy errors |
| `make verify` | Pass: JS lint/type/test fan-out and Python lint/type/test fan-out complete without non-fatal mypy error spam; Node 22 warning is not readiness evidence, so frontend readiness claims require `make verify-js-node24` |
| `make build` | Pass: JS builds and Python package builds |

## Valid deferrals / external blockers

| Area | Deferred work | Reason |
|---|---|---|
| Production deployment | Live Vercel/Cloud Run deploy, production header proof, `/healthz` served-hash match, live domain checks | Requires external credentials, configured projects/domains, and deployment authority. Local build and deploy configs are verified, workflow contracts now fail closed when production credentials are missing, `production-authority.example.json` defines the required signed authority packet with `pending-external:deploy-owner-approval` placeholders, `production-evidence.example.json` defines the required proof intake shape, `verify:production-live` defines the post-deploy DNS/HTTPS/healthz/header/Storybook evidence command plus blocker ids, including `storybook-manifest-live` for hosted buyer-evidence manifest proof, `build:production-blocker-report` packages failing live JSON into a local `production-blocker-report` with `copyIntoProductionEvidence`, `build:production-cutover-plan` packages required GitHub secrets, DNS cutover records, `productionLiveBlockers`, and `copyIntoProductionEvidence` into a `production-cutover-plan`, `build:production-evidence-draft` converts saved blocked artifacts into `production-evidence.deployment-blocked.json` with explicit `pending-external:` deploy/run refs, `build:hosted-storybook-handoff` writes `hosted-storybook-handoff.json` with `pending-external:storybook-pages-proof` for Storybook Pages proof, and `validate:production-evidence` verifies a completed manifest plus live JSON/`productionLiveBlockers` after the operator attaches those artifacts; production proof must come from a real deploy run. |
| Frontend production auth | OIDC/WorkOS/Auth0 production session boundary from `acgi-ai/PLAN.md` | Requires provider/project choice, secrets, callback domains, and product decision. The local `AUTH_UPSTREAM` forward-auth deploy contract now fails closed and blocks deploy without an authorizer secret, and `/auth/status` gives the SPA a provider-agnostic same-origin production session bridge after edge auth succeeds, but provider configuration and staged session proof remain external. |
| Legal review of claim matrix | Legal-reviewed public compliance/security claim matrix | Engineering draft and overclaim gate are local-complete; legal owner review/signoff still requires external authority and cannot be truthfully completed by code-only local work. |
| Third-party penetration test | External pentest required by `acgi-ai/PLAN.md` before production deploy | Requires vendor engagement and external report. |
| Full WCAG/manual screen reader evidence | NVDA/VoiceOver signed manual pass and visual-diff baselines | Static skip-link/landmark/focus/reduced-motion foundation is locally gated; full conformance still requires manual assistive-tech run and browser screenshot baseline workflow beyond this local code pass. |
| Hosted Storybook buyer evidence | Official Storybook runtime and live `storybook.acgs.ai` proof | `storybook-runtime.plan.json` now records the official-runtime dependency plan and keeps `@storybook/react-vite` / `npx storybook@latest init` behind `pending-external:dependency-owner-approval`; it is not official Storybook runtime proof, not hosted Storybook proof, and not production deployment proof. The buyer-evidence publication workflow scaffold now builds a Pages-ready artifact for `storybook.acgs.ai`; the live verifier now requires `storybook-manifest-live` `/manifest.json` proof of the expected buyer-evidence stories, publish target, and conservative claim boundary; `test:hosted-storybook-handoff` locally verifies the `hosted-storybook-handoff` artifact so operators can copy `copyIntoProductionEvidence.hostedStorybook` without treating `pending-external` handoff state as live proof. Official Storybook dependency/runtime, live DNS/Pages proof, browser screenshots, axe, and visual-diff evidence remain pending. |
