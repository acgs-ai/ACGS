# Roadmap

> **Roadmap of record.** This is the single maintained roadmap for the
> repository. Superseded roadmaps and plans are frozen in
> [`docs/archive/`](archive/README.md); do not plan new work from them.

This roadmap is claim-safe: planned work is not described as implemented.

The resumable SaaS beta dependency program and its claim-safe capability view live in
[`docs/saas/DELIVERY_DAG.yaml`](saas/DELIVERY_DAG.yaml) and
[`docs/saas/ACCEPTANCE_MATRIX.md`](saas/ACCEPTANCE_MATRIX.md). Those records refine
delivery dependencies and evidence state; this file remains the only roadmap of record.
The Phase-0 target product contract is specified in
[`saas/PRODUCT_REQUIREMENTS.md`](saas/PRODUCT_REQUIREMENTS.md),
[`saas/ASSURANCE_CLASSES.md`](saas/ASSURANCE_CLASSES.md),
[`saas/OPEN_CORE_BOUNDARY.md`](saas/OPEN_CORE_BOUNDARY.md), and
[`saas/ENTITLEMENT_AND_METERING_MATRIX.md`](saas/ENTITLEMENT_AND_METERING_MATRIX.md).
They are future acceptance contracts, not evidence that the managed service,
entitlements, pricing, or billing implementation exists.
As of the current SaaS DAG snapshot at origin/master
`c20b51ee9ab1d89cfa496330568ca032c34d5022`, PRs #369, #370, and
#371 are merged into current master and provide partial current-local evidence
for project/environment attachment, one signed native agent-create transaction
route, and route-scoped idempotency/outbox behavior. Open clean PRs #391,
#393, #395, #398, and #413 remain current-local stack evidence only; PR #413's
authoritative G015 proof failed and was consumed, so its later green CI is
non-authoritative. These records do not promote the full managed service:
legacy unsigned route aliases, provider wiring, durable idempotency for other
mutating routes, async export jobs, tenant isolation, external exactly-once
delivery, enrollment/policy lifecycle completion, and production launch remain
open.
The companion Phase-0 architecture packet is
[`saas/ARCHITECTURE.md`](saas/ARCHITECTURE.md),
[`saas/THREAT_MODEL.md`](saas/THREAT_MODEL.md),
[`saas/API_AND_DATA_CONTRACT.md`](saas/API_AND_DATA_CONTRACT.md), and
[`saas/MIGRATION_VERSIONING_COMPATIBILITY_POLICY.md`](saas/MIGRATION_VERSIONING_COMPATIBILITY_POLICY.md),
with proposed owner-gated [identity](adr/saas-identity-oidc-saml-scim-build-vs-buy.md),
[KMS](adr/saas-kms-build-vs-buy.md),
[object-retention](adr/saas-object-retention-build-vs-buy.md),
[independent-witness](adr/saas-independent-witness-build-vs-buy.md),
[billing](adr/saas-billing-build-vs-buy.md), and
[commercial-boundary](adr/saas-commercial-module-boundary.md) ADRs. It records
future trust, migration, and decision requirements; it is not evidence that a
managed plane, provider, retention policy, witness, identity federation, or
commercial program exists.

Status legend: **✅ SHIPPED** — every acceptance sub-clause has a source file *and* a cited test; **🟡 PARTIAL** — some sub-clauses met (the missing one is named in Status evidence below); **⬜ PLANNED** — forward-looking, not implemented yet. The stage table below is the historical local-kernel roadmap snapshot verified against `master` @ `941e398cb266b29b88325a58605a16008d2af63c` (see Status evidence); the current SaaS beta DAG/matrix snapshot is separate and frozen at `c20b51ee9ab1d89cfa496330568ca032c34d5022`.

| Stage | Status | Why it matters | Acceptance test | User impact | Risk if skipped |
|---|---|---|---|---|---|
| Local receipt-gated kernel | ✅ SHIPPED [1] | Establish the invariant locally. | Smoke proof, receipt-gated demo, executor/audit/receipt tests pass. | Evaluators can prove `No valid Decision Receipt, no side effect` on a laptop. | Project sounds like governance theory without runnable proof. |
| Secure profile: signature-required gate by default | ✅ SHIPPED [2] | Close recomputed-hash residual for production-adjacent pilots. | Default gate rejects unsigned receipts; explicitly configured signed issuance and trusted-verifier tests pass. | Operators get a fail-closed secure gate default without implied automatic key setup. | Unsigned dev mode may be misused as production assurance. |
| Replay evidence: audit verification plus conditional re-derivation | ✅ SHIPPED [3] | Prove longer workflows/evidence bundles without overstating retained data. | Audit-only replay verifies chain/event integrity and policy-version consistency; separate side-store tests re-derive only with retained raw calls and the matching original policy. | Reviewers can distinguish evidence integrity from reproduced policy decisions. | Missing/redacted side records or policy bundles limit review to audit-only verification. |
| Integration: MCP and agent-framework adapters | ✅ SHIPPED [4] | Put the gate where teams already run agents. | Conformance tests for MCP gateway, OpenAI-style tool wrapper, LangGraph node/tool wrapper. | Faster adoption without replacing existing agent stacks. | Integrators may wire the gate incorrectly or bypass it. |
| Reference integration: real agent platform (e.g. Mattermost Agents MCP; **candidate:** flue `outboundByHost` egress callback) | ⬜ PLANNED | Prove the MCP gateway against a widely-deployed agent surface, not just local examples. | A real platform's agent tool-calls (`create_post`, `create_channel`, file ops) route through the gate; fail-closed when no receipt; `expected_actor` bound to the platform's authenticated user; bypass-path documented. The same fail-closed / `expected_actor` acceptance applies to a flue `outboundByHost` integration (call kernel → forward on `ALLOW` with the signed receipt → fail closed on `DENY`). | Operators see the gate working on a production agent stack they recognise. | MCP-adapter conformance stays illustrative; adopters can't see a concrete end-to-end wiring. |
| Policy: signed bundles and versioned policy registry | 🟡 PARTIAL [6] | Stop policy-bundle substitution and stale policy confusion. | Bundle signatures, active/stale/revoked states, tenant binding tests. | Clear operational policy lifecycle. | Receipts may bind hashes but operators lack lifecycle controls. |
| Evidence: exportable proof packs | ✅ SHIPPED [5] | Make review, buyer diligence, and incident response concrete. | CLI exports receipts, audit chain, replay verdicts, limitations, and manifest; tamper tests fail. | Reviewers can inspect artifacts without trusting prose. | Claims remain hard to verify externally. |
| Operations: production deployment profile | ⬜ PLANNED | Move from local proof to production-adjacent operation. | Deployment guide, health checks, audit sink, key management, fail-closed config, rollback drills. | Teams can run pilots with known responsibilities. | Local demo may be mistaken for production readiness. |
| Review: external security review | ⬜ PLANNED | Independent challenge of threat model and implementation. | Published review scope, findings, remediations, residual risks. | Higher buyer/security confidence. | Internal tests may miss design or threat-model gaps. |
| Ecosystem: standard receipt schema for agent runtimes | ⬜ PLANNED | Make receipts portable across frameworks. | Versioned schema, compatibility tests, reference validators in multiple hosts. | Agent runtimes can share governance evidence. | Fragmented receipts make integration harder. |
| Skill trust: identity, permission ceilings, host interception | ⬜ PLANNED | Bring agent skills (`.claude/skills/**`, `.agents/skills/**`) under governance before their declared permissions can be enforced (steps 4-6 of [`design/agent-skill-trust-pipeline-adoption.md`](design/agent-skill-trust-pipeline-adoption.md)). | Trusted per-skill identity (name/version/artifact digest), the artifact digest computed over a versioned, collision-resistant canonical tree manifest binding every entry's normalized relative path, entry type, relevant mode bits, and content length and hash under an unambiguous length-delimited encoding, with the manifest scheme version bound into ceiling records and receipts and a structural-substitution negative test proving a tree that permutes the same content across different paths or entry boundaries yields a different digest and is rejected, manifest paths canonicalized per supported host filesystem (case folding and Unicode normalization included) with any manifest whose entries collide after canonicalization for the target host rejected, proven by a cross-platform path-collision negative test, manifest and archive entries that are absolute or whose normalized paths escape the skill root rejected and extraction confined descriptor-relative beneath the skill root, with absolute-path and parent-traversal negative tests proving an escaping entry is rejected rather than materialized outside the snapshot root, extraction itself bounded by admission-time resource limits (maximum entry count, per-entry and total uncompressed size, sparse-file materialization, and decompression ratio) failing admission closed when exceeded, with an archive-bomb negative test proving a high-ratio or many-entry archive is rejected during admission without exhausting the admitting host, a maximum compressed/container input size with snapshot parsing and extraction run under enforceable CPU, memory, temporary-storage, and deadline budgets failing admission closed when exhausted, with a parser-resource negative test proving an oversized or parser-pathological container is rejected within those budgets without exhausting the admitting host, security-relevant filesystem metadata (ownership, POSIX ACLs, security labels, extended attributes such as Linux `security.capability`) either stripped and normalized to a fixed approved baseline at snapshot construction or bound into the manifest digest and rechecked when the snapshot is constructed, with a metadata-substitution negative test proving an artifact whose bytes match the approved digest but whose security-relevant metadata differs is rejected rather than executed with that authority, each host mirror of a skill carrying its own full-tree digest over exactly the bytes that host loads, both mirrors tied to a common canonical-source/release identity, with mirror parity checked on the normalized instruction body, permission semantics, and every canonical enforcement field (`deny_behavior` and normalized invocation semantics included), never on digest equality across mirrors, and a mismatched-mirror negative test proving mirrors identical in body, permissions, and release identity but divergent in `deny_behavior` or invocation semantics fail parity; independently reviewed permission ceilings held outside the skill and outside agent write authority, authenticated and version-bound to the skill digest they approve, and validated against trusted freshness state (an active-version/revocation record or monotonic counter outside agent write authority identifying the currently approved ceiling-and-skill pair), with tamper and rollback tests at both issuance and execution, including rollback of a previously valid ceiling-and-skill pair as a matched set, every ceiling record and active-pair entry additionally bound to the tenant, `project_id`, `environment_id`, execution boundary, and applicable host or executor profile it was approved for (project and environment being distinct trust dimensions in the v2 receipt model that staging and production can otherwise share), rechecked against the requesting deployment context at both issuance and execution, with cross-tenant, cross-project, cross-environment, and cross-boundary substitution negative tests proving a ceiling approved in one tenant, project, environment, or execution boundary cannot authorize a receipt issued or executed in another, and revocation either effective against running work (process trees and brokered resources launched under a ceiling-and-skill pair tied to a revocable capability lease the host closes when the active pair changes, lease acquisition serialized with the revocation transition (one transaction, or an epoch check at registration failing closed when the pair changed since validation) so a request validated against the outgoing pair cannot register a fresh lease and launch after revocation, with negative tests proving a still-running child process's file and network effects are cut off after revocation and a concurrent revoke-versus-launch race proving no stale process survives or starts regardless of interleaving) or explicitly scoped to future launches only, with that residual risk recorded in the ceiling record and receipt and a test documenting that a pre-revocation process retains its capabilities until it exits; host interception routing governed skill capabilities through `execute_with_receipt`, with declared `permissions:` enforced deny-only against the ceiling and the permission intersection re-evaluated against the final executable arguments after any policy `TRANSFORM`, denying when transformed arguments cross the ceiling, and every security-relevant by-reference input named by those final arguments (mutable content outside the immutable skill snapshot) hashed or snapshotted at authorization with that digest or snapshot identity recorded in the receipt and the handler consuming exactly those verified bytes from a sealed or copy-on-write byte snapshot materialized at authorization or with writes to the underlying object provably excluded through an immutable seal or exclusive write lease held from authorization through the handler's read (an already-resolved descriptor alone is insufficient because it pins the inode, not the bytes), with a negative test replacing an authorized by-reference input's content between authorization and handler read and an in-place overwrite race test writing the authorized inode through another permitted descriptor in that window, proving in both cases the handler consumes the authorized bytes or the execution is denied, never the swapped content; a missing, malformed, or unrecognized permission declaration failing admission for skills in the governed set (no fallback to the approved ceiling, no receipt), with a real-handler negative test proving a governed skill lacking a valid declaration produces no receipt and no side effect, and the governed set defined by loadability rather than content classification or invocation path: every loadable skill is governed whether model-selected, implicitly invoked, or explicitly invoked by the user (`disable-model-invocation` removes model selection, not loadability), documentation-only skills carry an explicit deny-by-default/no-capability declaration (an empty permission set) rather than an exemption, and real-handler negative tests prove a tool call originating from a documentation-classified skill with a no-capability declaration produces no receipt and no side effect rather than being governed under the agent's non-skill ceiling, and a tool call directed by a manual skill the user explicitly invoked is governed under that skill's declaration and ceiling and denied when the skill lacks a valid declaration; one versioned permission-language grammar and canonicalization shared by the loadability gate, host admission, receipt issuance, and the executor (a single parser or conformance-tested equivalent), with every declaration reduced to canonical form before use, the language version and canonical effective permission set bound into ceiling records and receipts, unknown or noncanonical forms rejected fail-closed, and parser-differential negative tests proving a pattern interpreted differently by two components (recursive-glob versus literal or prefix-match readings) is rejected or yields identical granted authority at every consumer, never a wider grant at the executor than at admission; a fail-closed `deny_behavior` mapping (an absent or unrecognized value means stop, never proceed) carried into the canonical skill contract, with human approval never reviving the original `DENY`/`ESCALATE` receipt: a granted approval triggers a freshly evaluated request producing a new `ALLOW`/`TRANSFORM` receipt that is the only thing the executor accepts, and the approval grant is authenticated to the granting human, bound to the exact request it approves (actor, skill identity/digest, action, final arguments) and to the originating decision (the denied/escalated request's audit hash plus the tenant, execution boundary, `project_id`, `environment_id`, and policy bundle/version in force, the decision serialization extended to carry both deployment dimensions so the bound audit hash covers them and the trusted approval channel rendering both to the approving human, with per-field mismatch negative tests, cross-project and cross-environment grant substitution included, proving the grant cannot be consumed for a different decision), the approval collected through a trusted approval channel that renders the decision directly from the payload being signed (actor, skill identity/digest, action, final arguments, resolved target, and referenced-content digests taken from the canonical bound fields, never from caller-supplied prose), with a mismatched-display negative test proving an approval prompted with a caller-supplied label or summary that disagrees with the bound request fields yields no consumable grant and no side effect, the trusted channel rendering every security-relevant field bound into the payload being signed (effective credential principal, resolved ambient target context, versioned enforcement and implementation identities, and security-relevant ambient configuration values such as `DEPLOY_MODE` included, or a trusted semantic representation derived from those bound values) and refusing to collect a signature over a payload carrying a bound behavior-defining field it did not render, with a hidden-field substitution negative test proving an approval whose rendered request fields all match but whose unrendered bound field carries a destructive value yields no consumable grant and no side effect, the trusted channel rejecting bound fields carrying terminal control sequences, newlines, or Unicode bidirectional-control code points or rendering every unsafe code point as an unambiguous visible escape without changing the bound value, with an approval-spoofing negative test proving ANSI and bidirectional-control payloads cannot hide or visually reorder the rendered destructive value and a signature collected over a spoofed rendering yields no consumable grant, secret-bearing final-argument fields (inline credentials such as authorization headers, password fields, or token values) classified exactly as secret-classified ambient inputs and bound through a secret-store reference or keyed commitment verified inside the trusted gate, the approval channel rendering only a non-disclosing designator of the secret and receipts, ceiling records, and audit events carrying the same non-disclosing binding, with non-disclosing rendering and serialization negative tests proving an inline-credential argument's approval, receipt, ceiling record, and audit events emit neither the secret value nor an offline-guessable digest of it while a substituted or mismatched secret reference still yields no consumable grant and no side effect, the active policy bundle identity and version held in monotonic rollback-refusing freshness state outside agent and child-process write authority (the same trust class as the ceiling freshness records and the grant ledger) and validated at receipt issuance, grant consumption, and execution, failing closed when the bundle in force is older than the recorded active version or the state is unavailable, with a matched bundle-and-binding rollback negative test proving a restored older permissive bundle and its restored active tenant binding cannot yield an executable receipt or side effect for a request the current policy denies, the freshness record and receipt bound to a content digest computed over the policy's code, configuration, dependencies, and applicable runtime (not its declared identity and version alone) with evaluation running only over immutable verified bytes matching that digest, and a same-version mutation negative test proving a policy bundle or custom policy implementation mutated in place while retaining its id and version yields no executable receipt and no side effect for an action the reviewed bytes denied, secret-classified policy configuration (a credential, token, private allowlist value, or other low-entropy secret among policy construction inputs) bound through a secret-store version identifier or keyed commitment verified against the captured bytes inside the isolated evaluator, with freshness records, receipts, and ceiling records carrying only that non-disclosing binding alongside the ordinary digest over the non-secret remainder, and a policy-secret serialization negative test proving no freshness record, receipt, or ceiling record emits the secret value or an offline-guessable digest of it while substitution of the secret configuration between review and evaluation is still detected and refused without a side effect, policy evaluation isolated from mutable runtime state (each evaluation running against a stateless or freshly instantiated policy constructed only from the verified bytes and bound configuration, or persistent evaluation state captured in an authenticated snapshot whose identity is bound into the freshness record and the receipt and validated at issuance, consumption, and execution, failing closed on mismatch), with a state-mutation negative test proving a policy whose in-memory state changes between evaluations without any change to its code, configuration, dependencies, or version yields no executable receipt and no side effect for an action the approved state denied, policy evaluation run in a capability-isolated environment where every input is explicit and authenticated (no ambient environment, filesystem, database, clock, or network reads reachable from evaluation) or every external input the policy consults captured in an authenticated snapshot whose identity is bound into the freshness record and the receipt and validated at issuance, consumption, and execution, failing closed on mismatch or on any external read outside the snapshotted set, with an external-state substitution negative test proving that altering an environment variable, file, or other external input the policy reads between approval and evaluation, while its code, configuration, dependencies, and version are unchanged, yields no executable receipt and no side effect for an action the approved external state denied, policy-freshness validation serialized with receipt consumption and launch against the active-policy transition via a policy-version lease or epoch check acquired atomically with validation (one transaction, or an epoch recheck at launch failing closed when the active policy changed since validation), with a concurrent policy-update-versus-launch negative test proving no interleaving lets an already-validated request run its side effect under a superseded policy that newly denies the action, the policy-version lease held until the governed effect commits or completes (or the launched work kept revocable or brokered so a policy transition revokes or re-validates in-flight work before its effect commits), with a delayed post-launch effect negative test proving a launched process's governed effect never commits under a superseded policy, bound to the versioned enforcement and implementation identities in force at approval (the active ceiling record and version, executor profile identity/version, permission-language version, and the admitted handler deployment digest the request resolves to) and rechecked at consumption against the identities active at fresh evaluation, failing closed on any change, with a context-substitution negative test proving a grant whose active ceiling or admitted handler was substituted after approval is rejected without a side effect, the grant and its approval evidence also binding the handler's security-relevant ambient configuration snapshot (the resolved values or content digests of the feature flags, environment values, configuration defaults, and service-discovery records enumerated at handler admission), rechecked at consumption against the currently resolved configuration and failing closed on any change, with an approval-to-consumption configuration-substitution negative test proving a grant is refused without a side effect after an ambient flag or service-discovery record changes between approval and grant presentation, the grant and its approval evidence also binding the effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch) the admitted handler's ambient credential resolved to at approval, rechecked at consumption against the currently resolved principal and failing closed on any change, with a credential-substitution negative test spanning approval to grant presentation proving a grant approved under a staging principal is refused after the handler's ambient credential is repointed to production, the grant and its approval evidence also binding the resolved ambient target context (the endpoint, cluster or project, region, namespace, and equivalent target identifiers) the admitted footprint resolved to at approval, rechecked at consumption against the currently resolved target and failing closed on any change, with an approval-to-consumption target-substitution negative test proving a grant approved while the default target designates staging is refused without a side effect after the target context is repointed to production before grant presentation, the grant and its approval evidence also binding the content digest or immutable snapshot identity of every mutable file or security-relevant by-reference input the reviewed request designates, re-resolved at consumption with the grant rejected when the current content no longer matches what the human reviewed, and a reviewed-content substitution negative test proving a grant approved for a request naming a mutable manifest is refused without a side effect after that file's content is replaced, consumption atomic with capture (each reviewed reference resolved once into an immutable verified snapshot or read and verified through a protected handle, with exactly those verified bytes bound into the receipt and consumed at execution, never a second independent read of the mutable path) and a concurrent replace-versus-consume negative test proving no interleaving of content replacement and grant consumption yields a receipt or side effect over bytes that differ from what was reviewed, the trusted channel rendering for each referenced-content binding the referenced content itself, a trusted semantic summary, or a reviewable diff derived from exactly the verified bytes the bound digest names (never from caller-supplied prose or requester-computed renderings), refusing to collect a signature over a referenced-content binding it did not render, with a destructive-referenced-content negative test proving an approval prompted with only a digest of destructive pre-approval content yields no consumable grant and no side effect, diff renderings computed only against an independently approved baseline whose digest and approval provenance are bound into the signed payload and displayed alongside the diff (falling back to full-content or trusted-summary rendering when no approved baseline exists, with a first-approval/substituted-baseline negative test proving a diff computed against a new, unreviewed, or attacker-substituted baseline is refused as approval evidence), trusted semantic summaries admissible as approval evidence only when the trusted channel schema-validates the referenced content against a schema whose behavior-relevant fields and sections the summarizer provably renders completely, refusing summary rendering and falling back to the safely escaped full content on failed validation, unsupported schemas or versions, or unaccounted sections, with an omitted-section negative test proving a summary that omits the manifest section performing the destructive operation is refused as approval evidence and only the escaped full-content or behavior-complete rendering yields a consumable grant, the referenced-content, summary, and diff renderings applying the same display-safety discipline as bound fields (referenced content carrying ANSI terminal-control, label-overwriting newline, or Unicode bidirectional-control code points rejected or rendered as unambiguous visible escapes without changing the verified bytes the bound digest names), with a referenced-content spoofing negative test proving a referenced manifest carrying such payloads either fails rendering or is displayed with the destructive operation unambiguously visible and an approval collected over a spoofed rendering yields no consumable grant and no side effect, reviewed by-reference inputs additionally classified for secrecy with a secret-classified referenced input bound through a secret-store version identifier or a keyed commitment verified inside the trusted gate rather than an ordinary content digest, the trusted approval channel rendering only a non-disclosing trusted description (identity, store version, and metadata, never the value, an unkeyed digest, or a content diff) and grant evidence, receipts, and ceiling records carrying only the non-disclosing binding, with a secret-reference serialization negative test proving no approval prompt, grant evidence, receipt, or ceiling record emits the value or any offline-guessable digest of it while substitution of the referenced secret between review and consumption is still detected and refused, the originating audit event held or anchored in authenticated rollback-resistant storage outside agent and child-process write authority with grant consumption and execution confirming the bound event remains present and anchored there, and a negative test proving a grant whose bound audit event was deleted or truncated before presentation is refused without a side effect, authenticated rollback-resistant audit retention and execution-time presence verification applied to every executable receipt (each executable receipt's bound decision event held or anchored in the same trust class of storage and confirmed present and anchored at presentation, failing closed when it is not), with a negative test truncating an ordinary receipt's decision event after issuance but before presentation and proving execution is refused without a side effect, expiry-bounded with expiry at issuance, grant consumption, and execution evaluated against a trusted time source (a trusted monotonic clock or signed time attestation held and validated outside agent and child-process write authority) that fails closed when unavailable rather than falling back to the mutable host clock, and a clock-rollback negative test proving an expired but unused approval grant, invocation context, or receipt is still rejected without a side effect after the verifier's host clock is set back before its expiry, and monotonic timestamps bound to a non-reusable boot/time-source epoch identifier with expiry evaluation failing closed across any epoch discontinuity (or fresh, non-replayable signed absolute time required instead), with a restart negative test proving an unused approval grant, invocation context, and receipt that expired before a host restart regain no validity after the monotonic clock resets or rebases, the trusted elapsed-time source suspend-aware (counting suspended and paused intervals via a boottime-class clock or equivalent held in the same trust class) or expiry evaluation requiring fresh non-replayable signed absolute time and failing closed when neither is available, with a pause/resume expiry negative test proving an unused approval grant, invocation context, and receipt whose intended real-time expiry passes during a host suspension or VM pause are rejected after resume without a side effect, recorded as evidence in the new receipt, and consumed atomically at receipt issuance (compare-and-consume or idempotent issuance against shared durable state) so at most one executable receipt is ever minted per grant, the grant ledger itself authenticated, monotonic/rollback-refusing, and held outside agent and child-process write authority (the same trust class as the receipt-consumption store), with a grant-ledger tamper/rollback negative test proving a deleted or rolled-back consumption record does not let a second executable receipt be minted from a consumed grant, with negative tests proving an altered-argument or different-skill grant, an expired grant, a replayed grant, and the original denied/escalated receipt are all rejected without a side effect, plus a concurrent-use test proving two evaluations racing on the same grant yield exactly one executable receipt and at most one side effect; approval bounded below the permission ceiling: a grant resolves only policy-level escalation for requests whose final arguments remain inside every effective permission ceiling and never overrides the declared/approved intersection, with an above-ceiling-with-valid-approval negative test through the real handler proving a request outside the ceiling yields no executable receipt and no side effect despite an otherwise valid grant, grants restricted to an explicit `ESCALATE` or a decision the policy itself marks approvable, with hard `DENY` (absolute controls such as tenant isolation or forbidden destructive actions) remaining denied regardless of any grant and a real-handler negative test proving a hard-denied request accompanied by an otherwise valid approval grant yields no executable receipt and no side effect; the approval grant issued by an authorized approver distinct from the requesting actor (preserving the proposer/validator separation `escalation.py::approve_escalation` already enforces), with a self-approval negative test through the real handler proving a grant issued by the requesting principal for its own request is rejected; independent-human approval compared by an authoritative stable human-subject identifier bound into both credentials at authentication (failing closed when that mapping is unavailable), with a same-human negative test proving a request submitted under one credential and approved under a second credential mapped to the same human subject is rejected, and principal-level-only separation explicitly recorded as a limitation in the ceiling record, approval evidence, and receipt where no authoritative human-subject mapping exists; the approver's credential and role identifiers bound into the grant and revalidated at consumption against fresh, monotonic, rollback-protected approver-authorization and credential-revocation state (the same trust class as the grant ledger), failing closed on a revoked credential, withdrawn role, or unavailable state, with a post-revocation negative test proving a grant signed by a since-revoked approver credential or issued under a since-withdrawn approver role is rejected at consumption without a side effect, and approver-state validation, the grant's compare-and-consume, and receipt issuance serialized against approver credential and role transitions (an approver-authorization epoch or lease acquired atomically with consumption and rechecked at issuance commit) so issuance either commits before a revocation takes effect or fails closed, with a concurrent revoke-versus-consume negative test racing an approver credential or role revocation against grant consumption and proving either the executable receipt was issued before the revocation committed or consumption is refused without a side effect, never both; approval-gated receipts binding the approver's credential and role identifiers and the approver-authorization epoch under which the grant was consumed, with the execution gate revalidating that state against the same fresh, monotonic, rollback-protected approver-authorization and credential-revocation store, serialized with receipt consumption and launch under the same epoch-or-lease discipline, failing closed when the credential is revoked, the role withdrawn, or the bound epoch superseded, with a negative test revoking the approver credential after receipt issuance but before the receipt is first presented and proving the receipt is refused at the execution gate without a side effect, and the approver-authorization lease held until the governed effect commits or completes, or launched work kept revocable or brokered so approver credential or role revocation revokes or re-validates in-flight and queued work before its effect commits, with a delayed-effect approver-revocation negative test revoking the approver credential after launch but before a deferred side effect and proving the effect either committed before the revocation or never occurs; every receipt binding the requesting actor's credential and session identifiers and the actor-authorization epoch under which it was issued, with the execution gate revalidating that state against fresh, monotonic, rollback-protected actor-authorization and credential-revocation state, serialized with receipt consumption and launch under the same epoch-or-lease discipline and failing closed when the credential, role, or session is revoked or that state is unavailable, with a post-issuance actor-revocation negative test proving an unused receipt is refused after the requesting actor's credential, role, or session is revoked and a concurrent revoke-versus-execute negative test proving either the side effect committed before the revocation took effect or execution is refused without a side effect, never both, the actor-authorization lease held until the governed effect commits or completes (or the launched work kept revocable or brokered so an actor revocation halts or re-vets in-flight work before its effect), with a delayed-effect actor-revocation negative test revoking the requesting actor after launch but before a deferred side effect and proving the effect either committed before the revocation or does not occur; the approval-gated receipt itself consumed atomically at the execution gate against shared durable state (a receipt-consumption ledger, not a stateless `execute_with_receipt`), with a concurrent-replay test of the issued receipt proving its side effect runs at most once; every skill-originated receipt (not only approval-gated ones) bound to its live invocation (the origin context's session binding and its nonce or handle identity), and every executable receipt regardless of origin class (skill-originated, approval-gated, or an ordinary authenticated non-skill tool call) consumed atomically at the execution gate against shared durable state of the same class, with concurrent-replay negative tests presenting an ordinary non-approval skill receipt and an ordinary authenticated non-skill receipt in parallel and proving each side effect runs at most once, non-skill receipt issuance atomic and idempotent over a trusted request nonce or idempotency key bound into the receipt (so a retried request replays the already-issued receipt rather than minting a new one), the trusted request identity existing before any first attempt is admitted as a per-operation identity rather than a content digest (a stable per-operation idempotency identity supplied by the caller's authenticated transport with every first attempt, unique per intentional operation even when actor, action, and arguments are identical, atomically mapped to at most one issuance before minting, with a defined retry lifetime retained at least as long as the mapped receipt remains executable, keyless requests rejected at admission, never deduplication solely on an actor/action/argument digest and never a key minted and returned only on first issuance), with a concurrent retry-to-issuance negative test proving parallel retries of one authenticated non-skill request yield at most one executable receipt whose side effect runs at most once, a keyless initial-attempt negative test proving duplicate authenticated first attempts carrying no per-operation identity are denied without minting any executable receipt, and a distinct-operation negative test proving two intentional operations with identical actor, action, and arguments but distinct per-operation identities each obtain their own executable receipt while a duplicate within its retry lifetime replays rather than re-mints, receipt expiry never making the operation's identity reusable (the identity-to-issuance mapping decaying into a non-reusable tombstone retained beyond receipt expiry, or the per-operation identity drawn from a monotonic delivery sequence whose already-consumed positions are permanently refused, held in the same authenticated, rollback-refusing trust class as the consumption ledger), with a delayed-duplicate negative test proving the same per-operation identity presented after the mapped receipt has expired and its retry lifetime elapsed mints no new executable receipt and runs no side effect, the replay protection representable in bounded authenticated state (monotonic identities collapsing consumed positions into a bounded watermark plus at most a bounded reordering window, a transport-enforced maximum redelivery horizon after which tombstones are safely discarded, or another authenticated replay structure bounded independently of operation count, with transports supplying neither ordering nor a verified redelivery horizon rejected at admission) and a replay-store growth negative test proving the authenticated replay state stays within its declared bound under sustained distinct-operation traffic while a duplicate at or before the watermark or horizon is still refused, consumption state kept in an authenticated, monotonic store that refuses rollback, held outside agent and child-process write authority, with a negative test proving tampering with or rolling back the ledger before replaying a consumed receipt does not make that receipt executable again, the consumption state representable in bounded authenticated form (authenticated rollback-refusing compaction discarding a consumption record only after the receipt's trusted suspend-aware expiry has verifiably passed and expiry alone suffices to refuse it, a bounded expiry-window structure retaining individual records only within the maximum receipt lifetime, or another rollback-refusing representation whose size is independent of lifetime request count, never compaction that forgets a still-executable receipt's consumption), with a consumption-store growth negative test driving sustained unique receipt issuance and consumption and proving the authenticated consumption state stays within its declared bound while a still-valid consumed receipt replayed after compaction and an expired receipt are both refused without a side effect, the execution gate additionally revalidating invocation liveness at consumption (the bound session or handle still active) with receipt expiry capped to the originating invocation's lifetime, and a negative test proving a receipt first presented after its originating invocation has ended is denied without a side effect, liveness validation, receipt consumption, and launch serialized against the invocation-termination transition via a revocable lease tied to the invocation handle, acquired atomically with the liveness check (one transaction, or an epoch check at consumption failing closed when the invocation ended since validation) and closed by termination, with a concurrent end-versus-launch negative test proving that racing invocation termination against receipt consumption never launches the side effect after the invocation has ended, regardless of interleaving, and the invocation lease effect-held so the launched work's brokered capabilities and not-yet-committed effects stay bound to it through effect completion with termination revoking or forcing revalidation of any deferred effect, proven by a deferred-effect negative test terminating the invocation after launch but before a deferred side effect and proving the effect is revoked or revalidated rather than produced under the ended invocation's authority; the authenticated skill identity/digest, the approved ceiling, and the declared/effective permission set bound into the Decision Receipt at issuance and rechecked at the executor gate, so a receipt issued for a skill-governed call cannot authorize execution without proving which skill ceiling governed it; issuance tests proving these bindings are present in skill-originated receipts, executor tests rejecting receipts whose skill bindings are missing or do not match the intercepted request, and tamper tests proving a mutated skill digest, ceiling, or permission set invalidates the receipt; skill-trust receipts signed at issuance (starting from the kernel's opt-in Ed25519 signing mode) and verified by a trusted verifier at the executor gate against key material outside agent write authority, with a recomputed-forgery negative test proving a receipt whose skill fields were altered and re-hashed is rejected, the receipt-issuance private key additionally held outside agent and child-process read authority (an isolated signer process/service or a non-exportable keystore/hardware-backed key that governed agents and their spawned processes can neither read nor invoke directly, reachable only through the issuance path), with a key-exfiltration/unauthorized-signing negative test proving a shell-enabled skill granted ambient credentials can neither read the issuance key material nor obtain a validly signed receipt outside the issuance path, every skill-trust receipt binding the signing key's identity and issuance purpose into the signed content and the execution gate consulting fresh, rollback-protected signer-trust state (a monotonic revocation/active-key record held outside agent write authority, of the same trust class as the ceiling freshness records; `execute_with_receipt`'s `revoked_keys` parameter is the starting point) so receipts signed by retired or wrong-purpose keys are rejected, with a retired-key negative test proving a receipt minted after its signing key was retired is rejected without a side effect even by an executor whose local signer-trust state is stale or rolled back, signer-trust validation, receipt consumption, and launch serialized against the active-key transition via a signer-trust lease or epoch acquired atomically with validation and held through launch (revalidated or failed closed when the epoch changes before the launch commits), with a concurrent revoke-versus-launch negative test racing key retirement against an in-flight receipt validated under that key and proving the launch either commits before retirement takes effect or is refused without a side effect, the signer-trust lease held until the governed effect commits or completes (or the launched work kept revocable or brokered so key retirement revokes or re-validates in-flight work before its effect commits), with a delayed-effect retirement negative test retiring the signing key after launch but before a deferred side effect and proving the effect either committed before the retirement or never occurs, skill-governed receipts carrying a new receipt schema version or a critical purpose/extension marker that a verifier must reject when unsupported, with a compatibility test proving the current pre-skill executor fails closed on a skill-governed receipt rather than executing it under pre-skill semantics, the result otherwise explicitly limited to non-adversarial unsigned development use; plus negative-path tests through the real host handler (not only direct policy/executor unit calls) proving the side effect did not run for a forged or mismatched skill digest, an above-ceiling action, an action inside the approved ceiling but outside the skill's own declared `permissions:` (proving the effective set is the intersection of declaration and ceiling, so declarations narrow the ceiling rather than remaining documentation-only, with the effective intersection verified in the receipt), a policy `TRANSFORM` that rewrites an initially within-ceiling request into above-ceiling final arguments, a request missing its authenticated origin, and a skill-originated request that substitutes or reuses an authenticated non-skill origin, proving origin-class mismatch is rejected rather than governed under the agent's broader normal ceiling; a positive end-to-end case through the same real host handler in which an authenticated skill-originated action inside both the declared set and the approved ceiling produces a correctly bound receipt and the side effect runs exactly once, proving the pipeline admits allowed work rather than passing its negative tests by rejecting everything; defined shell containment semantics: a shell grant is treated as granting the spawned process's ambient file and network capabilities unless the launch runs under an OS-level sandbox or capability-brokered executor that materially enforces narrower ceilings, `shell.allowed_scripts` enforced by an argv-based launcher that directly resolves and executes the approved executable from the immutable snapshot with caller-supplied arguments (never through shell interpretation of a command string) or a parser rejecting wrappers, substitutions, redirections, and command chaining, with a negative test proving a command string referencing an allowed script with an appended command is denied rather than launched, code-bearing arguments to allowed scripts (eval expressions; module, plugin, or code-evaluated configuration paths) constrained by per-script argument schemas reviewed at admission that reject command/code/plugin inputs or by recursively admitting and content-binding every executable input the arguments can name before the receipt is issued, with a negative test proving an allowed script invoked with a code-bearing argument naming unadmitted code is denied rather than launched, allowed-script standard input closed or null by default with any legitimately consumed stdin bound and validated as exact bytes under the per-script input schema (content-bound like by-reference inputs), with a stdin-code negative test proving code delivered on standard input to an allowed script is never evaluated, the loadability gate records the containment capabilities each declaration requires as machine-readable requirements (rather than statically deciding enforceability it cannot know), host deployment/admission and the executor at execution time each fail closed when the active executor profile cannot supply the recorded requirements, the executor profile itself derived from the actual dispatching executor's capabilities or held as an authenticated, versioned record outside agent and child-process write authority with its identity and version bound into the receipt and rechecked at execution, with a profile-tampering negative test proving a forged or edited profile cannot satisfy the containment requirements, the active profile version held in monotonic rollback-refusing freshness state with any older authenticated profile rejected at admission, grant consumption, and execution, and a matched profile/configuration rollback negative test proving a downgraded executor presenting a restored older authenticated profile fails closed rather than launching uncontained, profile validation, the containment-requirement check, and launch serialized against profile transitions via a capability lease or profile epoch acquired atomically with validation and held through launch (revalidated or failed closed when the epoch changes before the launch commits), with a concurrent downgrade-versus-launch negative test proving a launch racing a profile or capability downgrade either runs under the still-enforced capable profile or is refused, the selected containment held immutable for the entire descendant/effect lifetime or the profile lease held revocably until the governed effect commits or completes so a profile or capability downgrade revokes or re-validates in-flight work before its effect commits, with a delayed post-launch effect negative test downgrading the profile after launch but before a deferred write or connection and proving the effect either runs under the still-enforced containment or never occurs, and any claimed sandboxed or brokered enforcement is proven by end-to-end negative tests on the transitive effects themselves (the spawned process's denied write outside the allowed directory, its denied network connection, its denied environment token and credential-handle use), not only on the launch arguments, with inherited credentials treated explicitly (the shell grant either defined as granting the ambient environment and credential handles the process inherits, or the sandboxed/brokered launch running with an allowlisted environment and closed inherited descriptors and credential-agent handles, with negative tests proving the spawned process can neither read a denied environment token nor use an inherited credential handle), shell grants, their approvals, and their receipts binding the effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch) each granted ambient credential resolved to at authorization, re-resolved atomically with launch, failing closed on a different principal, scope, or epoch, and materialized as an immutable credential instance (a materialized session, token, or non-reswitchable handle) that is the only credential the spawned process inherits or consumes, with a shell credential-substitution negative test proving an allowed script authorized while the ambient credential resolved to the staging principal is refused launch after that credential is repointed to production and a concurrent switch-versus-use negative test racing an ambient-credential repoint against script launch and use, proving the spawned process only ever acts as the pinned verified principal, the pinned credential instance on both the shell and admitted-handler paths tied to a revocable authorization lease held until the governed effect commits or completes, revoked or re-validated when the source credential or role is revoked, or the downstream service required and verified to enforce revocation of the materialized instance, with post-launch credential-revocation negative tests for both paths revoking the source credential or role after launch but before a deferred effect and proving the effect either committed before the revocation or never occurs, the resolved ambient target context (the endpoint, cluster or project, region, namespace, and equivalent target identifiers each granted credential resolves to) bound into shell grants, approvals, and receipts at authorization, resolved atomically with launch, failing closed on mismatch, and pinned into the same immutable credential instance the spawned process consumes rather than re-read from mutable defaults, with a shell target-substitution negative test proving an allowed script authorized while its credential's target designated staging is refused launch after that target is repointed to production under an unchanged principal, scope, and epoch, each allowed script's security-relevant ambient inputs (the environment values, configuration files and defaults, and feature flags its operation depends on) enumerated at admission and bound into the shell grant, approvals, and receipts at authorization (non-secret inputs by resolved value or content digest; secret-classified ambient inputs such as an unnamed secret-bearing configuration file or private feature value by the same secret-store identity-and-version or keyed-commitment representation the admitted-handler secret-ambient-input rule requires, never the resolved value or an unkeyed offline-guessable digest), resolved atomically with launch into the pinned immutable snapshot the spawned process consumes (the allowlisted environment and configuration materialized from the bound values, never re-read from mutable host state), failing closed on mismatch, with a shell ambient-configuration substitution negative test proving an allowed script authorized under a reviewed `DEPLOY_MODE` or equivalent flag or default value is refused launch, or cannot have its executed operation altered, after that value is modified between authorization and launch, and a shell secret-ambient-input serialization negative test proving shell grants, approvals, and receipts for a script consuming a secret-bearing ambient input never contain the secret's value or an unkeyed digest of it while substitution of that input is still refused, and enforceable resource budgets (CPU time, memory, process/PID count, wall-clock runtime, storage, plus open-file/socket descriptor limits and disk and network I/O bandwidth quotas, or explicit executor-profile isolation of those shared descriptor and I/O resources) recorded in the ceiling/executor profile and imposed by the sandboxed or brokered launch on the spawned process and its descendant tree, with shell grants rejected at admission and execution when the executor profile cannot impose the recorded budgets, an end-to-end resource-exhaustion negative test proving a launched process that attempts to exceed its budget (an unbounded fork loop, allocation, or disk write) is terminated or denied and the host-level exhaustion does not occur, and descriptor-exhaustion and I/O-bandwidth-exhaustion negative tests proving a launched process that opens descriptors or sockets in a loop or saturates an allowed disk or network endpoint is throttled, denied, or terminated while other workloads' descriptor and bandwidth availability is preserved, per-invocation and aggregate filesystem-object (inode and directory-entry) quotas imposed on spawned process trees and admitted handlers, or writes confined to an isolated filesystem with a bounded inode pool, with a sequential empty-file exhaustion negative test proving a governed process or handler that creates and closes zero-length files in a loop is denied or terminated while the shared filesystem's object capacity is preserved, and shared admission control with aggregate resource quotas scoped to the skill, tenant, and execution boundary enforced across all concurrent and queued launches attributed to that scope, denying, queueing, or throttling a launch whose admission would exceed the aggregate quota, with a concurrent many-launch exhaustion negative test proving many simultaneous individually-within-budget launches from one skill are collectively bounded and other workloads retain capacity, the aggregate quotas hierarchical with per-skill totals nested under tenant-wide and execution-boundary-wide totals for every budgeted resource (CPU, memory, PID, storage bytes, descriptors, bandwidth, and filesystem-object inode/directory-entry budgets alike) enforced at admission across every concurrent and queued launch attributed to that tenant or boundary regardless of originating skill, with a cross-skill shared-boundary exhaustion negative test proving simultaneous individually-within-quota launches from distinct skills sharing one execution boundary are collectively bounded by the boundary-wide limit, including a cross-skill empty-file exhaustion variant proving distinct skills each within their own filesystem-object quota cannot jointly exhaust the shared filesystem's inode or directory-entry capacity, permitted queued outcomes governed by their own hierarchical queue-length and queued-byte budgets (per-skill totals nesting under tenant-wide and execution-boundary-wide totals) enforced before enqueueing, with over-budget submissions rejected fail-closed or held behind submitter-blocking backpressure and a sustained over-quota submission negative test proving continuous individually valid launches from multiple skills keep dispatcher memory and durable queue storage within the declared queue budgets while other workloads' submissions still make progress, queue admission partitioned sybil-resistantly exactly as the audit quotas require (concurrently admitted child-scope queue allocations collectively leaving a protected reserve of each enclosing allowance, with child-scope creation itself a governed allocation-consuming admission, or scheduling guaranteeing each submitter a minimum reserved queue capacity no set of other scopes can consume) with backpressure landing only on the exhausting scopes, and the sustained over-quota submission test extended to a continuous submitter and coordinated submissions spread across child scopes whose shares would sum to the enclosing allowance, proving unrelated submitters still enqueue and make progress out of the protected reserve, and atomic count, rate, and value budgets on externally visible actions scoped to the skill, actor, tenant, and action, enforced at admission across all concurrent and queued requests attributed to that scope, with a concurrent many-request negative test proving a low-host-resource but externally effectful handler's aggregate external effect count, rate, or value is bounded even though every individual request is allowed and every host-resource quota is respected, external-effect budgets hierarchical with per-skill and per-actor counters nesting under tenant-wide, target-account-wide, and execution-boundary-wide action totals enforced at admission across every concurrent and queued request attributed to the enclosing scope regardless of originating skill or actor, with a concurrent cross-skill/cross-actor negative test proving simultaneous individually-within-budget requests from distinct skills and actors sharing one tenant or target account are collectively bounded by the enclosing total, admission also debiting a canonical target/effect-class budget shared across every admitted action and alias that can produce the same class of external effect against the same target, the mapping from admitted actions to canonical effect classes declared and reviewed at admission with unmapped externally effectful actions failing admission closed, in addition to the action-specific counters, with a concurrent cross-action negative test proving simultaneous individually-within-budget requests through distinct actions producing the same effect class against one account are collectively bounded by the shared effect-class total, external-effect budget accounting kept in authenticated, monotonic, rollback-refusing state outside agent and child-process write authority, with a counter-reset/rollback negative test proving deleting or rolling back budget state between admitted requests does not allow the aggregate count, rate, or value bound to be exceeded, rate reservations evaluated against the same trusted, epoch-bound, rollback-resistant time source as grant and receipt expiry, failing closed across clock or epoch discontinuities rather than opening a fresh window, with a host-clock manipulation negative test proving advancing or rolling the host clock does not admit requests beyond the aggregate rate bound, the external-effect budget reserved atomically against the final transformed action and arguments recomputed after any policy transformation, with the reservation bound into the receipt and a transform-that-increases-value negative test proving a transform that raises a request's count or value beyond the remaining aggregate budget is denied, queued, or escalated rather than run, external-effect reservations following a two-phase lifecycle in the same authenticated rollback-refusing state as the counters (committed only when the effect becomes irrevocable, released atomically by explicit abort or receipt-lifetime-bounded expiry on every provable pre-effect failure such as receipt expiry without consumption, execution-gate denial, or lease revocation, with ambiguous outcomes conservatively retaining the charge), with an abandoned-receipt exhaustion negative test proving a requester that repeatedly reserves budget and abandons, expires, or invalidates its receipts before execution cannot durably exhaust the shared tenant or target-account budget, enclosing external-effect budgets preserving capacity for unrelated submitters through the same sybil-resistant protected-share or fair-scheduling rule as queue and audit capacity (concurrently admitted child-scope reservations collectively leaving a protected reserve of the enclosing allowance with child-scope creation itself a governed allocation-consuming admission, or per-submitter minimum reserved shares no set of other scopes can consume), with the abandoned-receipt test extended to sustained reserve-and-abandon submissions coordinated across multiple actor scopes and proving unrelated requesters in the same enclosing scope still obtain reservations and execute out of the protected reserve, and with the handler path reserving and committing external-effect budget per transitive effect, propagating a downstream idempotency identity so ambiguous-timeout retries cannot double-effect, or conservatively reserving a verified maximum effect count and value per invocation at admission and refusing invocations whose maximum cannot be established, proven by a retry-or-batch negative test driving a handler that batches downstream operations and retries after an ambiguous downstream timeout and proving its actual external effects stay within the reserved budget even though admission observed a single request), brokered shell effects governed identically (each externally effectful brokered operation debiting the scoped external-effect budget individually before issuance, or a verified maximum downstream effect count and value reserved per script invocation at admission and enforced by the direct-network broker, refusing invocations whose maximum cannot be established, with a batching-script negative test proving an allowed script issuing many brokered calls to a permitted origin under one receipt stays within the reserved aggregate count and value), persistent external effects carrying an enforceable lifetime-cost bound at admission (the creation reserving the resource's maximum total lifetime value against the scoped value budget, or the resource held under a revocable budget lease whose renewal debits the scoped budget on a trusted-time schedule and whose expiry or revocation triggers enforced teardown, with creations whose maximum lifetime or total value cannot be established refused fail-closed, and a persistent-resource negative test proving a resource that continues charging after its creation receipt completes stays within the reserved value or is torn down when its lease exhausts), and process-control and IPC channels (signaling or tracing same-UID processes, abstract Unix-domain sockets, shared IPC segments and message queues) either defined as ambient authority the shell grant documents or isolated/brokered by the executor profile (separate PID and IPC namespaces or equivalent brokering of signals, tracing, and shared IPC, plus a separate network namespace or explicit brokering of abstract-socket access, because Linux abstract Unix-domain sockets are scoped to the network namespace and remain host-reachable under PID and IPC isolation alone), with an end-to-end negative test exercising that exact profile and proving the launched process cannot signal, trace, connect to a host abstract Unix-domain socket, or otherwise affect another host process through those channels, and OS identity and privilege controls (a fixed non-privileged UID/GID and supplementary group set, dropped Linux capabilities, `no_new_privs` or the platform equivalent, and denied or brokered setuid/setgid helpers, `sudo`, and user-namespace creation) enforced as executor-profile requirements, with shell grants rejected at admission and at execution when the profile cannot impose them and an end-to-end privilege-escalation negative test proving a launched process attempting each escalation path the host exposes acquires no elevated identity and the out-of-ceiling effect does not occur; host deployment/admission verifying that the active host's skill-schema version matches the version the vendored or repo-local loadability validator is pinned against, failing closed (the skill held un-invocable) on mismatch until a validator matching the active host version has revalidated the tree, with a negative test proving a skill validated only against a stale schema version is not enabled on an upgraded host; skills executed and their bundled resources loaded from a read-only content-addressed snapshot whose digest is bound into the receipt (pre-launch digest revalidation alone is insufficient), with mutation tests between load and launch and after launch proving mutated content is never executed or read, the snapshot and every ancestor directory used to resolve it held outside attacker namespace-mutation authority or loaded exclusively through protected descriptors retained from authentication time, with a snapshot-root substitution race negative test proving swapping the snapshot's directory entry or an ancestor mount between authentication and a later load never executes or reads unverified bytes, snapshot construction and resource loading rejecting symlinks and other references that escape the snapshot (or materializing and hashing their targets inside it, with materialization permitted only for link targets resolved descriptor-relative inside the source skill tree and absolute or escaping targets rejected at admission without ever being opened or read), with a negative test mutating an external link target between authentication and use and an out-of-root symlink-disclosure negative test proving a skill bundling a symlink to an out-of-root secret fails admission with no byte of the target disclosed into the snapshot, the manifest and materialization walk pinning the source root's filesystem identity and refusing to cross any mount boundary encountered beneath it (failing admission closed when a walked entry resides on a different mount than the pinned root) or running entirely inside an immutable private mount namespace constructed before authentication that no outside workload can alter, with a mounted-subtree disclosure negative test proving a bind-mounted out-of-root directory containing a secret beneath a candidate skill tree fails admission with no byte of that content disclosed into the snapshot, no authenticated snapshot, no receipt, and no side effect, snapshot admission rejecting every entry that is not a regular file or directory (FIFOs, Unix sockets, device nodes) unless separately brokered and content-bound as its own reviewed capability, with a bundled-FIFO negative test proving a skill bundling a special inode fails snapshot construction with no authenticated snapshot, receipt, or side effect, snapshot admission rejecting any source entry whose link count exceeds one or safely vetting multiply linked entries before reading them (every link proven to resolve inside the source skill tree, or the entry materialized only from a private single-linked copy whose content the submitting identity could already read), with a hard-linked-secret disclosure negative test proving a candidate tree bundling a hard link to an out-of-root secret fails admission with no byte of the target disclosed into the snapshot and no authenticated snapshot, receipt, or side effect, the snapshot digest explicitly scoped to bundled skill content with the ambient runtime (interpreter, installed packages, `PATH`-resolved binaries) recorded in the approval and receipt as an unverified boundary unless a content-addressed runtime image or interpreter/dependency closure is bound into them; `file_read`/`file_write` directory ceilings enforced at filesystem resolution (descriptor-relative/no-follow opens or an equivalent filesystem sandbox) with symlink-escape and check-to-open path-race negative tests, runtime permission matching and every consumer-path exclusion evaluated against the target filesystem's canonical path identity (case folding and Unicode normalization form included) with a path-alias negative test proving a case-folded or Unicode-normalization-equivalent alias of an excluded path is denied on an aliasing host, writable targets placed in a private namespace or view whose entries outside workloads cannot rename or relocate (results exposed to shared locations only through a controlled publish step) or the governed write serialized against relocation so no outside workload can rename or relocate the target or an ancestor while any written byte remains unpublished, written content landing in private staging that outsiders cannot relocate and published only through a commit step that revalidates the destination's identity beneath the ceiling (a recheck that merely blocks further writes after relocation being insufficient because already-written bytes relocate with the inode), with a rename-after-open negative test proving no written content, bytes already written before the relocation included, ever becomes visible at the denied location when the target is renamed into a denied directory after a validated open, runtime changes to ownership, mode, ACLs, security labels, and extended attributes defined as distinct deny-by-default capabilities never implied by `file_write` or any other filesystem capability (snapshot metadata normalization covering only snapshot construction), granted only through explicit declaration and review and brokered under reviewed constraints naming the permitted targets and attribute transitions, with an ACL/mode escalation negative test proving a skill holding `file_write` over a directory cannot change an in-ceiling file's mode, ACL, ownership, label, or extended attributes to expose it to an outside principal or alter how another process executes it unless the metadata-mutation capability was explicitly declared and approved, resolution additionally refusing to cross mount points within the ceiling (`RESOLVE_NO_XDEV`-style no-cross-mount resolution or equivalent isolation, beneath-style resolution alone being lexical and letting a bind mount or mounted subtree inside an allowed directory expose a denied tree), with a bind-mount escape negative test proving an access through a mount point inside an allowed directory fails to reach the denied tree, the ceiling root itself bound to a protected root descriptor whose mount and inode identity is established and verified before execution (every resolution descriptor-relative to that pinned root) or resolution run inside a private immutable mount namespace, with a root-replacement negative test proving an access after a denied tree is bind-mounted over the ceiling root itself fails closed rather than resolving inside the substituted mount, and hard-link escapes handled explicitly (cross-boundary multi-linked entries rejected or individually vetted before any copy or materialization, copying/copy-on-write alone being insufficient because materialization reads the denied inode and discloses its contents into the sandbox, and a mount namespace or bind-mounted view being insufficient because it preserves the shared inode, with vetting atomic with materialization: the materialized view constructed from a frozen source snapshot read by both the vet and the copy, or each entry validated and copied through the same opened descriptor with concurrent substitution prevented or failed closed), with a cross-boundary hard-link negative test proving a hard link inside an allowed directory to a denied file fails to read or write the outside content and that no copy or snapshot of the denied content becomes readable inside the materialized view, including under a mount-namespace or bind-mount view, and a concurrent swap-versus-materialization negative test proving a vetted entry replaced with a hard link to a denied file between vetting and materialization fails closed without disclosing the denied content, the writable target residing for its entire lifetime in storage where outsiders cannot create links to it (a distinct or access-controlled filesystem whose link operations are exclusively broker-owned, a private mount namespace being insufficient because namespaces isolate mount topology rather than inode link operations on the shared underlying filesystem), or the governed write landing in such broker-private storage and published only through a broker-controlled copy whose destination has that same no-outside-link property for the published object's lifetime, with the link-after-open negative test racing hard-link creation both before and after commit (the post-commit attack issued from a different mount namespace mapping the same underlying filesystem) and proving a link added to the target at any point in its lifetime never makes the governed content readable through the out-of-ceiling path, and directory capabilities restricted to regular files and directories, failing closed on any other inode type (FIFOs, device nodes, sockets) unless a specific special inode is explicitly brokered as its own reviewed capability, with FIFO and device-node negative tests proving the external effect itself does not occur (no command reaches the privileged reader through an in-ceiling FIFO, and an in-ceiling device node is not opened); network ceilings enforced through a direct-network broker that re-validates every redirect hop and the actual resolved destination address against the ceiling and connects using that approved resolution, with redirect-to-forbidden-origin and DNS-rebinding negative tests proving the outside destination is never reached, permission to use a forwarding endpoint (an HTTP CONNECT or SOCKS proxy or equivalent relay) treated as authority over every destination it can relay to or the logical tunnel destination validated and bound against the ceiling by a protocol-aware broker before the tunnel is established, with a proxy-tunnel negative test proving an attempt to tunnel through an allowed proxy to a denied origin fails to reach that origin, the logical network authority (scheme, authority, TLS SNI, and application-level host) bound and enforced at connection use by the direct-network broker or a shared endpoint treated as authority over every virtual host it serves, with a shared-address virtual-host negative test proving a connection to an allowed origin's address presenting a denied origin's SNI or Host value fails to reach the denied virtual host, the broker itself validating the TLS (or equivalent authenticated-channel) peer's certificate chain and hostname against a trusted anchor set or pinned identity for the allowed origin independently of caller-controlled verification settings and refusing the connection when peer authentication fails, with an invalid-certificate/MITM negative test proving a connection to an allowed origin whose peer presents an invalid or mismatched certificate is refused by the broker even when the caller disables verification, with no request data reaching the unauthenticated peer, network authority bound to protocol-specific operations where the protocol expresses them (for HTTP, the allowed methods and resource/path constraints recorded in the ceiling and enforced by the broker on every request) or an origin grant explicitly treated as authority over every operation the entire service exposes, granted only when that full-service reach is itself reviewed and intended, with a same-origin disallowed-operation negative test proving a request to an allowed origin whose method or resource falls outside the recorded operation constraints is refused by the broker rather than sent, inbound listener authority defined as its own capability distinct from outbound origin authority, denied by default for declarations naming only outbound origins and enforced by the executor profile, with a negative test proving another workload cannot reach a listener bound by a process launched under an outbound-only grant, and each request an allowed declared listener accepts (or each externally visible effect it performs) passing through the dispatcher and the same scoped external-effect and rate budgets as directly issued requests, or a verified maximum accepted-operation count reserved for the listener and enforced by terminating it at that bound, with a declared-listener amplification negative test proving externally initiated requests cannot drive unbounded operations under one launch receipt, every operation an allowed listener accepts debiting the scoped external-effect and rate budgets per downstream effect it triggers or carrying a verified maximum downstream effect count and value enforced by terminating the work at that bound, with a single-request fan-out negative test proving one accepted listener request cannot trigger unbounded downstream external effects under the single launch receipt; an authenticated origin/delegation stack carried on skill-originated requests, each loader-issued origin context bound to the host session and the exact tool-call/request digest it authorizes with a short lifetime and a nonce or non-exportable live handle, with stale-context and cross-call replay negative tests rejecting before receipt issuance, receipt issuance atomically compare-and-consuming the invocation context (or idempotent, keyed on the session, nonce, and request digest, returning the already-issued receipt rather than minting another) against shared durable state of the same trust class as the grant ledger so one origin context yields at most one signed receipt, context consumption modeled as an atomic state transition rather than a single-shot flag (issuing a non-executable escalation receipt atomically transitions the bound context into an escalated state tied to that decision's approval grant, from which consuming the matching grant may issue at most one executable approved receipt under the same bound context, after which the context is terminally consumed, every other issuance path failing closed), with a concurrent multi-issuance negative test proving racing or retried issuance on the same fresh or escalated context mints at most one non-executable escalation receipt followed by at most one executable receipt and runs the side effect at most once; skill execution run in an isolated capability-scoped context whose ceiling governs everything the model does within it, or every loaded skill's ceiling conservatively retained and intersected until that model context is discarded (context freshness does not remove skill instructions from the conversation), with a delayed-call negative test proving a tool call deferred until after invocation end is still governed by the skill's ceiling rather than the broader non-skill ceiling, untrusted skills loaded only into least-data isolated contexts containing no data beyond what their invocation requires with the isolated context's response passing a governed output boundary before reaching any broader recipient, or the claim explicitly excluding response-channel confidentiality with the host refusing to load untrusted skills into model contexts holding data classified above the skill's clearance, with a shared-context disclosure negative test proving a skill loaded alongside sensitive foreign data either cannot cause that data to appear in the response delivered to its requester or is refused loading, and output returned from an isolated skill context either terminal (never re-entering any model context as consumable instructions) or propagating the isolated context's full skill stack into every model context that consumes it, intersected and retained until that recipient context is discarded, with a negative test proving a tool call issued by a caller after consuming isolated-context output is still governed by the originating skill's ceiling; ingestion of a registered skill's instruction files into a model context through an ordinary file read (rather than the host's skill loader) treated as a skill activation that propagates the skill's identity and ceiling into the consuming context (intersected and retained until that context is discarded), or ordinary non-loader contexts prevented from consuming registered skill artifacts as instructions at all, with a negative test proving a tool call issued by a context that read a registered `SKILL.md` via an ordinary file read is governed by that skill's ceiling rather than the reader's broader non-skill ceiling, or that the read itself is refused, registered-skill identity propagated through copies and transformations (any artifact derived from a registered skill's instruction files host- or broker-labeled with that origin at the copying or transforming operation and treated on read as ingesting the originating skill, or registered skill artifacts refused as the source of ordinary non-loader copy and transform operations), with a copy-then-read negative test proving a tool call issued by a context that read a copied or transformed registered `SKILL.md` is governed by the originating skill's ceiling rather than the reader's broader non-skill ceiling, or that the copy is refused, registered-skill origin also propagating through VCS, history, and archive extraction (content extracted from repository objects that corresponds to a registered skill's instruction files carrying that skill's origin whether the output lands in a file or streams directly into a model context, or such extraction refused as an instruction source for ordinary non-loader contexts), with a VCS-extraction negative test proving a tool call issued by a context that obtained a registered `SKILL.md` through a history or archive read is governed by the originating skill's ceiling rather than the reader's broader non-skill ceiling, or that the extraction itself is refused, historical-extraction origin keyed by registered path and version lineage rather than current content alone (superseded versions of a registered skill's instruction files propagating the skill's identity and ceiling via an authenticated index of registered historical versions and superseded blob identities, or historical extractions matching no registered artifact identity refused as instruction sources for ordinary non-loader contexts), with a historical-version extraction negative test proving a superseded `SKILL.md` obtained through a history or object read is governed by the originating skill's ceiling or the extraction is refused, ingestion tracking extended to network-fetched instruction artifacts (content fetched through a network capability from a raw repository URL, mirror, or artifact service whose bytes match a registered skill's current or superseded instruction files, via the same authenticated historical-version index, carrying that skill's origin and ceiling, or untrusted network content entering a model context conservatively tainted rather than trusted as ordinary non-skill instructions, or such fetches refused as instruction sources for ordinary non-loader contexts), with a network-fetch laundering negative test proving a registered or superseded `SKILL.md` obtained through a raw URL, mirror, or artifact-service fetch governs the reader under the originating skill's ceiling or the conservative taint, or the fetch is refused; skill-produced persistent artifacts carrying host-assigned origin metadata recording the producing skill stack, held outside the producing skill's write authority (a mandatory host/broker label or an authenticated out-of-band provenance record, never a self-carried marker the writing skill could omit or forge) and preserved across copies and updates, every output an operation derives from a provenance-labeled input inheriting the intersection of every labeled input's contributing origin stacks (host- or broker-assigned at output-commit time exactly as write-time labels are and accumulated with any label the output already carries), operations whose input-to-output derivation the broker cannot track refused read access to labeled inputs or every artifact they can write conservatively labeled with those inputs' contributors, with a derived-artifact laundering negative test proving a non-skill transformation of a restricted-labeled artifact yields an output whose next reader and any receipt minted over it are governed at or below the restricted intersection rather than the transformer's broader ceiling, every update accumulating provenance rather than overwriting it (recording every contributing origin stack across the artifact's write history, with consumers governed by the intersection of all contributors' ceilings, and a mixed-writer negative test proving an artifact written first by a broader or non-skill context and then modified by a restricted skill governs its next reader at the restricted intersection), the accumulated provenance held as a bounded authenticated representation (deduplicated effective origin stacks plus a compact authenticated history commitment under a declared retention policy, with a repeated-update growth negative test proving the protected provenance record for an artifact updated arbitrarily many times by the same origin stacks stays within its declared bound while its next reader is still governed at the restricted intersection, and the governing record bounded independently of distinct-contributor count by storing the monotonically narrowing effective intersection itself alongside the compact history commitment in place of any enumerated stack set, or by enforcing a hard bound on distinct retained contributors beyond which further updates are denied or the effective ceiling collapses to the most restrictive contributor with the artifact quarantined pending review, with a distinct-stack growth negative test proving the provenance record for an artifact updated by an unbounded sequence of distinct origin stacks stays within its declared bound while its next reader is governed at or below the restricted intersection, the bounded record kept revalidatable against live authority by retaining a bounded authenticated contributor index (the deduplicated contributing skill identities and their ceiling epochs kept within the hard distinct-contributor bound, or an equivalent verifiable index/proof structure whose proofs let the consumer enumerate every contributor and check each against live revocation and ceiling state; an opaque running hash or accumulator alone is insufficient because it neither enumerates contributors nor supplies their current ceilings) checked against live revocation and ceiling state on consumption, recomputing the effective ceiling as the intersection of the stored write-time effective intersection (preserved as an immutable upper bound so revalidation can only retain or narrow, never broaden, artifact authority) with the enumerated contributors' current ceilings or quarantining the artifact when any contributor was revoked or narrowed and failing closed when the index cannot be enumerated or verified, or by recomputing the artifact's effective ceiling on every consumption as the intersection of the stored write-time bound with live contributor state, with a write-then-narrow negative test proving an artifact written under a broad skill ceiling whose skill is later revoked or narrowed governs a later reader at or below the narrowed ceiling and yields no receipt for an action the current ceiling denies, including when the provenance record was already compacted to its bounded representation before the narrowing, and a write-then-broaden negative test proving an artifact written under a narrow skill ceiling whose contributor's ceiling is later broadened still governs a later reader at or below the write-time effective intersection and yields no receipt for an action outside that write-time intersection), content visibility and the accumulated provenance committing as one atomic transition (the updated content readable only in the same commit that records the contributing origin stack, or the artifact held unreadable until the provenance record commits), with a concurrent update-versus-read negative test proving a reader racing a restricted skill's update observes either the old content under the old provenance or the new content under the accumulated provenance, never the restricted content under the broader ceiling, each provenance record bound to the exact artifact version it labels and held in monotonic rollback-refusing storage of the same trust class as the other freshness records, consumption failing closed (reader refused or artifact quarantined) whenever the current content has no provenance record matching its version at or above the recorded watermark, with a matched content/provenance rollback negative test restoring an earlier authentic provenance record after a restricted skill's update and proving a later reader either observes matched content and provenance at the restricted intersection or is refused, never the current content under the stale broader label, with the originating ceiling propagated (intersected and retained until discard) into every model context that consumes such an artifact, or skill-written artifacts prevented from re-entering any model context as consumable instructions, with a negative test proving a tool call issued by a context after reading a skill-written artifact is still governed by the producing skill's ceiling rather than the reader's broader ceiling, and a provenance-stripping negative test proving a skill write attempting to strip, omit, or forge an artifact's provenance is denied or still yields a host-assigned record identifying the producing skill stack; writes to machine-consumed control artifacts (CI workflows, host hooks, package scripts, executables another host process runs directly) treated as authority delegation, either denied by default (control-artifact paths carved out of every skill-writable ceiling) or origin-gated at the consumer's execution point (the runner verifying the recorded producing skill stack before executing, or confining execution within the originating ceiling), with admission failing closed for that write capability (the skill-writable ceiling narrowed to exclude that consumer's control-artifact paths, or the skill held un-invocable) whenever neither write-time denial nor consumer-side origin gating or confinement is enforceable, never a recorded-but-permitted residual boundary, and negative tests proving a skill-written modification to a machine-consumed control artifact is either rejected at write time or not executed with authority beyond the producing skill's ceiling, and that a declaration whose control-artifact write capability no reachable consumer can enforce is denied admission, control-artifact admissions bound to a freshness-protected registry of consumer-to-path mappings (or the executor profile encoding them) with any consumer-mapping change revoking or revalidating every admission whose writable ceiling intersects the new consumer's executed paths (artifacts already written to those paths re-vetted or quarantined before the new consumer first executes them) or skill writes confined to storage that can never later become executable by a machine consumer, with a consumer-addition negative test proving a path written under an admitted ceiling and later mapped into a new consumer's executed set is not executed with that consumer's authority until the admission and existing artifacts are revalidated, the consumer topology naming every durable namespace a machine consumer interprets (durable object namespaces, topics, queues, subscriptions, and tables alongside filesystem paths) with skill-writable ceilings intersected against those namespaces under the same admission, mapping-change revalidation, and consumer-side origin-gating or confinement rules and the same fail-closed narrowing where none is enforceable, and a non-file consumer negative test proving a message, row, or object written under an admitted ceiling into a namespace a privileged worker consumes is not interpreted with that worker's broader authority and that a newly subscribed worker triggers revalidation of the admission and of artifacts already written to that namespace before it first consumes them, and every brokered communication payload that can enter another model context (live network, IPC, inherited-handle, or streaming-handler channels) treated as origin-carrying output, labeled at transmission by the broker or host with the sending context's full skill stack held outside the sender's write authority and inheriting the intersected ceiling in every consuming model context until that context is discarded, or constrained so channel payloads can never become model-consumable instructions, with a live-channel laundering negative test proving a tool call issued by a broader context after consuming a restricted skill's live-channel payload is still governed by the sending skill's ceiling, live machine-interpreted endpoints governed exactly as durable consumer namespaces are (the consumer topology naming every live endpoint a privileged machine consumer interprets, skill-connectable channels intersected against those endpoints under the same admission, mapping-change revalidation, and consumer-side origin-gating or confinement rules, or permission to send to such an endpoint admitted and reviewed as carrying the full transitive authority of every operation its payloads can trigger, with the same fail-closed narrowing where neither is enforceable), with a live machine-consumer negative test proving a payload sent under an admitted ceiling to a privileged worker endpoint is not interpreted with that worker's broader authority and a newly attached privileged consumer on a live endpoint triggers revalidation of the admission before it first interprets skill payloads; the effective permission set being the intersection of every contributing skill's approved ceiling and its own declared permissions, with nested- and concurrent-skill negative tests (including a narrower-outer-declaration case where the outer ceiling permits what the outer declaration omits) proving that composing skills cannot amplify authority; agent delegation from a skill context either denied, or every child agent session minted a non-amplifying origin derived from the full parent skill stack with that intersection retained for the child's lifetime and propagated to further descendants, with a negative test proving a child agent launched from a skill context is denied an action above the parent skill's ceiling; skill origin propagating to spawned processes as well: every tool request originating from a descendant process of a skill-launched shell carrying the full parent skill stack (host-derived from the launch, never self-reported) and governed by the same non-amplifying intersection, or descendant processes denied the gateway transport and any inheritable gateway credential at launch, with an end-to-end subprocess-to-tool negative test proving a tool call issued by a process spawned from a skill's allowed script is either governed by the parent skill's ceiling or refused at the gateway, never executed under the broader non-skill ceiling; interception resolving every capability-bearing action, side-effectful and read-only alike (an unregistered read-only tool can disclose files or secrets beyond a skill's `file_read` ceiling), regardless of origin class, skill-originated or authenticated non-skill (the non-skill origin rule authenticates the caller, not the tool), against an exhaustive admitted-tool registry and failing closed on any unmapped tool or alias (never exiting successfully as `_classify` does today for unrecognized actions), with only operations explicitly proven pure eligible for exemption and real-handler negative tests, an unrecognized read-only tool included, proving an unknown or newly exposed tool is denied whether invoked from a skill or from an authenticated non-skill context, pure exemptions retaining enforceable per-call and aggregate resource budgets (CPU, memory, and wall-clock or dispatcher time across all concurrent calls attributed to the skill, actor, tenant, and execution boundary) with the exemption refused when those budgets cannot be imposed and a pathological-input exhaustion negative test proving a pure-exempt operation driven with pathological input is throttled, denied, or terminated while other workloads retain capacity, pure exemptions also carrying per-call and aggregate output-byte limits (and output-token limits where results enter a model context) with declared expansion bounds relative to input size, enforced through streaming backpressure or termination at the bound rather than unbounded buffering, with the pathological-input test also driving a high-expansion input and proving the emitted output is bounded and other workloads retain response-channel, transport, model-context, and result-logging capacity, every purity decision bound to the immutable implementation and runtime identities it was proven against (resolved handler or server identity, implementation digest, and runtime version) and rechecked on every call at execution, failing closed into ordinary registry admission on any change, with a pure-to-effectful substitution negative test proving a pure-exempt alias rebound or upgraded to code that reads host state or performs an effect loses the exemption and is routed through registry admission rather than executed under the stale purity decision, ingress size and structural-depth limits enforced on requests before governance work begins and the entire pre-dispatch governance path (canonicalization, permission matching, policy evaluation, and audit serialization) running under cancellable per-call and aggregate resource budgets whose exhaustion terminates the work rather than abandoning the thread, with a denied-request exhaustion negative test proving oversized or pathological arguments that are ultimately denied leave the governance path bounded and other workloads with dispatcher capacity, raw transport bytes capped before any parsing occurs and request deserialization itself running under the same structural-depth, CPU, memory, and deadline limits as the governance path (parsing terminated at the bound rather than completed and then measured), with the denied-request test delivering its oversized and parser-pathological payloads through the real transport endpoint rather than passing an already-materialized request object to the dispatcher, durable audit growth governed by its own hierarchical audit-event and audit-byte quotas (per-skill and per-actor totals nesting under tenant-wide and execution-boundary-wide totals) backed by authenticated rotation/checkpointing or a bounded authenticated remote sink, with fail-closed backpressure on the submitting scope when a quota is reached (never dropped events, never execution without a recorded event, and requests arriving after a scope's quota is exhausted refused at a pre-admission rejection boundary explicitly outside the per-request audit obligation, kept auditable through bounded authenticated aggregation: a fixed-size tamper-evident per-scope record of over-quota attempt counts, byte totals, and first/last timestamps updated in place rather than appended per attempt), enclosing tenant-wide and execution-boundary-wide allowances preserving capacity for unrelated submitters via a sybil-resistant partition or scheduling rule preserving capacity for unrelated submitters (bounding each child scope's share individually is insufficient because coordinated skills submitting under several child scopes can keep every scope within quota while their shares sum to the entire enclosing allowance, leaving rotation no over-quota events to reclaim): the admitted child allocations collectively leaving a protected reserve of the enclosing allowance, with the sum of concurrently admitted child shares bounded strictly below the enclosing total and the creation of new child scopes itself a governed allocation-consuming admission rather than a free identity, or scheduling guaranteeing each submitter a minimum reserved capacity that no set of other scopes can consume, and events required by unexpired receipts retained through their retention obligations, with a many-small-denials storage-exhaustion negative test proving a sustained stream of small denied requests (including coordinated submissions spread across multiple actor scopes whose combined shares would sum to the enclosing allowance and continued submissions after the exhausting scope's quota is already full) keeps audit storage within its declared quotas, lands fail-closed backpressure only on the exhausting scopes, adds no per-request audit growth for post-exhaustion attempts while keeping them visible in the bounded aggregate record, and unrelated governed requests in the same enclosing scope still record required evidence and execute out of the protected reserve; each admitted registry entry binding the implementation it was reviewed against (resolved server/handler identity and compatible schema or implementation version, plus a content-addressed deployment digest or equivalent signature over the handler's executable and dependency closure, held and verified outside agent and handler write authority) into the ceiling record and receipt, rechecked at execution time against both the implementation actually resolved and the implementation bytes actually loaded and failing closed on mismatch, with a handler-substitution negative test proving an admitted alias rebound to a different server, or a swapped handler behind an admitted tool name, is denied rather than executed under the stale admission, and an in-place mutation negative test proving a handler whose code is replaced in place while its name, identity, and advertised schema/version remain unchanged is denied rather than executed, and the gate executing the handler only from verified bytes (the verified handler and its dependency closure materialized into an immutable snapshot from which every load, lazy imports included, resolves, or loaded and verified through the same protected handles so the bytes verified are the bytes executed), with a concurrent swap-versus-load negative test racing a dependency replacement against a lazy or execution-time load and proving the swapped code never runs, the admission record and receipt extending the content-addressed binding over the handler's execution runtime (the interpreter binary, dynamic loader, and native shared libraries it loads, or an equivalent content-addressed runtime image) verified and loaded from the same immutable snapshot or protected handles as the handler bytes, with a runtime-substitution negative test proving a handler whose interpreter or shared library is replaced while its own bytes remain unchanged is denied rather than executed, or the runtime recorded in the admission and receipt as an explicitly unverified boundary with no implementation-identity claim extending over it, footprint-based admission requiring a verifiable runtime closure with any handler whose runtime remains explicitly unverified executing only under OS-level containment or a capability broker independently enforcing the invoking skill's effective ceiling on its transitive effects, and a substituted-runtime footprint negative test proving a footprint-admitted handler whose unverified interpreter or shared library is replaced cannot perform a filesystem or network effect absent from its reviewed footprint, and the active handler admission held as monotonic, rollback-refusing freshness state (a registry version or admission epoch) scoped to its tenant and execution boundary and held outside agent and handler write authority, validated at receipt issuance and rechecked at the execution gate so a superseded or retired admission fails closed even when its registry entry and deployment snapshot are byte-identical to a formerly valid state, with a matched registry-and-deployment rollback negative test restoring a retired registry entry together with its old handler snapshot and proving execution is refused rather than run under the stale admission, and admission validation, receipt consumption, and launch serialized against active-admission transitions via an admission epoch or lease acquired atomically with validation and held through launch (revalidated or failing closed when the admission state changes before the launch commits), with a concurrent retire-versus-launch negative test racing a handler-admission retirement against an in-flight validated invocation and proving no interleaving lets the retired handler perform the side effect, the admission lease held until the governed effect commits or completes or the handler's in-flight and queued work kept revocable or brokered so a retirement revokes or re-validates it before its effect commits, with a delayed post-launch effect negative test retiring a handler admission after launch but before a queued or deferred effect and proving the effect never commits under the retired admission; each admitted handler's ambient effect footprint (the filesystem paths, network origins, credentials, and process capabilities its implementation exercises internally, which need not pass back through the dispatcher) either reviewed at admission and recorded in the ceiling record and receipt as authority the admission grants, with invocation from a skill whose ceiling is narrower than that footprint denied at issuance, or the handler executed under OS-level containment or a capability broker gating its transitive filesystem, network, and process effects to the invoking skill's effective ceiling, with a negative test analogous to the shell containment tests proving an admitted, unmodified handler whose internal behavior attempts an out-of-ceiling write or connection fails to perform that transitive effect, the handler footprint and broker alternative covering handler IPC and privilege channels with the same explicitness as the shell containment contract (process-control and IPC access (signals, tracing, shared memory, message queues, abstract Unix-domain sockets) recorded as admission-granted authority or isolated/brokered by the executor profile, inherited descriptors and credential handles closed at handler launch or explicitly recorded, inbound listeners denied or recorded as declared listener authority, and OS identity and privilege controls enforced for handler execution as for shell launches), with end-to-end negative tests proving an admitted handler attempting a same-UID signal or trace, a shared-memory or message-queue write, an abstract-socket connection, use of an inherited credential handle, an undeclared inbound listener, or each host privilege-escalation path fails to affect another process, use the handle, receive the connection, or acquire elevated identity, each admitted handler carrying enforceable per-invocation resource budgets (CPU, memory, a wall-clock execution deadline, process/thread count, open-file/socket descriptor limits, storage, and disk and network I/O bandwidth) imposed on the handler and any work it spawns or executing as isolated or rate-limited service capacity whose exhaustion cannot starve other workloads, with admission failing closed when the active executor profile cannot supply the recorded budgets or isolation and a handler-driven exhaustion negative test proving an admitted handler that allocates memory, forks, opens descriptors or sockets, or saturates an allowed disk or network endpoint in a loop is throttled, denied, or terminated while other workloads' capacity is preserved, a non-terminating-handler negative test proving an admitted handler that never returns is terminated at its wall-clock deadline and its dispatcher capacity reclaimed, handler invocations additionally run under shared admission control with aggregate resource quotas scoped to the skill, tenant, and execution boundary across all concurrent and queued handler invocations (neither per-invocation budgets nor isolated service capacity alone sufficing), with a concurrent many-handler-call exhaustion negative test proving many simultaneous individually-within-budget handler invocations from one skill are collectively bounded, shell and handler execution carrying per-invocation and aggregate output-byte limits (and output-token limits where results enter a model context) enforced through streaming backpressure or termination of the workload at the bound rather than unbounded buffering, with a result-output exhaustion negative test proving an admitted handler or allowed script streaming an oversized result is throttled or terminated at its recorded output bound while other workloads retain response-channel, transport, model-context, and result-logging capacity, accelerator-equipped hosts recording and enforcing per-invocation and aggregate accelerator budgets (device-memory allocation and accelerator compute time or utilization, scoped to the skill, tenant, and execution boundary) for shell and handler workloads, with admission failing closed when the active executor profile cannot enforce or isolate accelerator consumption, and a shared-GPU exhaustion negative test proving an admitted workload attempting to allocate all device memory or saturate accelerator compute is throttled, denied, or terminated while other workloads' accelerator capacity is preserved, handler state partitioned by invocation and tenant scope with verified teardown between scopes (or persistent handler state and response disclosure treated as brokered capabilities recorded in the ceiling record and mediated by the dispatcher), with a cross-tenant state-retention negative test proving a secret submitted in one invocation or tenant scope is never observable from a different scope served by the same admitted handler, and each admitted handler's effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch, that its ambient credential or default context resolves to) bound into the ceiling record and receipt, resolved atomically with launch and failing closed when it designates a different principal, scope, or epoch, the verified result pinned as an immutable credential instance (a materialized session, token, or non-reswitchable handle) that is the only credential the handler consumes rather than a re-read of the mutable ambient credential, with a credential-substitution negative test proving a swapped ambient credential or context is denied rather than executed against a principal the admission never named and a concurrent switch-versus-use negative test proving an ambient-credential repoint raced against handler launch and use never runs the side effect as anything but the pinned verified principal, and the resolved ambient target context (the endpoint, cluster or project, region, namespace, and equivalent target identifiers the admitted footprint resolves to) bound into the ceiling record, approvals, and receipts, resolved atomically with launch, failing closed on mismatch, and pinned into the immutable credential instance the handler consumes rather than re-read from mutable defaults, with a target-substitution negative test proving a retargeted context or default with an unchanged principal is denied rather than executed against a target the admission never named, every security-relevant ambient input the admitted handler consumes (configuration files and defaults, environment values, feature flags, service-discovery records) enumerated at admission and bound by resolved value or content digest into the ceiling record and receipt, resolved atomically with launch into a pinned configuration snapshot that is the only configuration the handler consumes rather than a re-read of mutable state, failing closed on mismatch, with an ambient-configuration substitution negative test proving a changed unnamed default (a repointed service-discovery record or flipped feature flag) is denied or cannot alter the executed operation, and secret-classified ambient inputs bound through a secret-store version identifier or keyed commitment verified inside the trusted gate and redacted from receipts, ceiling records, and approval rendering, with a secret-serialization negative test proving neither the resolved secret value nor an offline-guessable digest of it is emitted in any receipt, ceiling record, or approval-rendering artifact while secret substitution is still detected and denied; interception implemented as a dispatcher or proxy in which the downstream host operation is itself the `tool_fn` executed by `execute_with_receipt` (an advisory hook that only emits evidence before the host performs the original call, as the `PreToolUse` hook path does today, does not count as routing), with a test proving that when the gate denies, no subsequent direct host execution path performs the operation; and the interceptor and its configuration held outside agent write authority (host-installed, not resolved from the writable checkout) or authenticated against a pinned digest with the host executing the verified bytes themselves from an immutable snapshot or protected copy materialized at verification time (never re-opened from the writable path after verification, closing the check-to-use window), with a hook-tampering negative test proving a modified hook or settings file yields fail-closed denial rather than silent bypass and that a hook swapped in after verification is never the code that executes, and the interceptor's interpreter and complete governance dependency closure (governance package, permission parser, executor implementation) equally protected as host-installed immutable code outside agent write authority or authenticated against pinned digests and executed from the same immutable materialization (never re-imported from the writable checkout after verification), with a negative test mutating an imported gate dependency while preserving the hook bytes and proving subsequent governed calls fail closed rather than execute the compromised gate. | Skill-driven agent actions become receipt-gated instead of trusted by convention. | Skills remain the least governed executable surface in a governance repository, and skill `permissions:` stay documentation-only. |

## Status evidence

Verified against `master` @ `941e398cb266b29b88325a58605a16008d2af63c`
(2026-07-17). Every `SHIPPED` /
`PARTIAL` row cites a source file and a test; each `PARTIAL` also names the
unmet sub-clause.

1. **Local kernel** — `kernel.py`, `executor.py`, `receipt.py`, `audit.py`; `gove-zone smoke`, `examples/receipt-gated-execution/demo.py`, and `test_kernel_dispatch.py` / `test_executor_guard.py` / `test_decision_receipt.py` / `test_audit_chain.py` / `test_end_to_end.py` / `test_fail_closed.py`.
2. **Secure profile (signature-required gate default)** — `profile.py` `production()` → `require_signature=True`; issuance still requires an explicit signer and the gate requires a matching trusted verifier. `test_profile.py::test_production_constructor_requires_signature` pins the unset-env → production default flip (#138); `test_receipt_signing.py` covers configured signing and verification.
3. **Replay evidence (two levels)** — audit-only `replay.py` verifies chain/event integrity and policy-version consistency; it cannot reproduce the original decision because audit records omit raw arguments. Decision re-derivation is a separate `replay_store.py` path and requires an opt-in `ReplaySideStore` plus the matching original policy bundle; `test_replay.py` and `test_replay_bundle_equivalence.py` cover those boundaries. The `consumption.py` ledger is anti-replay enforcement, not decision replay: its high-water-mark check catches wholesale + tail-below-HWM deletion, **not** interior burned-entry deletion (only the out-of-band `verify_ledger` chain check does), and the default `checkpoint=False` means no HWM at all. Workflow replay coverage is in `test_workflow_receipt_chain.py`; consumption integrity is covered by `test_consumption_{tamper,reconcile,hwm}.py`.
4. **Integration.** MCP/A2A + OpenAI-Chat/Responses + LangChain-style payload parsing ships in `integration.py`, with conformance cases in `test_adapter_conformance.py` and `test_mcp_binding.py`. The dependency-free `make_langgraph_tool_node` wrapper is exported from the same module; `test_langgraph_node.py` proves allow execution plus fail-closed deny and malformed-call behavior, and `examples/agent-framework-wrapper/demo.py` demonstrates the contract-level pattern. This does not claim conformance against an installed `langgraph` dependency.
5. **Evidence (exportable proof packs)** — `verifier.py::verify_proof_pack`; CLI `verify-proofpack`; `docs/PROOF_PATH.md`; conformance corpus `test_proofpack_corpus.py` (#144).
6. **Policy (PARTIAL).** *Shipped:* content-addressed policy **versioning** (`policy.py` `_compute_version` / `RuleSetPolicy`; CLI `policy inspect` / `policy export`; `test_policy_bundle_io.py`) and **tenant binding** (`tenant.py` active-bundle-by-tenant lookup; `test_tenant_safety.py`). *Missing:* policy-**bundle signatures** (no bundle signing exists) and the **active/stale/revoked lifecycle states** — the latter is explicitly *not modeled* (`contracts.py`: "(stale / revoked) is intentionally not modeled here").

## Roadmap wording rule

Use "planned", "roadmap", or "not implemented yet" unless a source file, test, demo output, and claim-ledger entry prove the feature exists.
