# Roadmap

> **Roadmap of record.** This is the single maintained roadmap for the
> repository. Superseded roadmaps and plans are frozen in
> [`docs/archive/`](archive/README.md); do not plan new work from them.

This roadmap is claim-safe: planned work is not described as implemented.

The resumable SaaS beta dependency program and its claim-safe capability view live in
[`docs/saas/DELIVERY_DAG.yaml`](saas/DELIVERY_DAG.yaml) and
[`docs/saas/ACCEPTANCE_MATRIX.md`](saas/ACCEPTANCE_MATRIX.md). Those records refine
delivery dependencies and evidence state; this file remains the only roadmap of record.
The Phase-0 target product contract is specified in
[`saas/PRODUCT_REQUIREMENTS.md`](saas/PRODUCT_REQUIREMENTS.md),
[`saas/ASSURANCE_CLASSES.md`](saas/ASSURANCE_CLASSES.md),
[`saas/OPEN_CORE_BOUNDARY.md`](saas/OPEN_CORE_BOUNDARY.md), and
[`saas/ENTITLEMENT_AND_METERING_MATRIX.md`](saas/ENTITLEMENT_AND_METERING_MATRIX.md).
They are future acceptance contracts, not evidence that the managed service,
entitlements, pricing, or billing implementation exists.
As of the current SaaS DAG snapshot at origin/master
`c20b51ee9ab1d89cfa496330568ca032c34d5022`, PRs #369, #370, and
#371 are merged into current master and provide partial current-local evidence
for project/environment attachment, one signed native agent-create transaction
route, and route-scoped idempotency/outbox behavior. Open clean PRs #391,
#393, #395, #398, and #413 remain current-local stack evidence only; PR #413's
authoritative G015 proof failed and was consumed, so its later green CI is
non-authoritative. These records do not promote the full managed service:
legacy unsigned route aliases, provider wiring, durable idempotency for other
mutating routes, async export jobs, tenant isolation, external exactly-once
delivery, enrollment/policy lifecycle completion, and production launch remain
open.
The companion Phase-0 architecture packet is
[`saas/ARCHITECTURE.md`](saas/ARCHITECTURE.md),
[`saas/THREAT_MODEL.md`](saas/THREAT_MODEL.md),
[`saas/API_AND_DATA_CONTRACT.md`](saas/API_AND_DATA_CONTRACT.md), and
[`saas/MIGRATION_VERSIONING_COMPATIBILITY_POLICY.md`](saas/MIGRATION_VERSIONING_COMPATIBILITY_POLICY.md),
with proposed owner-gated [identity](adr/saas-identity-oidc-saml-scim-build-vs-buy.md),
[KMS](adr/saas-kms-build-vs-buy.md),
[object-retention](adr/saas-object-retention-build-vs-buy.md),
[independent-witness](adr/saas-independent-witness-build-vs-buy.md),
[billing](adr/saas-billing-build-vs-buy.md), and
[commercial-boundary](adr/saas-commercial-module-boundary.md) ADRs. It records
future trust, migration, and decision requirements; it is not evidence that a
managed plane, provider, retention policy, witness, identity federation, or
commercial program exists.

Status legend: **✅ SHIPPED** — every acceptance sub-clause has a source file *and* a cited test; **🟡 PARTIAL** — some sub-clauses met (the missing one is named in Status evidence below); **⬜ PLANNED** — forward-looking, not implemented yet. The stage table below is the historical local-kernel roadmap snapshot verified against `master` @ `941e398cb266b29b88325a58605a16008d2af63c` (see Status evidence); the current SaaS beta DAG/matrix snapshot is separate and frozen at `c20b51ee9ab1d89cfa496330568ca032c34d5022`.

| Stage | Status | Why it matters | Acceptance test | User impact | Risk if skipped |
|---|---|---|---|---|---|
| Local receipt-gated kernel | ✅ SHIPPED [1] | Establish the invariant locally. | Smoke proof, receipt-gated demo, executor/audit/receipt tests pass. | Evaluators can prove `No valid Decision Receipt, no side effect` on a laptop. | Project sounds like governance theory without runnable proof. |
| Secure profile: signature-required gate by default | ✅ SHIPPED [2] | Close recomputed-hash residual for production-adjacent pilots. | Default gate rejects unsigned receipts; explicitly configured signed issuance and trusted-verifier tests pass. | Operators get a fail-closed secure gate default without implied automatic key setup. | Unsigned dev mode may be misused as production assurance. |
| Replay evidence: audit verification plus conditional re-derivation | ✅ SHIPPED [3] | Prove longer workflows/evidence bundles without overstating retained data. | Audit-only replay verifies chain/event integrity and policy-version consistency; separate side-store tests re-derive only with retained raw calls and the matching original policy. | Reviewers can distinguish evidence integrity from reproduced policy decisions. | Missing/redacted side records or policy bundles limit review to audit-only verification. |
| Integration: MCP and agent-framework adapters | ✅ SHIPPED [4] | Put the gate where teams already run agents. | Conformance tests for MCP gateway, OpenAI-style tool wrapper, LangGraph node/tool wrapper. | Faster adoption without replacing existing agent stacks. | Integrators may wire the gate incorrectly or bypass it. |
| Reference integration: real agent platform (e.g. Mattermost Agents MCP; **candidate:** flue `outboundByHost` egress callback) | ⬜ PLANNED | Prove the MCP gateway against a widely-deployed agent surface, not just local examples. | A real platform's agent tool-calls (`create_post`, `create_channel`, file ops) route through the gate; fail-closed when no receipt; `expected_actor` bound to the platform's authenticated user; bypass-path documented. The same fail-closed / `expected_actor` acceptance applies to a flue `outboundByHost` integration (call kernel → forward on `ALLOW` with the signed receipt → fail closed on `DENY`). | Operators see the gate working on a production agent stack they recognise. | MCP-adapter conformance stays illustrative; adopters can't see a concrete end-to-end wiring. |
| Policy: signed bundles and versioned policy registry | 🟡 PARTIAL [6] | Stop policy-bundle substitution and stale policy confusion. | Bundle signatures, active/stale/revoked states, tenant binding tests. | Clear operational policy lifecycle. | Receipts may bind hashes but operators lack lifecycle controls. |
| Evidence: exportable proof packs | ✅ SHIPPED [5] | Make review, buyer diligence, and incident response concrete. | CLI exports receipts, audit chain, replay verdicts, limitations, and manifest; tamper tests fail. | Reviewers can inspect artifacts without trusting prose. | Claims remain hard to verify externally. |
| Operations: production deployment profile | ⬜ PLANNED | Move from local proof to production-adjacent operation. | Deployment guide, health checks, audit sink, key management, fail-closed config, rollback drills. | Teams can run pilots with known responsibilities. | Local demo may be mistaken for production readiness. |
| Review: external security review | ⬜ PLANNED | Independent challenge of threat model and implementation. | Published review scope, findings, remediations, residual risks. | Higher buyer/security confidence. | Internal tests may miss design or threat-model gaps. |
| Ecosystem: standard receipt schema for agent runtimes | ⬜ PLANNED | Make receipts portable across frameworks. | Versioned schema, compatibility tests, reference validators in multiple hosts. | Agent runtimes can share governance evidence. | Fragmented receipts make integration harder. |
| Skill trust: identity, permission ceilings, host interception | ⬜ PLANNED | Bring agent skills (`.claude/skills/**`, `.agents/skills/**`) under governance before their declared permissions can be enforced (steps 4-6 of [`design/agent-skill-trust-pipeline-adoption.md`](design/agent-skill-trust-pipeline-adoption.md)). | Trusted per-skill identity (name/version/artifact digest), the artifact digest computed over a versioned, collision-resistant canonical tree manifest binding every entry's normalized relative path, entry type, relevant mode bits, and content length and hash under an unambiguous length-delimited encoding, with the manifest scheme version bound into ceiling records and receipts and a structural-substitution negative test proving a tree that permutes the same content across different paths or entry boundaries yields a different digest and is rejected, manifest paths canonicalized per supported host filesystem (case folding and Unicode normalization included) with any manifest whose entries collide after canonicalization for the target host rejected, proven by a cross-platform path-collision negative test, security-relevant filesystem metadata (ownership, POSIX ACLs, security labels, extended attributes such as Linux `security.capability`) either stripped and normalized to a fixed approved baseline at snapshot construction or bound into the manifest digest and rechecked when the snapshot is constructed, with a metadata-substitution negative test proving an artifact whose bytes match the approved digest but whose security-relevant metadata differs is rejected rather than executed with that authority, each host mirror of a skill carrying its own full-tree digest over exactly the bytes that host loads, both mirrors tied to a common canonical-source/release identity, with mirror parity checked on the normalized instruction body and permission semantics, never on digest equality across mirrors; independently reviewed permission ceilings held outside the skill and outside agent write authority, authenticated and version-bound to the skill digest they approve, and validated against trusted freshness state (an active-version/revocation record or monotonic counter outside agent write authority identifying the currently approved ceiling-and-skill pair), with tamper and rollback tests at both issuance and execution, including rollback of a previously valid ceiling-and-skill pair as a matched set, every ceiling record and active-pair entry additionally bound to the tenant, execution boundary, and applicable host or executor profile it was approved for, rechecked against the requesting deployment context at both issuance and execution, with cross-tenant and cross-boundary substitution negative tests proving a ceiling approved in one tenant or execution boundary cannot authorize a receipt issued or executed in another, and revocation either effective against running work (process trees and brokered resources launched under a ceiling-and-skill pair tied to a revocable capability lease the host closes when the active pair changes, lease acquisition serialized with the revocation transition (one transaction, or an epoch check at registration failing closed when the pair changed since validation) so a request validated against the outgoing pair cannot register a fresh lease and launch after revocation, with negative tests proving a still-running child process's file and network effects are cut off after revocation and a concurrent revoke-versus-launch race proving no stale process survives or starts regardless of interleaving) or explicitly scoped to future launches only, with that residual risk recorded in the ceiling record and receipt and a test documenting that a pre-revocation process retains its capabilities until it exits; host interception routing governed skill capabilities through `execute_with_receipt`, with declared `permissions:` enforced deny-only against the ceiling and the permission intersection re-evaluated against the final executable arguments after any policy `TRANSFORM`, denying when transformed arguments cross the ceiling, and every security-relevant by-reference input named by those final arguments (mutable content outside the immutable skill snapshot) hashed or snapshotted at authorization with that digest or snapshot identity recorded in the receipt and the handler consuming exactly those verified bytes or already-resolved descriptors rather than re-reading the mutable path, with a negative test replacing an authorized by-reference input's content between authorization and handler read and proving the handler consumes the authorized bytes or the execution is denied, never the swapped content; a missing, malformed, or unrecognized permission declaration failing admission for skills in the governed set (no fallback to the approved ceiling, no receipt), with a real-handler negative test proving a governed skill lacking a valid declaration produces no receipt and no side effect, and the governed set defined by loadability rather than content classification or invocation path: every loadable skill is governed whether model-selected, implicitly invoked, or explicitly invoked by the user (`disable-model-invocation` removes model selection, not loadability), documentation-only skills carry an explicit deny-by-default/no-capability declaration (an empty permission set) rather than an exemption, and real-handler negative tests prove a tool call originating from a documentation-classified skill with a no-capability declaration produces no receipt and no side effect rather than being governed under the agent's non-skill ceiling, and a tool call directed by a manual skill the user explicitly invoked is governed under that skill's declaration and ceiling and denied when the skill lacks a valid declaration; one versioned permission-language grammar and canonicalization shared by the loadability gate, host admission, receipt issuance, and the executor (a single parser or conformance-tested equivalent), with every declaration reduced to canonical form before use, the language version and canonical effective permission set bound into ceiling records and receipts, unknown or noncanonical forms rejected fail-closed, and parser-differential negative tests proving a pattern interpreted differently by two components (recursive-glob versus literal or prefix-match readings) is rejected or yields identical granted authority at every consumer, never a wider grant at the executor than at admission; a fail-closed `deny_behavior` mapping (an absent or unrecognized value means stop, never proceed) carried into the canonical skill contract, with human approval never reviving the original `DENY`/`ESCALATE` receipt: a granted approval triggers a freshly evaluated request producing a new `ALLOW`/`TRANSFORM` receipt that is the only thing the executor accepts, and the approval grant is authenticated to the granting human, bound to the exact request it approves (actor, skill identity/digest, action, final arguments) and to the originating decision (the denied/escalated request's audit hash plus the tenant, execution boundary, and policy bundle/version in force, with per-field mismatch negative tests proving the grant cannot be consumed for a different decision), the approval collected through a trusted approval channel that renders the decision directly from the payload being signed (actor, skill identity/digest, action, final arguments, resolved target, and referenced-content digests taken from the canonical bound fields, never from caller-supplied prose), with a mismatched-display negative test proving an approval prompted with a caller-supplied label or summary that disagrees with the bound request fields yields no consumable grant and no side effect, the active policy bundle identity and version held in monotonic rollback-refusing freshness state outside agent and child-process write authority (the same trust class as the ceiling freshness records and the grant ledger) and validated at receipt issuance, grant consumption, and execution, failing closed when the bundle in force is older than the recorded active version or the state is unavailable, with a matched bundle-and-binding rollback negative test proving a restored older permissive bundle and its restored active tenant binding cannot yield an executable receipt or side effect for a request the current policy denies, policy-freshness validation serialized with receipt consumption and launch against the active-policy transition via a policy-version lease or epoch check acquired atomically with validation (one transaction, or an epoch recheck at launch failing closed when the active policy changed since validation), with a concurrent policy-update-versus-launch negative test proving no interleaving lets an already-validated request run its side effect under a superseded policy that newly denies the action, bound to the versioned enforcement and implementation identities in force at approval (the active ceiling record and version, executor profile identity/version, permission-language version, and the admitted handler deployment digest the request resolves to) and rechecked at consumption against the identities active at fresh evaluation, failing closed on any change, with a context-substitution negative test proving a grant whose active ceiling or admitted handler was substituted after approval is rejected without a side effect, the grant and its approval evidence also binding the effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch) the admitted handler's ambient credential resolved to at approval, rechecked at consumption against the currently resolved principal and failing closed on any change, with a credential-substitution negative test spanning approval to grant presentation proving a grant approved under a staging principal is refused after the handler's ambient credential is repointed to production, the grant and its approval evidence also binding the content digest or immutable snapshot identity of every mutable file or security-relevant by-reference input the reviewed request designates, re-resolved at consumption with the grant rejected when the current content no longer matches what the human reviewed, and a reviewed-content substitution negative test proving a grant approved for a request naming a mutable manifest is refused without a side effect after that file's content is replaced, the originating audit event held or anchored in authenticated rollback-resistant storage outside agent and child-process write authority with grant consumption and execution confirming the bound event remains present and anchored there, and a negative test proving a grant whose bound audit event was deleted or truncated before presentation is refused without a side effect, expiry-bounded with expiry at issuance, grant consumption, and execution evaluated against a trusted time source (a trusted monotonic clock or signed time attestation held and validated outside agent and child-process write authority) that fails closed when unavailable rather than falling back to the mutable host clock, and a clock-rollback negative test proving an expired but unused approval grant, invocation context, or receipt is still rejected without a side effect after the verifier's host clock is set back before its expiry, recorded as evidence in the new receiptset back before its expiry, monotonic timestamps bound to a non-reusable boot/time-source epoch identifier with expiry evaluation failing closed across any epoch discontinuity (or fresh, non-replayable signed absolute time required instead), with a restart negative test proving an unused approval grant, invocation context, and receipt that expired before a host restart regain no validity after the monotonic clock resets or rebases, recorded as evidence in the new receipt, and consumed atomically at receipt issuance (compare-and-consume or idempotent issuance against shared durable state) so at most one executable receipt is ever minted per grant, the grant ledger itself authenticated, monotonic/rollback-refusing, and held outside agent and child-process write authority (the same trust class as the receipt-consumption store), with a grant-ledger tamper/rollback negative test proving a deleted or rolled-back consumption record does not let a second executable receipt be minted from a consumed grant, with negative tests proving an altered-argument or different-skill grant, an expired grant, a replayed grant, and the original denied/escalated receipt are all rejected without a side effect, plus a concurrent-use test proving two evaluations racing on the same grant yield exactly one executable receipt and at most one side effect; approval bounded below the permission ceiling: a grant resolves only policy-level escalation for requests whose final arguments remain inside every effective permission ceiling and never overrides the declared/approved intersection, with an above-ceiling-with-valid-approval negative test through the real handler proving a request outside the ceiling yields no executable receipt and no side effect despite an otherwise valid grant, grants restricted to an explicit `ESCALATE` or a decision the policy itself marks approvable, with hard `DENY` (absolute controls such as tenant isolation or forbidden destructive actions) remaining denied regardless of any grant and a real-handler negative test proving a hard-denied request accompanied by an otherwise valid approval grant yields no executable receipt and no side effect; the approval grant issued by an authorized approver distinct from the requesting actor (preserving the proposer/validator separation `escalation.py::approve_escalation` already enforces), with a self-approval negative test through the real handler proving a grant issued by the requesting principal for its own request is rejected; the approver's credential and role identifiers bound into the grant and revalidated at consumption against fresh, monotonic, rollback-protected approver-authorization and credential-revocation state (the same trust class as the grant ledger), failing closed on a revoked credential, withdrawn role, or unavailable state, with a post-revocation negative test proving a grant signed by a since-revoked approver credential or issued under a since-withdrawn approver role is rejected at consumption without a side effect, and approver-state validation, the grant's compare-and-consume, and receipt issuance serialized against approver credential and role transitions (an approver-authorization epoch or lease acquired atomically with consumption and rechecked at issuance commit) so issuance either commits before a revocation takes effect or fails closed, with a concurrent revoke-versus-consume negative test racing an approver credential or role revocation against grant consumption and proving either the executable receipt was issued before the revocation committed or consumption is refused without a side effect, never both; the approval-gated receipt itself consumed atomically at the execution gate against shared durable state (a receipt-consumption ledger, not a stateless `execute_with_receipt`), with a concurrent-replay test of the issued receipt proving its side effect runs at most once; every skill-originated receipt (not only approval-gated ones) bound to its live invocation (the origin context's session binding and its nonce or handle identity), and every executable receipt regardless of origin class (skill-originated, approval-gated, or an ordinary authenticated non-skill tool call) consumed atomically at the execution gate against shared durable state of the same class, with concurrent-replay negative tests presenting an ordinary non-approval skill receipt and an ordinary authenticated non-skill receipt in parallel and proving each side effect runs at most once, consumption state kept in an authenticated, monotonic store that refuses rollback, held outside agent and child-process write authority, with a negative test proving tampering with or rolling back the ledger before replaying a consumed receipt does not make that receipt executable again, the execution gate additionally revalidating invocation liveness at consumption (the bound session or handle still active) with receipt expiry capped to the originating invocation's lifetime, and a negative test proving a receipt first presented after its originating invocation has ended is denied without a side effect, liveness validation, receipt consumption, and launch serialized against the invocation-termination transition via a revocable lease tied to the invocation handle, acquired atomically with the liveness check (one transaction, or an epoch check at consumption failing closed when the invocation ended since validation) and closed by termination, with a concurrent end-versus-launch negative test proving that racing invocation termination against receipt consumption never launches the side effect after the invocation has ended, regardless of interleaving; the authenticated skill identity/digest, the approved ceiling, and the declared/effective permission set bound into the Decision Receipt at issuance and rechecked at the executor gate, so a receipt issued for a skill-governed call cannot authorize execution without proving which skill ceiling governed it; issuance tests proving these bindings are present in skill-originated receipts, executor tests rejecting receipts whose skill bindings are missing or do not match the intercepted request, and tamper tests proving a mutated skill digest, ceiling, or permission set invalidates the receipt; skill-trust receipts signed at issuance (starting from the kernel's opt-in Ed25519 signing mode) and verified by a trusted verifier at the executor gate against key material outside agent write authority, with a recomputed-forgery negative test proving a receipt whose skill fields were altered and re-hashed is rejected, the receipt-issuance private key additionally held outside agent and child-process read authority (an isolated signer process/service or a non-exportable keystore/hardware-backed key that governed agents and their spawned processes can neither read nor invoke directly, reachable only through the issuance path), with a key-exfiltration/unauthorized-signing negative test proving a shell-enabled skill granted ambient credentials can neither read the issuance key material nor obtain a validly signed receipt outside the issuance path, every skill-trust receipt binding the signing key's identity and issuance purpose into the signed content and the execution gate consulting fresh, rollback-protected signer-trust state (a monotonic revocation/active-key record held outside agent write authority, of the same trust class as the ceiling freshness records; `execute_with_receipt`'s `revoked_keys` parameter is the starting point) so receipts signed by retired or wrong-purpose keys are rejected, with a retired-key negative test proving a receipt minted after its signing key was retired is rejected without a side effect even by an executor whose local signer-trust state is stale or rolled back, signer-trust validation, receipt consumption, and launch serialized against the active-key transition via a signer-trust lease or epoch acquired atomically with validation and held through launch (revalidated or failed closed when the epoch changes before the launch commits), with a concurrent revoke-versus-launch negative test racing key retirement against an in-flight receipt validated under that key and proving the launch either commits before retirement takes effect or is refused without a side effect, skill-governed receipts carrying a new receipt schema version or a critical purpose/extension marker that a verifier must reject when unsupported, with a compatibility test proving the current pre-skill executor fails closed on a skill-governed receipt rather than executing it under pre-skill semantics, the result otherwise explicitly limited to non-adversarial unsigned development use; plus negative-path tests through the real host handler (not only direct policy/executor unit calls) proving the side effect did not run for a forged or mismatched skill digest, an above-ceiling action, an action inside the approved ceiling but outside the skill's own declared `permissions:` (proving the effective set is the intersection of declaration and ceiling, so declarations narrow the ceiling rather than remaining documentation-only, with the effective intersection verified in the receipt), a policy `TRANSFORM` that rewrites an initially within-ceiling request into above-ceiling final arguments, a request missing its authenticated origin, and a skill-originated request that substitutes or reuses an authenticated non-skill origin, proving origin-class mismatch is rejected rather than governed under the agent's broader normal ceiling; a positive end-to-end case through the same real host handler in which an authenticated skill-originated action inside both the declared set and the approved ceiling produces a correctly bound receipt and the side effect runs exactly once, proving the pipeline admits allowed work rather than passing its negative tests by rejecting everything; defined shell containment semantics: a shell grant is treated as granting the spawned process's ambient file and network capabilities unless the launch runs under an OS-level sandbox or capability-brokered executor that materially enforces narrower ceilings, `shell.allowed_scripts` enforced by an argv-based launcher that directly resolves and executes the approved executable from the immutable snapshot with caller-supplied arguments (never through shell interpretation of a command string) or a parser rejecting wrappers, substitutions, redirections, and command chaining, with a negative test proving a command string referencing an allowed script with an appended command is denied rather than launched, code-bearing arguments to allowed scripts (eval expressions; module, plugin, or code-evaluated configuration paths) constrained by per-script argument schemas reviewed at admission that reject command/code/plugin inputs or by recursively admitting and content-binding every executable input the arguments can name before the receipt is issued, with a negative test proving an allowed script invoked with a code-bearing argument naming unadmitted code is denied rather than launched, the loadability gate records the containment capabilities each declaration requires as machine-readable requirements (rather than statically deciding enforceability it cannot know), host deployment/admission and the executor at execution time each fail closed when the active executor profile cannot supply the recorded requirements, the executor profile itself derived from the actual dispatching executor's capabilities or held as an authenticated, versioned record outside agent and child-process write authority with its identity and version bound into the receipt and rechecked at execution, with a profile-tampering negative test proving a forged or edited profile cannot satisfy the containment requirements, the active profile version held in monotonic rollback-refusing freshness state with any older authenticated profile rejected at admission, grant consumption, and execution, and a matched profile/configuration rollback negative test proving a downgraded executor presenting a restored older authenticated profile fails closed rather than launching uncontained, profile validation, the containment-requirement check, and launch serialized against profile transitions via a capability lease or profile epoch acquired atomically with validation and held through launch (revalidated or failed closed when the epoch changes before the launch commits), with a concurrent downgrade-versus-launch negative test proving a launch racing a profile or capability downgrade either runs under the still-enforced capable profile or is refused, and any claimed sandboxed or brokered enforcement is proven by end-to-end negative tests on the transitive effects themselves (the spawned process's denied write outside the allowed directory, its denied network connection, its denied environment token and credential-handle use), not only on the launch arguments, with inherited credentials treated explicitly (the shell grant either defined as granting the ambient environment and credential handles the process inherits, or the sandboxed/brokered launch running with an allowlisted environment and closed inherited descriptors and credential-agent handles, with negative tests proving the spawned process can neither read a denied environment token nor use an inherited credential handle), shell grants, their approvals, and their receipts binding the effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch) each granted ambient credential resolved to at authorization, re-resolved atomically with launch, failing closed on a different principal, scope, or epoch, and materialized as an immutable credential instance (a materialized session, token, or non-reswitchable handle) that is the only credential the spawned process inherits or consumes, with a shell credential-substitution negative test proving an allowed script authorized while the ambient credential resolved to the staging principal is refused launch after that credential is repointed to production and a concurrent switch-versus-use negative test racing an ambient-credential repoint against script launch and use, proving the spawned process only ever acts as the pinned verified principal, the resolved ambient target context (the endpoint, cluster or project, region, namespace, and equivalent target identifiers each granted credential resolves to) bound into shell grants, approvals, and receipts at authorization, resolved atomically with launch, failing closed on mismatch, and pinned into the same immutable credential instance the spawned process consumes rather than re-read from mutable defaults, with a shell target-substitution negative test proving an allowed script authorized while its credential's target designated staging is refused launch after that target is repointed to production under an unchanged principal, scope, and epoch, and enforceable resource budgets (CPU time, memory, process/PID count, wall-clock runtime, storage) recorded in the ceiling/executor profile and imposed by the sandboxed or brokered launch on the spawned process and its descendant tree, with shell grants rejected at admission and execution when the executor profile cannot impose the recorded budgets and an end-to-end resource-exhaustion negative test proving a launched process that attempts to exceed its budget (an unbounded fork loop, allocation, or disk write) is terminated or denied and the host-level exhaustion does not occur), and process-control and IPC channels (signaling or tracing same-UID processes, abstract Unix-domain sockets, shared IPC segments and message queues) either defined as ambient authority the shell grant documents or isolated/brokered by the executor profile (separate PID and IPC namespaces or equivalent brokering of signals, tracing, and shared IPC, plus a separate network namespace or explicit brokering of abstract-socket access, because Linux abstract Unix-domain sockets are scoped to the network namespace and remain host-reachable under PID and IPC isolation alone), with an end-to-end negative test exercising that exact profile and proving the launched process cannot signal, trace, connect to a host abstract Unix-domain socket, or otherwise affect another host process through those channels, and OS identity and privilege controls (a fixed non-privileged UID/GID and supplementary group set, dropped Linux capabilities, `no_new_privs` or the platform equivalent, and denied or brokered setuid/setgid helpers, `sudo`, and user-namespace creation) enforced as executor-profile requirements, with shell grants rejected at admission and at execution when the profile cannot impose them and an end-to-end privilege-escalation negative test proving a launched process attempting each escalation path the host exposes acquires no elevated identity and the out-of-ceiling effect does not occur; host deployment/admission verifying that the active host's skill-schema version matches the version the vendored or repo-local loadability validator is pinned against, failing closed (the skill held un-invocable) on mismatch until a validator matching the active host version has revalidated the tree, with a negative test proving a skill validated only against a stale schema version is not enabled on an upgraded host; skills executed and their bundled resources loaded from a read-only content-addressed snapshot whose digest is bound into the receipt (pre-launch digest revalidation alone is insufficient), with mutation tests between load and launch and after launch proving mutated content is never executed or read, snapshot construction and resource loading rejecting symlinks and other references that escape the snapshot (or materializing and hashing their targets inside it), with a negative test mutating an external link target between authentication and use, the snapshot digest explicitly scoped to bundled skill content with the ambient runtime (interpreter, installed packages, `PATH`-resolved binaries) recorded in the approval and receipt as an unverified boundary unless a content-addressed runtime image or interpreter/dependency closure is bound into them; `file_read`/`file_write` directory ceilings enforced at filesystem resolution (descriptor-relative/no-follow opens or an equivalent filesystem sandbox) with symlink-escape and check-to-open path-race negative tests, resolution additionally refusing to cross mount points within the ceiling (`RESOLVE_NO_XDEV`-style no-cross-mount resolution or equivalent isolation, beneath-style resolution alone being lexical and letting a bind mount or mounted subtree inside an allowed directory expose a denied tree), with a bind-mount escape negative test proving an access through a mount point inside an allowed directory fails to reach the denied tree, and hard-link escapes handled explicitly (cross-boundary multi-linked entries rejected or individually vetted before any copy or materialization, copying/copy-on-write alone being insufficient because materialization reads the denied inode and discloses its contents into the sandbox, and a mount namespace or bind-mounted view being insufficient because it preserves the shared inode, with vetting atomic with materialization: the materialized view constructed from a frozen source snapshot read by both the vet and the copy, or each entry validated and copied through the same opened descriptor with concurrent substitution prevented or failed closed), with a cross-boundary hard-link negative test proving a hard link inside an allowed directory to a denied file fails to read or write the outside content and that no copy or snapshot of the denied content becomes readable inside the materialized view, including under a mount-namespace or bind-mount view, and a concurrent swap-versus-materialization negative test proving a vetted entry replaced with a hard link to a denied file between vetting and materialization fails closed without disclosing the denied content, and directory capabilities restricted to regular files and directories, failing closed on any other inode type (FIFOs, device nodes, sockets) unless a specific special inode is explicitly brokered as its own reviewed capability, with FIFO and device-node negative tests proving the external effect itself does not occur (no command reaches the privileged reader through an in-ceiling FIFO, and an in-ceiling device node is not opened); network ceilings enforced through a direct-network broker that re-validates every redirect hop and the actual resolved destination address against the ceiling and connects using that approved resolution, with redirect-to-forbidden-origin and DNS-rebinding negative tests proving the outside destination is never reached, permission to use a forwarding endpoint (an HTTP CONNECT or SOCKS proxy or equivalent relay) treated as authority over every destination it can relay to or the logical tunnel destination validated and bound against the ceiling by a protocol-aware broker before the tunnel is established, with a proxy-tunnel negative test proving an attempt to tunnel through an allowed proxy to a denied origin fails to reach that origin, inbound listener authority defined as its own capability distinct from outbound origin authority, denied by default for declarations naming only outbound origins and enforced by the executor profile, with a negative test proving another workload cannot reach a listener bound by a process launched under an outbound-only grant; an authenticated origin/delegation stack carried on skill-originated requests, each loader-issued origin context bound to the host session and the exact tool-call/request digest it authorizes with a short lifetime and a nonce or non-exportable live handle, with stale-context and cross-call replay negative tests rejecting before receipt issuance, receipt issuance atomically compare-and-consuming the invocation context (or idempotent, keyed on the session, nonce, and request digest, returning the already-issued receipt rather than minting another) against shared durable state of the same trust class as the grant ledger so one origin context yields at most one signed receipt, context consumption modeled as an atomic state transition rather than a single-shot flag (issuing a non-executable escalation receipt atomically transitions the bound context into an escalated state tied to that decision's approval grant, from which consuming the matching grant may issue at most one executable approved receipt under the same bound context, after which the context is terminally consumed, every other issuance path failing closed), with a concurrent multi-issuance negative test proving racing or retried issuance on the same fresh or escalated context mints at most one non-executable escalation receipt followed by at most one executable receipt and runs the side effect at most once; skill execution run in an isolated capability-scoped context whose ceiling governs everything the model does within it, or every loaded skill's ceiling conservatively retained and intersected until that model context is discarded (context freshness does not remove skill instructions from the conversation), with a delayed-call negative test proving a tool call deferred until after invocation end is still governed by the skill's ceiling rather than the broader non-skill ceiling, and output returned from an isolated skill context either terminal (never re-entering any model context as consumable instructions) or propagating the isolated context's full skill stack into every model context that consumes it, intersected and retained until that recipient context is discarded, with a negative test proving a tool call issued by a caller after consuming isolated-context output is still governed by the originating skill's ceiling; ingestion of a registered skill's instruction files into a model context through an ordinary file read (rather than the host's skill loader) treated as a skill activation that propagates the skill's identity and ceiling into the consuming context (intersected and retained until that context is discarded), or ordinary non-loader contexts prevented from consuming registered skill artifacts as instructions at all, with a negative test proving a tool call issued by a context that read a registered `SKILL.md` via an ordinary file read is governed by that skill's ceiling rather than the reader's broader non-skill ceiling, or that the read itself is refused, registered-skill identity propagated through copies and transformations (any artifact derived from a registered skill's instruction files host- or broker-labeled with that origin at the copying or transforming operation and treated on read as ingesting the originating skill, or registered skill artifacts refused as the source of ordinary non-loader copy and transform operations), with a copy-then-read negative test proving a tool call issued by a context that read a copied or transformed registered `SKILL.md` is governed by the originating skill's ceiling rather than the reader's broader non-skill ceiling, or that the copy is refused; skill-produced persistent artifacts carrying host-assigned origin metadata recording the producing skill stack, held outside the producing skill's write authority (a mandatory host/broker label or an authenticated out-of-band provenance record, never a self-carried marker the writing skill could omit or forge) and preserved across copies and updates, every update accumulating provenance rather than overwriting it (recording every contributing origin stack across the artifact's write history, with consumers governed by the intersection of all contributors' ceilings, and a mixed-writer negative test proving an artifact written first by a broader or non-skill context and then modified by a restricted skill governs its next reader at the restricted intersection), with the originating ceiling propagated (intersected and retained until discard) into every model context that consumes such an artifact, or skill-written artifacts prevented from re-entering any model context as consumable instructions, with a negative test proving a tool call issued by a context after reading a skill-written artifact is still governed by the producing skill's ceiling rather than the reader's broader ceiling, and a provenance-stripping negative test proving a skill write attempting to strip, omit, or forge an artifact's provenance is denied or still yields a host-assigned record identifying the producing skill stack; writes to machine-consumed control artifacts (CI workflows, host hooks, package scripts, executables another host process runs directly) treated as authority delegation, either denied by default (control-artifact paths carved out of every skill-writable ceiling) or origin-gated at the consumer's execution point (the runner verifying the recorded producing skill stack before executing, or confining execution within the originating ceiling), with admission failing closed for that write capability (the skill-writable ceiling narrowed to exclude that consumer's control-artifact paths, or the skill held un-invocable) whenever neither write-time denial nor consumer-side origin gating or confinement is enforceable, never a recorded-but-permitted residual boundary, and negative tests proving a skill-written modification to a machine-consumed control artifact is either rejected at write time or not executed with authority beyond the producing skill's ceiling, and that a declaration whose control-artifact write capability no reachable consumer can enforce is denied admission; the effective permission set being the intersection of every contributing skill's approved ceiling and its own declared permissions, with nested- and concurrent-skill negative tests (including a narrower-outer-declaration case where the outer ceiling permits what the outer declaration omits) proving that composing skills cannot amplify authority; agent delegation from a skill context either denied, or every child agent session minted a non-amplifying origin derived from the full parent skill stack with that intersection retained for the child's lifetime and propagated to further descendants, with a negative test proving a child agent launched from a skill context is denied an action above the parent skill's ceiling; interception resolving every side-effectful action regardless of origin class, skill-originated or authenticated non-skill (the non-skill origin rule authenticates the caller, not the tool), against an exhaustive admitted-tool registry and failing closed on any unmapped tool or alias (never exiting successfully as `_classify` does today for unrecognized actions), with real-handler negative tests proving an unknown or newly exposed tool is denied whether invoked from a skill or from an authenticated non-skill context; each admitted registry entry binding the implementation it was reviewed against (resolved server/handler identity and compatible schema or implementation version, plus a content-addressed deployment digest or equivalent signature over the handler's executable and dependency closure, held and verified outside agent and handler write authority) into the ceiling record and receipt, rechecked at execution time against both the implementation actually resolved and the implementation bytes actually loaded and failing closed on mismatch, with a handler-substitution negative test proving an admitted alias rebound to a different server, or a swapped handler behind an admitted tool name, is denied rather than executed under the stale admission, and an in-place mutation negative test proving a handler whose code is replaced in place while its name, identity, and advertised schema/version remain unchanged is denied rather than executed, and the gate executing the handler only from verified bytes (the verified handler and its dependency closure materialized into an immutable snapshot from which every load, lazy imports included, resolves, or loaded and verified through the same protected handles so the bytes verified are the bytes executed), with a concurrent swap-versus-load negative test racing a dependency replacement against a lazy or execution-time load and proving the swapped code never runs; each admitted handler's ambient effect footprint (the filesystem paths, network origins, credentials, and process capabilities its implementation exercises internally, which need not pass back through the dispatcher) either reviewed at admission and recorded in the ceiling record and receipt as authority the admission grants, with invocation from a skill whose ceiling is narrower than that footprint denied at issuance, or the handler executed under OS-level containment or a capability broker gating its transitive filesystem, network, and process effects to the invoking skill's effective ceiling, with a negative test analogous to the shell containment tests proving an admitted, unmodified handler whose internal behavior attempts an out-of-ceiling write or connection fails to perform that transitive effect, and each admitted handler's effective credential principal (the stable principal, account, or role identifier, with its scope and trust epoch, that its ambient credential or default context resolves to) bound into the ceiling record and receipt, resolved atomically with launch and failing closed when it designates a different principal, scope, or epoch, the verified result pinned as an immutable credential instance (a materialized session, token, or non-reswitchable handle) that is the only credential the handler consumes rather than a re-read of the mutable ambient credential, with a credential-substitution negative test proving a swapped ambient credential or context is denied rather than executed against a principal the admission never named and a concurrent switch-versus-use negative test proving an ambient-credential repoint raced against handler launch and use never runs the side effect as anything but the pinned verified principal, and the resolved ambient target context (the endpoint, cluster or project, region, namespace, and equivalent target identifiers the admitted footprint resolves to) bound into the ceiling record, approvals, and receipts, resolved atomically with launch, failing closed on mismatch, and pinned into the immutable credential instance the handler consumes rather than re-read from mutable defaults, with a target-substitution negative test proving a retargeted context or default with an unchanged principal is denied rather than executed against a target the admission never named, every security-relevant ambient input the admitted handler consumes (configuration files and defaults, environment values, feature flags, service-discovery records) enumerated at admission and bound by resolved value or content digest into the ceiling record and receipt, resolved atomically with launch into a pinned configuration snapshot that is the only configuration the handler consumes rather than a re-read of mutable state, failing closed on mismatch, with an ambient-configuration substitution negative test proving a changed unnamed default (a repointed service-discovery record or flipped feature flag) is denied or cannot alter the executed operation; interception implemented as a dispatcher or proxy in which the downstream host operation is itself the `tool_fn` executed by `execute_with_receipt` (an advisory hook that only emits evidence before the host performs the original call, as the `PreToolUse` hook path does today, does not count as routing), with a test proving that when the gate denies, no subsequent direct host execution path performs the operation; and the interceptor and its configuration held outside agent write authority (host-installed, not resolved from the writable checkout) or authenticated against a pinned digest with the host executing the verified bytes themselves from an immutable snapshot or protected copy materialized at verification time (never re-opened from the writable path after verification, closing the check-to-use window), with a hook-tampering negative test proving a modified hook or settings file yields fail-closed denial rather than silent bypass and that a hook swapped in after verification is never the code that executes, and the interceptor's interpreter and complete governance dependency closure (governance package, permission parser, executor implementation) equally protected as host-installed immutable code outside agent write authority or authenticated against pinned digests and executed from the same immutable materialization (never re-imported from the writable checkout after verification), with a negative test mutating an imported gate dependency while preserving the hook bytes and proving subsequent governed calls fail closed rather than execute the compromised gate. | Skill-driven agent actions become receipt-gated instead of trusted by convention. | Skills remain the least governed executable surface in a governance repository, and skill `permissions:` stay documentation-only. |

## Status evidence

Verified against `master` @ `941e398cb266b29b88325a58605a16008d2af63c`
(2026-07-17). Every `SHIPPED` /
`PARTIAL` row cites a source file and a test; each `PARTIAL` also names the
unmet sub-clause.

1. **Local kernel** — `kernel.py`, `executor.py`, `receipt.py`, `audit.py`; `gove-zone smoke`, `examples/receipt-gated-execution/demo.py`, and `test_kernel_dispatch.py` / `test_executor_guard.py` / `test_decision_receipt.py` / `test_audit_chain.py` / `test_end_to_end.py` / `test_fail_closed.py`.
2. **Secure profile (signature-required gate default)** — `profile.py` `production()` → `require_signature=True`; issuance still requires an explicit signer and the gate requires a matching trusted verifier. `test_profile.py::test_production_constructor_requires_signature` pins the unset-env → production default flip (#138); `test_receipt_signing.py` covers configured signing and verification.
3. **Replay evidence (two levels)** — audit-only `replay.py` verifies chain/event integrity and policy-version consistency; it cannot reproduce the original decision because audit records omit raw arguments. Decision re-derivation is a separate `replay_store.py` path and requires an opt-in `ReplaySideStore` plus the matching original policy bundle; `test_replay.py` and `test_replay_bundle_equivalence.py` cover those boundaries. The `consumption.py` ledger is anti-replay enforcement, not decision replay: its high-water-mark check catches wholesale + tail-below-HWM deletion, **not** interior burned-entry deletion (only the out-of-band `verify_ledger` chain check does), and the default `checkpoint=False` means no HWM at all. Workflow replay coverage is in `test_workflow_receipt_chain.py`; consumption integrity is covered by `test_consumption_{tamper,reconcile,hwm}.py`.
4. **Integration.** MCP/A2A + OpenAI-Chat/Responses + LangChain-style payload parsing ships in `integration.py`, with conformance cases in `test_adapter_conformance.py` and `test_mcp_binding.py`. The dependency-free `make_langgraph_tool_node` wrapper is exported from the same module; `test_langgraph_node.py` proves allow execution plus fail-closed deny and malformed-call behavior, and `examples/agent-framework-wrapper/demo.py` demonstrates the contract-level pattern. This does not claim conformance against an installed `langgraph` dependency.
5. **Evidence (exportable proof packs)** — `verifier.py::verify_proof_pack`; CLI `verify-proofpack`; `docs/PROOF_PATH.md`; conformance corpus `test_proofpack_corpus.py` (#144).
6. **Policy (PARTIAL).** *Shipped:* content-addressed policy **versioning** (`policy.py` `_compute_version` / `RuleSetPolicy`; CLI `policy inspect` / `policy export`; `test_policy_bundle_io.py`) and **tenant binding** (`tenant.py` active-bundle-by-tenant lookup; `test_tenant_safety.py`). *Missing:* policy-**bundle signatures** (no bundle signing exists) and the **active/stale/revoked lifecycle states** — the latter is explicitly *not modeled* (`contracts.py`: "(stale / revoked) is intentionally not modeled here").

## Roadmap wording rule

Use "planned", "roadmap", or "not implemented yet" unless a source file, test, demo output, and claim-ledger entry prove the feature exists.
